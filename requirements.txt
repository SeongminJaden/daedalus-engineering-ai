# Install inside the project venv ONLY, and always with PYTHONPATH unset:
#   env -u PYTHONPATH .venv/bin/pip install -r requirements.txt
# (A sourced shell environment can export PYTHONPATH and shadow venv packages
#  with older ones. Running with a clean PYTHONPATH avoids that.)

--extra-index-url https://download.pytorch.org/whl/cu130

# --- GPU physics core ---
warp-lang

# --- tensor / autodiff (CUDA 13.0 build, matches driver 595.84) ---
torch==2.13.0+cu130

# --- numerics ---
numpy
scipy

# --- config / schema ---
pyyaml
pydantic

# --- CLI + TUI monitoring ---
typer
rich
tqdm

# --- geometry / mesh ---
trimesh
# Marching cubes for turning a topology density field into a smooth surface.
# The existing voxel export is exact but blocky by construction; an isosurface
# is what makes an organic shape possible at all.
scikit-image

# --- test ---
psutil
pytest
# Parallel test distribution. See pytest.ini for when it helps and when
# it does not; it is not enabled by default.
pytest-xdist
