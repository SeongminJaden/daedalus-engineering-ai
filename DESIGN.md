# Daedalus Engineering AI: 개념설계 (v0)

> 이 문서는 자율 엔지니어링 AI 비전을 개발 머신(RTX 3050 4GB, 확장 예정)의 실제 실행 계획으로 내려앉힌 것이다. 에이전트/CLI 브랜드는 **Daedalus**.

## 0. 설계 원칙
1. **비전은 그대로, 하드웨어는 현실**: 3050 4GB에서 "지금 돌아가는 최대치"로 시작하되, 큰 GPU로 옮기면 코드 수정 없이 규모만 커지도록 **프로파일 옵션**으로 스케일을 분리.
2. **시스템 불가침**: 모든 파이썬·CUDA 의존성은 `.venv` 안에만. 시스템 파이썬(3.10.12)·드라이버 건드리지 않음.
3. **CLI 우선**: 자율 루프·모니터링은 터미널(rich TUI + JSONL 로그). 웹 GUI는 `interfaces/api/` 자리만 비워두고 후순위.

## 1. 기술 스택 (하드웨어 반영)
| 계층 | 선택 | 이유 |
|---|---|---|
| 물리 코어 | **NVIDIA Warp (warp-lang)** | pip·venv 설치, 자체 JIT(시스템 CUDA 툴킷 불필요), 미분가능, sm_86 지원, 확장성 |
| ML surrogate | **PyTorch (cu13x)** | CUDA 동작 확인, Warp↔torch 텐서 연동 |
| 최적화 | Warp autodiff + 파이썬 진화/베이지안 | 4GB에선 GPU 배치평가 + CPU 메타옵티마이저 |
| Geometry | implicit/SDF(파이썬+Warp) 우선, CAD 후순위 | 초기 topology엔 voxel/level-set이 적합 |
| Orchestration | Python + Typer CLI | |
| 모니터링 | rich/textual TUI + JSONL | CLI-only, tmux attach로 관찰 |
| 추론(LLM) | 외부 세션 오케스트레이터 | 상위 오케스트레이션은 엔진 바깥의 LLM 세션이 담당 |

## 2. GPU 프로파일 시스템 (핵심)
스케일을 코드에서 분리 → `configs/profiles/*.yaml`. 선택 우선순위: `--profile` → `ENG_PROFILE` → VRAM 자동감지 → default fallback. 로더: `core/profile.py`.

| 프로파일 | VRAM | 성격 |
|---|---|---|
| laptop_4gb | 4GB | 현재. 후보풀 작게, 저해상, AMP+grad checkpoint |
| desktop_16gb | 16GB | 중형 확장 |
| rtx5090_32gb | 32GB | 고대역폭·빠른 solve. 이 워크로드 속도 최적 |
| dgx_spark_128gb | 128GB | 대용량·저대역폭. 큰 문제/로컬LLM용, solve 속도는 낮음 |
| cloud_a100 | 80GB | 대량 병렬 |

## 3. 하드웨어 사이징 가이드 (참고)
VRAM을 잡아먹는 3요인: ①3D 토폴로지 해상도(res³×필드×4B) ②동시 후보 수 ③surrogate 모델. 병목은 항상 "후보수×해상도"이며 프로파일이 상한을 건다.
- **4GB(현재)**: 전 단계 개발 + MVP 가능. ~256³, 후보 ~수백~1천.
- **32GB(5090)**: 이 프로젝트 대부분을 빠르게. 대역폭 1.8TB/s로 solve가 빠름.
- **128GB(DGX Spark)**: 용량은 크나 대역폭 273GB/s로 solve 느림. 로컬 대형 LLM·초대형 단일문제에 유리.
- **멀티GPU**: 독립 후보 병렬은 선형 확장(주 워크로드에 최적). 단일 거대문제 분산은 NVLink 없는 5090에선 PCIe 병목. → 설계에 device-pool(후보 배치 샤딩) 개념 예정.

**판단 기준**: "solve를 빨리·많이" = 5090. "거대한 걸 통째로 담기/로컬 LLM 두뇌" = DGX Spark. 지금은 하드웨어 구매 불필요: 3050으로 전 파이프라인 구축 후 천장에 닿을 때 선택. 어느 쪽이든 `--profile`로 흡수.

## 4. 자율 루프 매핑
MD의 REASON→PLAN→DESIGN→ACT→SIMULATE→EVALUATE→LEARN→UPDATE_BRAIN을 현 구조에 매핑:
- 추론/계획/관찰/학습(LLM층) = 외부 LLM 세션 오케스트레이터
- 설계생성/물리/최적화/평가(엔진층) = venv 파이썬+Warp 프로세스
- Brain = 레포 내 구조화 저장소(초기 SQLite+JSONL, 후에 벡터+그래프)

## 5. 단계 계획 (물리엔진 조기투자 반영)
- **Phase 0 (완료)**: venv + 스켈레톤 + 프로파일 + Warp/torch GPU 새너티.
- **Phase 1**: Engineering IR + Design Genome (단일 링크 파라메트릭).
- **Phase 2**: Warp GPU 물리 코어: 보/링크 응력·처짐·질량, 미분가능. 해석해 대조 검증.
- **Phase 3**: 최적화 루프: Warp autodiff 경사 + 진화탐색, 4GB 내 다후보 배치.
- **Phase 4**: 자율 루프: 세션이 엔진 구동·관찰·재계획.
- **Phase 5**: Brain: 에피소드/전략/지식.
- **Phase 6**: Surrogate(torch): 데이터 축적 후.
- **Phase 7**: 고정밀 검증 + 확장 프로파일.

MVP 목표(MD §34): "하중 받는 단일 로봇 링크의 최소 질량 설계" 루프 1회 완주.

## 재료 확장 로드맵
재료 확장은 **물리가 감당할 수 있는 순서**로 진행한다. 스키마와 솔버가 못 받는 재료를 먼저 넣으면 값은 들어가도 계산이 틀린다.

- **1단계 (현재/MVP)**: 등방성 Al 2종만: `al_7075_t6`, `al_6061_t6`.
- **2단계: 등방성 금속 세트 확장**: 6061 / 7075 / 2024, S45C / SCM440, SS316, Ti-6Al-4V, AZ31 등. **현재 스키마·선형탄성 물리를 그대로 수용**하므로 코드 변경 없이 데이터만 추가하면 됨. 각 항목은 출처 있는 값 + `status` 태그 필수.
- **3단계: 이방성 재료 추가**: CFRP(방향별 물성·적층 구성), 3D프린팅 플라스틱(공정 의존·직교이방성).
  **선행 조건: 스키마 확장(방향별 물성 필드) + 이방성 솔버 지원.** 단일 E/항복/포아송 스키마에 이방성 재료를 밀어넣으면 물리가 틀리므로, 선행 조건이 갖춰지기 전에는 추가 금지.

## 모델 충실도 (Phase 2 시점)
Phase 2의 물리는 **Euler-Bernoulli 보 이론**이다. 미분가능하고 배치 평가가 싸다는 게 장점이지만, 다음을 **반영하지 않는다**:
- **근부 응력집중 미반영**: 필렛·볼트홀·체결부 형상을 못 봄. 실제 근부 최대응력은 여기서 보고하는 값보다 **높다**. 보고된 응력은 실제 peak의 하한으로 취급할 것.
- **전단변형 미반영**: 처짐은 굽힘 성분만. 세장비가 큰 링크에선 타당하나 뭉툭해질수록 틀림.
- **좌굴 미검사**: 얇은 벽은 항복 전에 국부 좌굴로 먼저 망가질 수 있는데 감지 못 함.
- 균일 프리즘 단면·완전 재료 가정(테이퍼·조인트·엔드캡·용접부·잔류응력 없음), 정적 해석만.

따라서 이 단계를 통과한 설계는 **후보**이지 검증된 부품이 아니다. 응력집중·전단·좌굴은 후속 **3D FEM 고정밀 단계**에서 해소하며, 설계는 그 관문을 통과해야 실물로 취급한다. (값싼 미분가능 모델로 공간을 탐색하고, 비싼 고정밀 모델로 승자를 확정하는 해석↔FEA 분업.)

## 6. 운영 규칙 (필독)
- **venv는 깨끗한 PYTHONPATH에서 실행할 것(`env -u PYTHONPATH`).** 셸에 source된 다른 환경이 PYTHONPATH를 내보내면 그쪽 패키지가 venv 패키지를 가려서, 버전이 다른 numpy 같은 게 조용히 먼저 잡힌다. 부트스트랩 설치 스크립트가 이 래핑을 자동으로 처리하는 것을 목표로 한다.
- venv는 sudo 없이 `--without-pip` + get-pip 부트스트랩으로 구성됨(시스템 미변경).
- git init만 됨, 커밋은 사용자 확인 후.

## 최적화 결과의 전제 (Phase 3)
MVP 링크의 최적해는 **처짐 지배(deflection-limited)**다. 응력 제약은 70% 넘게 여유가 남고(안전율 ~14, 요구 2) 팁 처짐만 1 mm 한계에 붙는다. 즉 이 링크가 지불하는 비용은 강도가 아니라 **강성**이다.

주의할 점: 설계변수 3개 중 **2개가 bound에 붙어서** 끝난다: b는 하한 10 mm, t는 하한 1 mm. 따라서 최적 질량은 물리만큼이나 **bound 설정이 좌우한다.** 특히 `t_min = 1 mm`는 CNC 알루미늄 가정의 **[ASSUMED]** 제조성 한계이지 유도된 값이 아니다(`core/design_genome/bounds.py`). 공정이 바뀌면(적층 제조, 압출, 판재 성형) 이 값이 바뀌고 최적 질량도 함께 바뀐다. 최적화 결과를 "달성 가능한 질량"으로 취급하기 전에 이 가정부터 확인할 것.

## 자율 루프의 "추론"에 대한 정직한 범위 (Phase 4)
Phase 4의 reasoner는 **결정론적 규칙기반 휴리스틱**이다. **LLM이 아니다.** 탐색/활용 스케줄을 따를 뿐이며, 이걸 "AI 추론"이라고 부르면 과대주장이다. 코드·README·이 문서 모두 휴리스틱이라고 명시한다.

의미가 있는 부분은 **확장점**이다. `Reasoner`는 `decide(state, history) -> Action` 하나짜리 추상 인터페이스이고, 루프 엔진은 그 너머를 보지 않는다. 따라서 LLM 기반 정책을 나중에 엔진 수정 없이 끼워넣을 수 있다. 전체 시스템에서 LLM은 이 클래스가 아니라 **바깥 오케스트레이터**(이 엔진을 구동하는 세션)에 해당한다.

루프가 단발 최적화보다 나은 이유: SLSQP는 가장 가까운 KKT 점으로만 수렴하는데, 설계변수 3개 중 2개가 bound에 붙는 이 문제에서는 "다른 basin이 더 나은가"가 실제 질문이다. 루프는 그래서 **multi-start 오케스트레이터**다: exploit은 현 best 주변 정제, explore는 무작위 재시작, 정체 시 강제 탐색. 국소최적·bound 견고성이 여기서 나온다.

## Brain의 정직한 범위 (Phase 5)
Brain은 **"실험 검증된 사실 저장소"가 아니라 "증거수준이 표시된 경험 저장소"**다. 세 가지를 명확히 한다.

1. **모든 지식은 시뮬레이션 유래**다. Phase 2 충실도(Euler-Bernoulli, 응력집중·전단·좌굴 미반영)를 그대로 상속하며, 각 지식항목의 `assumptions`에 그 사실이 기록된다.
2. **검색은 수치 특징 유사도이지 의미검색이 아니다.** 임베딩 모델이 없고 "의미" 개념도 없다. 설계의 공학량(문제 파라미터·설계변수·metric)을 공통 스케일에 올려 비교할 뿐이다. 그래서 API 이름도 `semantic_search`가 아니라 `retrieve_similar`다. 텍스트 임베딩은 모델 의존성이 필요해 이번 단계에서 의도적으로 배제했고, 확장점으로만 문서화했다.
3. **EXPERIMENTALLY_VALIDATED는 시뮬레이션으로 도달 불가능하다.** 물리시험 증거(`PHYSICAL_TEST`)만 그 관문을 연다. 시뮬 1000회도, 통과한 테스트 스위트도, 해석적 유도도 열지 못한다. 이것이 Brain이 스스로를 과신하지 않게 하는 핵심 규칙이며 전용 테스트로 고정돼 있다.

**독립성은 에피소드가 아니라 run 단위로 센다.** 한 번의 최적화 run 안의 20개 이터레이션은 하나의 탐색을 20번 샘플링한 것이지 독립 관측 20개가 아니다. 이를 독립으로 세는 것이 바로 기억이 근거 없는 확신을 쌓는 경로라서, 단일 run은 아무리 길어도 최대 SIMULATED에 머문다. 미해결 반례가 있으면 레벨은 REPEATED로 상한이 걸린다.

confidence는 발명하지 않는다. `min(n/(n+k) × 1/(1+미해결반례), 레벨 상한)` 이며, 증거에 단조증가, 반례에 감소, [0,1] 유계.

## Surrogate의 정직한 범위 (Phase 6)
- **근사 대상은 Phase 2 보 이론 평가기이지 3D FEM이 아니다.** 아직 고정밀 모델이 없으므로 surrogate 오차는 보 이론 오차 **위에 더해진다**. 근사의 근사다.
- **현재 속도 이득은 없다.** 20,000 후보 실측: surrogate 약 850만/s vs 배치 솔버 약 2,200만/s → **0.38배, 즉 surrogate가 약 2.6배 느리다.** 보 커널이 닫힌 형태 산술이라 MLP 순전파보다 싸기 때문이며 당연한 결과다. 이 인프라의 가치는 base가 비싼 3D FEM이 되는 **Phase 7에서 발현**된다. (프로파일 청크 경로(batch 4, 5,000회 launch)와 비교하면 약 5,000배 "speedup"이 나오는데 그건 launch 오버헤드지 물리가 아니다. 공정한 비교는 단일 배치 launch 기준이다.)
- **2단계 규율**: surrogate는 순위만 매기고 **결정은 솔버가 한다.** `screen_and_verify`가 반환하는 설계는 항상 솔버가 실제로 평가한 것이며(`verified=True`), 보고되는 질량·응력·처짐은 전부 솔버 값이다. 스크리닝이 좋은 후보를 잘못 버릴 수는 있다(recall 위험, 단축의 대가). 하지만 **검증 안 된 설계를 결과로 내놓지는 못한다.**
- 예측은 절대 맨몸으로 반환되지 않고 metric별 p95 held-out 오차를 동반한다. 안전율은 예측하지 않고 예측 응력에서 **유도**한다(모델이 낸 응력과 어긋날 수 없게).
- **정확도 측정의 함정 1건**: 데이터는 컨텍스트 단위로 묶여 생성되므로(컨텍스트 1개 = launch 1회, 설계 다수) 무작위 **행 분할**은 같은 문제의 설계를 train/test 양쪽에 넣는다. 그러면 "본 적 있는 문제 안에서의 보간"을 재는 셈이라 점수가 부풀려진다. 그래서 **컨텍스트 단위 분할**이 기본이며, held-out은 학습 때 본 적 없는 문제들로만 구성된다.
- **단조성은 보장되지 않는다.** 학습 모델은 물리 경향을 지킬 의무가 없어서, 벽 두께를 늘려도 예측 응력이 안 줄어드는 위반이 약 0.1% 표본에서 발생한다. 작지만 0은 아니며, 이것도 솔버가 최종 결정을 하는 이유다.

## 3D FEM의 정직한 범위 (Phase 7)
- **고정 경계는 응력 특이점이다.** 이상적인 완전고정 면의 재진입 모서리에서 선형탄성 해는 응력이 발산한다. 따라서 **peak 응력은 메시에 수렴하지 않는다.** 실측: 고체 캔틸레버를 4단계 정제하며 peak von Mises가 3.485 → 3.842 → 4.043 → 4.314 MPa로 계속 상승했고 멈추지 않았다. 그러므로 두 가지를 구분해 보고한다.
  - `peak_von_mises_pa`: 원시 최대값. **메시 의존, 수렴하지 않음.** 같은 방식으로 메시한 설계들 사이의 상대 비교에만 쓸 것. 부품을 인증하는 숫자가 아니다.
  - `gauge_von_mises_pa`: 지지부에서 단면 높이만큼 떨어진 게이지 스테이션의 값. 매끄러운 굽힘장 안이라 **수렴한다.**
- **처짐은 이 문제가 없고 깨끗하게 수렴**하므로 신뢰할 수 있는 강성 지표다.
- 응력 비교는 **같은 station·같은 fibre**에서 한다. 요소 중심 응력을 최외곽 fibre 값과 비교하면 20~30% "오차"처럼 보이지만 그건 샘플링 위치 차이일 뿐이다. fibre를 맞추면 FEM/보 이론 비 = 1.0015 → 1.0027로 수렴한다.
- 3D FEM도 **여전히 시뮬레이션**이다: 선형탄성, 소변형, 등방성, 이상화된 완전고정 경계, 필렛·체결부·접촉 없음. 실제 지지부는 완전 강체가 아니고 실제 부품에는 필렛이 있다. 진짜 peak 응력을 알려면 실제 조인트 형상이 필요하며 그건 이 모델 밖이다.
- Brain 기록도 **SIMULATED에 머문다.** 충실도가 높아졌다는 사실은 note에 남기지만 증거 종류는 여전히 시뮬레이션이며, **EXPERIMENTALLY_VALIDATED는 물리시험 없이는 여전히 도달 불가**다. 전용 테스트로 고정했다.

## Phase 7.5: 다충실도 루프를 닫다 (실패에서 배워 재설계)
Phase 7의 3D FEM 관문이 Phase 3 최적해를 탈락시켰다. 원인은 값싼 모델(Euler-Bernoulli)이 **전단 변형을 빼먹기** 때문이고, MVP 링크는 세장하지 않다(L/h 약 6). 최적화기는 값싼 모델의 맹점 위에 정확히 올라앉은 해를 찾아냈던 것이다. Phase 7.5는 그 모델을 고치고 다시 설계해 관문을 통과시킨다.

**1. 보 모델 업그레이드: Euler-Bernoulli → Timoshenko**
δ = PL³/(3EI) + PL/(G·A_s), G = E/(2(1+ν)), A_s = 웹 면적.
중공 박스는 수직 벽(웹)이 전단을 나른다고 보아 **A_s = 2·t·h**로 둔다. 이 계수는 **[ASSUMED]** 박벽 박스 근사이지 유도한 값이 아니며, **3D FEM으로 검증**했다. 기존 Euler-Bernoulli 경로는 `shear_deformation=False`로 남겨 두 모델의 차이(= 전단 기여)를 직접 볼 수 있게 했다. 미분가능성은 유지된다(autodiff가 전단 항을 통과하며, 벽 두께 민감도가 실제로 달라지는 것을 테스트로 확인).

**2. 3D FEM 대조 (계수 검증)**  단면 10×40×1 mm, L/h를 4에서 20까지:
| L/h | 3D FEM | Euler-Bernoulli | EB/FEM | Timoshenko | Timo/FEM |
|---|---|---|---|---|---|
| 4 | 0.23771 mm | 0.22302 | 0.9382 | 0.23758 | **0.9995** |
| 6 | 0.77202 | 0.75271 | 0.9750 | 0.77454 | 1.0033 |
| 8 | 1.80565 | 1.78419 | 0.9881 | 1.81331 | 1.0042 |
| 12 | 6.03701 | 6.02165 | 0.9975 | 6.06532 | 1.0047 |
| 20 | 27.81803 | 27.87802 | 1.0022 | 27.95081 | 1.0048 |

평균 절대오차: Euler-Bernoulli **2.067%** → Timoshenko **0.350%**, 약 **5.9배 개선**. 전단이 가장 중요한 L/h=4에서 0.9382 → 0.9995로 거의 정확해진다.
참고로 잔차에 최소자승으로 전단계수를 맞추면 음수가 나오는데, 이는 큰 L/h에서 두 1D 모델이 **모두** FEM을 약간 넘어서기 때문이다(FEM 메시 이산화와 고정 경계의 영향). 즉 잔차가 순수한 전단 성분이 아니므로 그 피팅은 의미가 없고, A_s = 2th(계수 1.0)를 유지한다.

**3. 재최적화와 고정밀 재검증**
| | 질량 | 모델 처짐 | 3D FEM 처짐 | 판정 |
|---|---|---|---|---|
| Phase 3 최적해 (EB) | 0.249977 kg | 1.02248 mm | 1.01898 mm | **FAIL** |
| Phase 7.5 최적해 (Timoshenko) | **0.251828 kg** | 1.00000 mm | **0.99749 mm** | **PASS** |

새 최적해는 b/h/t = 10.000 / 81.618 / 1.000 mm로 높이가 0.66 mm 자랐고 질량은 **+0.74%** 늘었다. 강성을 공짜로 얻고 있던 만큼을 실제로 지불한 셈이다. SLSQP와 DE가 다시 일치했고 활성제약은 여전히 처짐이다.
**여유는 +2.51 μm(0.25%)로 얇다.** 통과하는 이유는 보정된 모델이 FEM보다 처짐을 약간 **크게** 예측하기 때문이다(FEM/모델 = 0.9975). 즉 안전한 방향으로 보수적이라 한계에 붙여 최적화해도 관문을 통과한다. 반대 방향이었다면 마진을 명시적으로 넣어야 했을 것이다.

**4. 정직성**  Timoshenko도 여전히 1D 보 근사다. 3D FEM이 관문으로 남고, 그 FEM조차 시뮬레이션이다. Brain에 기록되는 이 교훈의 증거수준도 **SIMULATED**에 머문다. 시뮬레이션 두 개를 대조한 것은 아무리 한쪽이 충실해도 물리시험이 아니다.

## Phase 8: 재료 전면 확장과 이방성 (구현 완료)
재료 15종(기존 2종 + 신규 13종)과 직교이방성 탄성을 지원한다.

**등방성을 특수경우로 통일**: 등방 재료도 Ei=E, Gij=E/(2(1+ν)), νij=ν로 9개 직교이방 상수로 전개한 뒤 **같은 compliance 역행렬 경로**를 탄다. 등방에만 별도 지름길을 두면 두 경로가 조용히 어긋날 수 있고, 그러면 Phase 7의 등방 결과가 일반 코드의 회귀 검사 역할을 못 하게 된다. 실측 일치: 일반 경로와 닫힌 형태 등방 행렬의 최대 차이 **3.05e-05 Pa**(1e11 스케일 대비 상대 3e-16), 전체 FEM 해까지 상대 1e-9 이내 동일.

**Constitutive 검증**: 모든 재료의 6×6 강성 C에 대해 **대칭성과 양정치**를 단언한다. 양정치가 아닌 C는 변형만으로 에너지를 방출하는 물리적으로 불가능한 재료를 뜻하므로 **거부**한다. 상반성 νij/Ei = νji/Ej는 저장하지 않고 항상 유도해서 compliance가 데이터 파일의 오타로 비대칭이 될 수 없게 했다.

**직교이방 FEM 검증**: 상수변형 패치 테스트를 CFRP 강성으로 통과(변위·응력 모두 1e-12 이내). 축정렬 CFRP 봉의 신장을 해석값 E1·A/L과 대조하니 비 **1.057**인데, 이는 완전고정 근부가 근처의 횡수축을 막아 봉이 약간 뻣뻣해지기 때문이며 자유수축 공식보다 큰 강성이 나오는 것이 정상이다. 등방 재료로 같은 시험을 하면 방향 선호가 전혀 없음(상대 1e-6 이내)을 대조군으로 확인했다.

**재료 DB (전부 reference_typical, 출처 명시, 생산 전 데이터시트 대조 필요)**
- 등방 금속 6종 추가: al_2024_t3, ti_6al_4v, steel_s45c, steel_scm440, ss_316, mg_az31b.
- 고분자 5종: abs, pla, pa12, petg, pc. **온도·변형률·공정 의존이 크다**고 각 항목 notes에 명시했다. 특히 프린팅 부품은 등방이 아니며, 여기 값은 벌크 물성이라 프린팅 부품의 **상한**이다.
- 세라믹 alumina_al2o3: **연성 항복점이 없다.** 스키마 유지를 위해 yield 필드에 굴곡강도(~350 MPa)를 넣되, **항복 기준 안전율은 이 재료에 틀린 파괴 기준**임을 notes에 적었다. 세라믹 강도는 산포 지배·크기 의존이라 Weibull 처리가 필요하고 이 DB는 그걸 제공하지 않는다.
- 직교이방 복합재 cfrp_ud: E1/E2 = **13.5배**, 섬유방향/횡방향 강도비 **30배**. 단일 항복값이 무의미하므로 방향별 강도(long/trans/shear)를 필수 필드로 두고, 없으면 스키마가 거부한다.

**G는 유도값이다**: 등방 재료의 전단탄성계수는 추정이 아니라 정의(G = E/2(1+ν))다. `derived_fields`에 기록하고, **저장된 값이 실제로 그 유도와 일치하는지 검사**한다. 처음에 유효숫자 6자리로 써서 12종이 이 검사에 걸렸고, 값을 완전 정밀도로 다시 써서 해결했다. 검사가 없었다면 "유도했다"고 적힌 채 어긋난 값이 남았을 것이다.

**물성 추론층 (규칙기반, ML 아님)**
- **유도(DERIVED)**: 정확한 항등식만. G, 체적탄성계수, 상반 포아송비. 불확실성 없음.
- **추정(ESTIMATED)**: 상관식·경험칙. **반드시 불확실성과 근거를 동반**하고, 적용하면 재료 status가 **ASSUMED로 강등**되며 notes에 "NOT a datasheet value"가 붙는다. 예: 강의 Se ≈ 0.5·Su(±25%), 알루미늄·마그네슘은 **진짜 내구한도가 없어서** 같은 형태의 규칙이라도 ±40%로 신뢰도가 낮다.
- ML 예측기는 일부러 만들지 않았다. 학습 데이터가 없고, 규칙보다 감사하기 어렵다. 나중에 넣더라도 같은 Estimate 형태(불확실성 포함)를 내야 한다.

**보 모델**: 축방향 모듈러스로 E1을 쓴다. 등방은 값이 같아 불변. 직교이방은 **재료 1축이 보 축과 정렬됐다는 가정**이며, off-axis 하중은 인장과 전단을 커플링시켜 1D 보 모델이 표현할 수 없다는 한계를 명시했다.

**여전히 SIMULATED**: 재료가 늘고 이방성을 다뤄도 증거수준 규율은 그대로다. 물리시험 없이는 EXPERIMENTALLY_VALIDATED에 도달하지 못한다.

## Phase 9: CAD 출력, STEP (구현 완료)
파라메트릭 설계를 B-rep으로 만들고 STEP으로 내보낸다. 커널은 **build123d 0.11.1**(OCCT 래핑), 대안으로 cadquery를 시도한다.

**선택 의존성**: CAD는 `requirements-cad.txt`로 분리했다. 해석만 쓰는 사용자가 수백 MB짜리 OpenCascade를 받을 이유가 없다. 커널이 없으면 CAD 테스트는 **skip**되고 `dae export`는 트레이스백 대신 설치 안내를 낸다. 커널을 차단한 환경에서 실제로 확인함: CAD 테스트 10개 skip, 6개 통과, 나머지 스위트는 영향 없음.

**정합 관문 (핵심)**: 내보낸 STEP이 **해석한 그 부품인지** 검사한 뒤에만 파일을 쓴다.
1. B-rep 부피 vs 해석 단면적×길이
2. 그 부피×밀도 vs 물리가 사용한 질량
어긋나면 예외를 던지고 **파일을 남기지 않는다**(테스트로 확인). 어긋난 채 내보내면 아무도 시뮬레이션하지 않은 부품을 제조에 넘기는 셈이기 때문이다.

Phase 7.5 최적해 실측:
- 부피 8.961850000e-05 m³, 해석값과 **상대 6.0e-16** 일치
- 질량 0.251827985 kg, 해석 질량과 **상대 2.4e-07** 일치
- 단일 솔리드, bbox 500.0000 × 81.6185 × 10.0000 mm로 설계 치수와 일치
- STEP 왕복: 다시 읽어 부피 1e-9 이내 동일, ISO-10303 헤더 확인

**질량 잔차 2.4e-07의 출처를 분명히 해둔다**: CAD 부피는 float64이고 박스에 대해 정확하다. 이 잔차는 **fp32 물리 커널의 정밀도**에서 온다. 따라서 허용오차를 fp32보다 조이면 엉뚱한 것을 재게 된다.

**"항상 STEP"의 경계를 정직하게 그었다**
- **파라메트릭 솔리드: 보장.** 중공 박스는 평면 몇 개라 B-rep이 정확하고 STEP이 깨끗하다.
- **유기·토폴로지 형상: 보장 안 됨.** 밀도장이나 음함수 표면으로 나오지 면과 모서리로 나오지 않는다. 깨끗한 B-rep을 얻으려면 표면 재구성과 재피팅이 필요하고, 대충 하면 downstream CAD가 받아주지 않는 NURBS 조각모음이거나 솔리드인 척하는 수천 개 패싯이 된다. 그래서 `mesh_from_density_field`는 **의도적으로 NotImplementedError**다. 제조 가능해 보이지만 아닌 지오메트리를 뱉는 것보다 낫다.
- `export_stl`은 기존 B-rep을 테셀레이션한다. 시각화·프린팅용이며 허용오차가 명시된 근사이지 치수가 중요한 곳에서 STEP의 대체가 아니다. 실측: watertight, 부피 상대 2.7e-09.

**이것은 해석 형상이지 제조 준비 형상이 아니다**: 필렛도, 체결 피처도, 공차도 없다. 특히 날카로운 근부 모서리는 **Phase 7이 응력집중을 찾아낸 바로 그 위치**이며 실제 부품이라면 거기에 필렛이 필요하다. DfM 피처는 후속 단계이고, 다부품 조립 STEP은 Phase 10이다. CLI 출력에도 이 경고를 상시 표시한다.

## Phase 10: 어셈블리와 기구학 (구현 완료)
단일 부품에서 다물체 조립으로 확장했다. 링크·조인트·구속을 정의하고, 기구학으로 자세를, 정역학으로 링크별 하중을 구해 기존 구조해석에 연결한다.

**좌표계를 먼저 못박았다**: 오른손 좌표계, SI. 월드는 x 전방, **y 상방**, z 지면 밖. 중력은 **-y**. 이건 `configs/default.yaml`과 보 모델(단면 높이 h와 팁 하중이 모두 y축)에 맞춘 것이다. 로보틱스 관행대로 z-up을 골랐다면 **기존 구조 결과 전부에 대해 하중 방향이 조용히 뒤집혔을 것**이다. `joint_torques`는 **자세를 유지하는 데 필요한 구동 토크**를 반환한다(수평 팔이 아래로 당기는 페이로드를 들려면 양의 토크).

**검증 (전부 독립 대조)**
- **FK vs 2링크 닫힌형식**: 6개 자세에서 **1e-12 이내** 일치. 구현은 축·원점 기반 일반식이라 평면 팔을 전혀 모른다. 따라서 닫힌형식은 재진술이 아니라 외부 검증이다.
- **Jacobian vs 중앙차분**: 4개 자세에서 최대 오차 **1e-6 미만**(실측 7.3e-11). 각속도 행이 조인트 축과 일치하는지도 확인.
- **τ = J^T F 일관성**: 별도 테스트로 고정.
- **IK 왕복**: 4개 목표에서 수렴, 해를 FK에 넣으면 목표 복원(1e-6). 도달 불가 목표는 **수렴 실패로 보고**(자신 있는 오답을 내지 않음). 조인트 한계 준수, **특이점(완전 신전) 시작에서도 발산하지 않음**(damped least squares. 유사역행렬이면 터진다).
- **정적 토크 손검증**: 자중 포함해 모멘트 합과 **1e-12 이내** 일치.
  τ1 = P(L1+L2) + m1·g·(L1/2) + m2·g·(L1+L2/2), τ2 = P·L2 + m2·g·(L2/2)
- **단일링크 브리지**: 1링크 고정 어셈블리가 이전 단계의 캔틸레버 근부 모멘트를 정확히 재현(프로젝트의 두 갈래가 어긋나지 않았음을 확인).

**🐛 손검증이 잡은 버그**: 기하 Jacobian 공식 `Jv = z × (p − p_joint)`는 **그 조인트가 실제로 움직이는 점**에만 유효하다. 모든 조인트에 무조건 적용하면 안쪽 링크의 질량중심이 바깥 조인트에 대한 민감도를 갖게 된다(팔꿈치가 위팔 무게중심을 움직일 수 없는데도). 결과 오차는 1% 미만이라 **토크 표에서 그럴듯해 보였고**, 손으로 계산한 모멘트 합이 잡아냈다. `supporting_joints`로 열을 제한하고 테스트로 고정했다.

**하중 전달**: 각 링크는 자기 **근부 굽힘모멘트**를 재현하는 `equivalent_tip_load_n`을 받는 캔틸레버로 검사한다. 근부 모멘트가 굽힘응력을 지배하고 구조가 임계가 되는 지점이므로 사이징에는 이 등가가 맞다. 다만 자중의 분포 형상은 재현하지 않으므로, **자중이 지배적인 링크는 분포하중 모델이 필요**하다는 한계를 명시했다.

**어셈블리 STEP**: FK로 배치한 다부품을 compound로 출력. Phase 9 정합 관문을 **부품마다 재사용**하고, 어셈블리 질량이 링크 질량 합과 일치하는지 추가 검사(실측 상대 **1.8e-16**). 왕복 시 솔리드 2개와 총 부피 보존 확인.

**캡스톤 데모** (`dae assemble --payload 2 --step`): 2링크 평면 팔, 최악 중력 자세 자동 탐색 → 관절토크(shoulder −11.5804, elbow −5.0549 N·m) → 링크별 구조검증(link1 σ 5.210 MPa / SF 96.55 PASS, link2 σ 3.720 MPa / SF 135.20 PASS) → 어셈블리 STEP 출력. 페이로드를 400 kg으로 올리면 구조검사가 **실패**하는 것도 테스트로 확인했다(실패할 수 없는 검사는 검사가 아니므로).

**정직성: 정역학까지만이다.** 관성·코리올리·가속 토크가 없고, 마찰·백래시·관절 컴플라이언스도 없다. 강체와 이상 관절 가정이다. 이 범위는 "이 자세를 중력과 페이로드에 맞서 유지하라"를 다루며 그것이 **링크**를 사이징한다. **모터와 감속기 사이징은 하지 못한다** 이것은 동역학 항이 필요하며 **Phase 11 이후**다. 폐루프 기구도 구속 해석이 필요해 후속이다. 여전히 SIMULATED.

## Phase 11: 강체 동역학 (구현 완료)
정역학에서 동역학으로. τ = M(q)q̈ + C(q,q̇)q̇ + G(q) + F_friction.

**링크 관성을 기하에서 유도**: 중공 프리즘 = 솔리드 박스 − 동심 공동이므로 관성텐서도 두 닫힌형식의 차. 동심이라 평행축 항이 필요 없다. **CAD 커널(실제 B-rep 적분)과 대조해 상대 7.6e-16 일치.** `is_valid_inertia`는 대칭·양정치에 더해 **주관성모멘트의 삼각부등식**까지 검사한다. 임의의 양정치 행렬이 곧 강체의 관성은 아니기 때문이다.

**M은 운동에너지에서 조립**: M(q) = Σ_k [m_k Jv_k^T Jv_k + Jw_k^T R_k I_k R_k^T Jw_k]. Phase 10의 야코비안을 그대로 재사용하므로 **조상 조인트 제한(Phase 10에서 잡은 버그의 수정)이 자동으로 상속**된다. C는 M의 Christoffel 기호에서 얻는다.

**검증 (전부 독립 대조)**
| 항목 | 대조 대상 | 최악 오차 |
|---|---|---|
| M(q) | 2링크 평면 팔 닫힌형식 | **3.5e-18** |
| C(q,q̇) | 동일 닫힌형식 | **3.4e-12** (∂M/∂q 중앙차분 한계) |
| G(q) | 동일 닫힌형식 | **5.6e-17** |
| 정역학 브리지 | q̇=q̈=0에서 Phase 10 holding torque | **0** (정확) |
| passivity | Ṁ − 2C 반대칭 | **0** |
구현은 야코비안·Christoffel 기반 일반식이라 평면 팔을 모른다. 따라서 2링크 닫힌형식은 외부 검증이다. G가 Phase 10 statics를 **재사용**하므로 정역학 극한은 근사가 아니라 구조적으로 정확하고, 두 단계가 어긋날 수 없다.
passivity는 **토크만 맞고 C가 틀린 구현을 걸러내는** 성질이라 따로 검사했다.

**하중 케이스와 peak ≠ continuous**
LC1 nominal / LC2 max payload / LC3 max acceleration / LC4 worst gravity / LC6 holding / LC7 combined worst. 모터는 짧게 낼 수 있는 **peak** 정격과 계속 낼 수 있는 **continuous** 정격이 따로 있고 보통 2~3배 차이가 난다. peak만 보고 고르면 과사양, continuous만 보고 고르면 가속할 때마다 과열이다. 둘 다 낸다.
continuous는 **duty 가중 RMS 토크**다(발열이 전류 제곱, 전류가 토크에 비례하므로). duty 0인 순간적 최악 조합은 peak를 지배하되 **열 정격은 올리지 않는다**(테스트로 확인).

실측(2링크 팔): shoulder peak 27.0429 / continuous 11.0800 N·m → **2.44배**, elbow 12.2135 / 4.8823 → **2.50배**.

**정직하게 읽어야 할 결과 하나**: "dynamic share"가 **음수**로 나오는 케이스가 있다(LC7에서 −2.7%). 오류가 아니라, 최악 중력 자세에서 지령 가속이 중력을 일부 **상쇄**해 총 토크가 유지 토크보다 작아지는 것이다. 가속이 항상 더해지지는 않는다.

**모델하지 않는 것**: 마찰·백래시·관절 컴플라이언스 **전부 0**. 항은 인터페이스가 나중에 바뀌지 않도록 자리만 두었다. 실제 값에는 관절별 breakaway 토크·점성계수·감속기 효율이 필요한데 이 프로젝트엔 없고, **지어내면 모터를 고르는 근거 토크에 조작된 숫자가 들어간다.** 관절 유연성·제어 동역학도 없다. 강체와 이상 관절이다.
**이 단계는 필요 토크와 파워를 낼 뿐 모터·감속기를 선정하지 않는다.** 그건 효율·열·관성 정합 데이터가 필요하며 Phase 12다. 여전히 SIMULATED.

## Phase 12: 모터·감속기 선정 (구현 완료)
Phase 11의 하중케이스 토크·파워를 받아 관절별 구동계를 고른다.

**카탈로그는 실제 부품이 아니다.** 전 항목이 `illustrative` 태그의 일반 아키타입이고 source는 "representative archetype, replace with vendor datasheet"다. **벤더 부품번호를 지어내지 않았다.** 가짜 카탈로그는 나중에 출처가 있는 것처럼 읽히며, 이는 재료 DB가 전부 `reference_typical`로 태그해 피하는 것과 같은 실패다. **선정 로직이 deliverable이고 DB가 아니다.**
아키타입의 내부 일관성은 테스트로 강제한다: 연속토크×정격속도가 각 등급의 공칭 출력을 재현하고, peak는 연속의 약 3배이며, planetary 효율은 ratio가 오를수록(단수가 늘수록) 떨어진다.

**관계식**
```
출력토크   = 모터토크 × ratio × 효율
모터속도   = 관절속도 × ratio
반사부하관성 = J_load / ratio²        (모터축 기준)
출력축 총관성 = (J_rotor + J_gearbox) × ratio² + J_load
```
**높은 ratio는 토크를 사고 속도를 판다.** 이 트레이드가 선택을 자명하지 않게 만들며, 토크는 여유롭게 만족하는 조합이 속도 검사에서 떨어지는 이유다(테스트로 확인: 관절 20 rad/s에 ratio 160이면 모터 3200 rad/s 필요).

**연속과 피크를 둘 다 본다**
peak를 만족해도 연속 부하를 유지하면 과열하고, 연속으로 고르면 첫 가속에서 멈춘다. **정확히 그 두 케이스를 구성해 각각 거부되는지 확인**했다. 하나만 봤다면 통과시켰을 조합들이다. 감속기는 모터와 무관한 자체 정격이 있어서 **큰 모터가 작은 감속기를 구제하지 못한다**는 것도 테스트로 고정했다.

**불가능하면 불가능하다고 한다**: 충족 조합이 없으면 선정 결과로 None을 반환하고, 어떤 검사가 몇 개 조합에서 몇 배 부족했는지 보고한다. **가장 덜 나쁜 것을 선정인 척 돌려주지 않는다.**

**대안 비교**: 관절마다 상위 3개를 질량·백래시·효율·반사관성 트레이드와 함께 근거를 붙여 제시한다. 순위 기준은 총질량(가벼운 순)인데, 직렬 팔에서는 한 관절의 1 kg을 그보다 안쪽 모든 관절이 짊어지므로 질량이 누적되는 비용이기 때문이다.

**2링크 팔 실측 선정**
| 관절 | 필요(연속/피크) | 선정 | ratio | 질량 | 제한 검사 | 여유 |
|---|---|---|---|---|---|---|
| shoulder | 11.080 / 27.043 N·m | bldc_100w + planetary_50 | 50 | 1.10 kg | 연속토크 | 1.23x |
| elbow | 4.882 / 12.214 N·m | bldc_50w + planetary_50 | 50 | 0.90 kg | 연속토크 | 1.39x |
반사관성비(부하/로터)는 shoulder 0.38, elbow 0.07로 정합 양호. 대안으로 harmonic 조합(백래시 1' vs 15', +0.10 kg)이 제시되며 정밀도가 필요하면 그쪽을 고를 근거가 표에 남는다. 백래시 2' 요구를 걸면 planetary 계열이 전부 배제되고 harmonic만 남는 것도 테스트로 확인.

**하지 않는 것**: 열 검사는 **연속토크 프록시**다(실제로는 duty 프로파일·주위온도·열저항 필요). 그래서 상태를 "PASS subject to thermal validation"으로 표기한다. 효율·백래시는 속도·부하·온도에 대한 곡선이 아니라 대표값 하나다. 마찰·관절 컴플라이언스는 Phase 11에서 이어받아 여전히 0. 비용·납기·장착 인터페이스·제어기 정합도 없다.
**1차 스크리닝이지 최종 부품 결정이 아니다.** 여전히 SIMULATED.

## Phase 13: 토폴로지 최적화 (구현 완료)
단면 파라미터가 아니라 **밀도장** 위에서 자유 형상을 최적화한다. Phase 7 구조격자와 matrix-free GPU FEM을 그대로 재사용하고, 커널에 요소별 강성 배율을 추가했다(강성이 E에 선형이라 공유 Ke 하나로 충분해 matrix-free 구조가 유지된다).

**정식화**: E_e = E_min + x_e³(E0 − E_min), 컴플라이언스 최소화, 체적분율 제약, OC 업데이트(체적 라그랑주 승수 이분탐색 + move limit), 민감도 필터.
E_min은 장식이 아니다. 진짜 0이면 K가 특이해져 해가 **실패**하지 물렁한 영역이 되지 않는다. p=3은 회색을 손해로 만든다(밀도 절반이 강성은 8분의 1).

**🔑 민감도 검증(관문)**: 자기수반 공식 dc/dx_e = −p·x^(p−1)·(E0−E_min)/E0·u_e^T Ke0 u_e 를 **실제 목적함수의 유한차분**과 요소별로 대조. **최악 상대오차 2.6e-05.** 옵티마이저가 하는 모든 일이 이 미분에서 나오므로, 틀리면 아무것도 최적화하지 않은 그럴듯한 형상이 나온다.
부수 검증으로 컴플라이언스를 두 경로(F·U vs Σ scale·u^T Ke0 u)로 계산해 **1.9e-14** 일치.

**솔리드 회귀 브리지**: x=1 전체에서 스케일 경로가 Phase 7 순수 FEM과 **1.4e-14** 일치. 밀도 경로가 검증된 경로에서 이탈하지 않았음.

**체적 제약**: 매 이터레이션 정확히 목표(0.400000) 유지.

**필터 효과를 측정했다**: 면 이웃 간 평균 밀도차를 체커보드 지표로 정의해, 필터 없음 **0.365** → 필터 있음 **0.144**. 주장이 아니라 수치.

**하중경로 재발견**: 단면 높이 방향 밀도가 하단 0.64·0.67, 중앙 0.24~0.26, 상단 0.41~0.53으로, **굽힘이 요구하는 대로 중립축에서 먼 곳에 재료가 몰린다.** 길이 방향으로는 모멘트가 큰 근부에 집중. 아무도 알려주지 않은 물리를 옵티마이저가 스스로 찾아낸 것이다.

**⚠️ 수렴 판정의 함정을 발견해 고쳤다**: 교과서 기준인 "최대 밀도 변화 < tol"만 쓰면 오도된다. damping을 키우면 매 스텝이 작아져 기준이 일찍 충족되고, **명백히 나쁜 설계에서 수렴했다고 보고한다.** 실측: damping 0.3은 14 이터레이션에 컴플라이언스 1.78e-2에서 멈췄고, 더 부드러운 스텝은 1.01e-2까지 갔다. **설계가 수렴한 게 아니라 스텝 크기가 수렴한 것.** 그래서 밀도 변화와 **목적함수 정체**(최근 5회 상대변화 < 5e-4)를 **둘 다** 요구하도록 바꿨다. 임계값은 특정 실행이 성공으로 보이게 맞춘 게 아니며, 그보다 빠르게 개선 중인 실행은 정직하게 converged=False로 보고한다.

최종 실행: 131 이터레이션 수렴, 컴플라이언스 **6.05e-2 → 1.02e-2 (0.169배)**, 체적 0.400000.

**출력의 한계를 명시한다**
- **SIMP는 회색을 남긴다.** 40% 존재하는 재료는 없으므로 임계처리가 필요하고, 임계처리한 형상은 최적화한 밀도장과 다르다. 그래서 grey fraction(0.1~0.9 구간 비율)을 함께 보고한다.
- **STL이지 깨끗한 STEP이 아니다.** Phase 9 경계와 일관되게, 밀도장에서 해석적 면을 복원하는 것은 표면 재구성이다. 출력은 복셀 표면이고 구조적으로 각졌으며, 부피는 복셀 수에서 정확히 나온다.
- **watertight가 아닌 경우가 많고 그건 버그가 아니다.** 모서리로만 맞닿은 복셀은 비다양체 모서리를 남긴다. 그런 식으로만 붙은 재료는 하중을 못 나르므로 **면 연결 성분 필터로 제거**하지만, 연결된 본체 내부의 대각 접촉은 여전히 남는다. 그 경우 부피는 복셀 수 기준으로 정확히 보고한다.
- **컴플라이언스 최소화이지 응력 제약이 아니다.** 응력에 대해 아무 말도 하지 않는다. 응력 기반 토폴로지는 훨씬 어렵고 후속이다.
토폴로지 결과는 **설계 개념이지 검증된 부품이 아니다.** 3D FEM 관문을 통과하고 제조 피처를 얻어야 한다. 여전히 SIMULATED.

## Phase 14a: 응력제약 토폴로지 최적화 (구현 완료)
Phase 13은 응력에 대해 아무 말도 하지 않았다. 여기서 그 구멍을 메운다. 벤치마크는 **L-브래킷**이다. 정사각형에서 우상단 사분면을 도려내 **재진입 모서리**를 만들면 선형탄성이 그 자리에 응력집중을 놓는다. 컴플라이언스 목적함수가 신경 쓸 이유가 없는 특징이고, Phase 7이 외팔보 고정단에서 찾아낸 것과 같은 물리다.

**정식화**: qp 완화(강성 지수 p=3, 응력 지수 q=2.5)로 요소 응력을 σ_e = x_e^q · σ_solid(u)로 정의하고, P-norm으로 집계해 제약으로 건다. 민감도는 수반법으로 구한다. 명시항(x^q 인자를 통한 경로)과 암시항(변위를 통한 경로, 수반해가 필요)을 나눠 계산한다.

**🔑 수반 민감도 검증(관문)**: d(σ_PN)/dx를 유한차분과 요소별로 대조. **최악 오차 3.9e-08**(최대 민감도로 스케일). 옵티마이저가 하는 모든 일이 이 미분에서 나온다.
처음에는 상대오차가 최대 60%로 나와 틀린 줄 알았다. 원인은 기울기가 아니라 **차분이 솔버 잡음에 잠긴 것**이었다. CG 잔차 1e-8에서는 0 근처 민감도 성분의 차분몫이 수렴 정밀도에 지배된다. 1e-12로 조이자 3.9e-08이 나왔다. 그래서 판정 기준을 각 성분의 상대오차가 아니라 **최대 민감도로 스케일한 오차**로 잡는다. 0에 가까운 성분을 자기 크기로 재면 옳은 기울기가 탈락한다.

**⚠️ P-norm의 방향을 실측했고, 예상과 반대였다**: 여기서 쓰는 **합 형태** P-norm은 참 최댓값을 **과소평가하지 않고 과대평가**한다. 모든 요소에 대한 합이므로 최대항보다 클 수밖에 없다. L-브래킷 실측 비율은 P=2에서 **3.92**, P=32에서 **1.009**로 **위에서** 참값에 접근한다.
이 방향이 중요한 이유는 **안전한 쪽**이기 때문이다. 집계 제약을 만족하면 요소별 실제 한계도 만족한다. 더 익숙한 평균 형태 P-norm은 반대로 최댓값을 과소평가하고, 그건 과응력 부품을 내보내는 방향이다. 어느 쪽인지 테스트로 못박았다.

**완화의 효과를 대조로 확인했다**: 최고응력 요소 하나를 밀도 1.0에서 1e-3까지 비워가며 두 지표를 같이 측정했다.
- **완화 없는(미시) 응력은 사라지지 않는다.** 10.871 MPa에서 오히려 **올라가 25.655 MPa에 정착**한다. 강성이 떨어지는 만큼 변형률이 커지기 때문이다. 이 양에 제약을 걸면 **재료를 빼야 할 바로 그 자리에서 빼는 것이 금지된다.** 이것이 특이점이다.
- **완화된 응력은 0으로 간다.** 10.871 MPa에서 1e-6 MPa 이하로 단조 감소한다. 제거가 허용된다.

**L-브래킷 대비(같은 체적 0.4, 20x20x2, 한계 16 MPa)**
| 설계 | 컴플라이언스 | 최대응력 | p-norm | 모서리 밀도 | grey |
|---|---|---|---|---|---|
| 컴플라이언스 최소화 | 5.17e-02 | 17.92 MPa (위반) | 1.354 | 0.799 | 0.590 |
| 응력제약 | 7.51e-02 | 12.62 MPa | 0.997 | 0.711 | 0.797 |

응력제약은 최고응력을 내리고 재진입 모서리에서 재료를 걷어내며, 그 대가를 **컴플라이언스 +46%**로 치른다. 세 가지가 동시에 성립해야 제약이 제 일을 한 것이다.

**❌ 기대했던 "모서리가 필렛처럼 둥글어진다"는 결과는 얻지 못했다.** 정직하게 적는다. 응력제약 설계는 최고응력을 30% 낮추지만 형상은 깨끗한 필렛이 아니라 **더 회색**이다(grey 0.590에서 0.797로). 밀도장 렌더링에서 컴플라이언스 설계는 뚜렷한 구멍을 갖는 반면 응력제약 설계는 하부 팔에 공동이 거의 없다. 여기서 제조 가능한 형상을 읽어내려면 투영(projection) 기법이 필요하다. **Phase 14.5-A에서 구현했고, 이 회색 결과가 부분적으로 완화항을 이용한 수치적 탈출구였음이 드러났다**(흑백으로 강제하면 모서리 거동이 반대로 뒤집힌다). 아래 Phase 14.5-A 참조.

**❌ 체적최소화 정식화는 시도했다가 버렸다.** "체적 최소화 + 응력제약 벌점"은 여기서 **퇴화한다**. 체적 기울기가 상수 1/n이라 정규화한 스텝이 모든 요소를 같은 양만큼 움직이고, 밀도장은 형상을 만들지 못한 채 균일하게 줄어든다. 50 이터레이션 실행이 **모든 요소가 정확히 0.0500인 장**을 돌려줬다. 그건 구조가 아니다. 되살리려면 MMA 같은 진짜 제약 스텝이 필요하다. 그래서 채택한 정식화는 **고정 체적에서 컴플라이언스를 최소화하며 응력을 제약**하는 쪽이다. 컴플라이언스 목적이 흑백을 만들어 주고, 응력항은 제약이 실제로 무는 곳(재진입 모서리)에서만 설계를 바꾼다.

**벌점 방식의 한계를 보고한다**
- **벌점법은 제약 경계에서 진동하므로 마지막 반복이 살짝 밖에 있을 수 있다.** 17 MPa 실행은 p-norm 1.0036으로 끝났다. 그래서 `best_feasible_density`(허용역 안에서 컴플라이언스가 가장 낮은 반복)를 따로 들고 있고, 지어야 할 설계는 그쪽이다. `feasible`은 마지막 반복이 어디서 멈췄는지만 말한다.
- **벌점 스케줄은 설계 품질과 수렴 속도를 맞바꾼다.** 센 스케줄은 빨리 허용역에 들어가지만 사실상 응력 기울기만 보고 움직여 회색 설계를 남긴다(16 MPa 한계에서 가중치 10·성장률 1.3은 컴플라이언스 +212%에 grey 0.996, 기본값인 부드러운 스케줄은 +46%에 grey 0.797). 기본값은 품질을 택했고, 빠른 허용점만 필요한 호출자는 센 스케줄을 명시적으로 넘긴다.
- **한계가 너무 빡세면 도달하지 못한다.** 20x20x2에서 15 MPa는 컴플라이언스를 +227%로 밀어올리고 설계를 사실상 전부 회색으로 만든다. 10x10x1에서는 20% 감소가 아예 도달 불가이고, 벌점을 아무리 키워도 p-norm이 1.10 근처에서 정체한다. 그건 제약이 아니라 격자 해상도를 시험하는 것이다.

CLI는 `dae topology --stress`로 이 대비를 그대로 재현한다. 결과는 여전히 **SIMULATED**이고 설계 개념이지 검증된 부품이 아니다.

## Phase 14b: Method Registry와 전략 선택 (구현 완료)
전략 선택을 세 이름에 대한 하드코딩 switch로 짓지 않았다. **방법을 데이터로 등록하고 그 메타데이터 위에서 라우팅한다.** 이유는 단순하다. reasoner의 지능은 방법 라이브러리의 넓이와 정확성에 상한이 걸린다. 방법을 추가하는 일이 selector를 고치는 일이 되면 라이브러리는 넓어지지 않는다.

**등록 항목이 선언하는 것**: name, category(design_generation / analysis / optimization / selection), inputs/outputs, **유효범위(applicability)**, fidelity, cost, 그리고 **implementation**(실제 코드 경로).

**🔑 유효범위가 이 설계의 존재이유다.** Phase 7이 그 실증이다. 값싼 Euler-Bernoulli 모델을 세장비 약 6인 링크에 썼고, 전단을 빼먹은 그 모델의 맹점 위에 최적화기가 정확히 올라앉았으며, 3D FEM 관문이 그 결과를 탈락시켰다. **코드에 버그는 없었다.** 모델이 유효범위 밖에서 쓰였을 뿐이고, 시스템의 어느 부분도 그걸 지적할 위치에 있지 않았다.
이제는 registry가 자동으로 막는다. 세장비 6짜리 문제로 해석 방법을 조회하면 `beam_eb`는 후보에서 **빠지고**, 빠진 이유가 함께 나온다.

```
$ dae methods --geometry prismatic_beam --slenderness 6 --category analysis
applicable: fem3d, beam_timoshenko, surrogate_screen
excluded:
  beam_eb   slenderness L/h is at least 12
            (shear is omitted; measured error is 2.5% at L/h 6 and 6.2% at L/h 4)
```

**임계값은 교과서 통설이 아니라 이 프로젝트의 실측이다.** Phase 7.5의 3D FEM 대조표에서 EB/FEM은 L/h=4에서 0.9382, 6에서 0.9750, 12에서 0.9975다. 즉 L/h=6에서 2.5%, 4에서 6.2%를 잃는다. 그래서 EB의 문턱을 12로 둔다. Timoshenko는 측정 구간 4~20 전체에서 0.5% 안에 들므로 문턱이 4이고, **4 미만은 측정한 적이 없으므로 유효하다고 주장하지 않는다.**

**말하지 않은 것은 허락이 아니다.** 조건이 요구하는 문제 특성을 호출자가 명시하지 않으면 조건은 통과가 아니라 **실패**한다. 세장비를 아무도 적지 않은 문제야말로 보 이론을 유효하다고 가정하면 안 되는 경우다. 이때의 배제 사유는 "범위 밖"이 아니라 "명시되지 않음"으로 따로 보고된다. 고칠 방법이 다르기 때문이다(하나는 문제를 특성화해야 하고, 하나는 다른 방법을 써야 한다).

**등록된 것은 실제로 구현된 것뿐이다.** registry 항목은 "이 방법이 존재하고 쓸 수 있다"는 주장이고 selector는 찾은 것으로 라우팅한다. 스텁을 등록하는 것은 **가짜 부품번호와 같은 종류의 실패**다. 기록처럼 읽히는데 기록이 아니다. 이 트리에서 `physics/thermal.py`, `physics/collision.py`, `physics/rigid_body.py`, `optimization/bayesian.py`는 한 줄짜리 스텁이라 **의도적으로 빠져 있다.** MMA도 마찬가지다. 응력제약 문제에 가장 어울리는 최적화기지만 여기 구현되어 있지 않다. 테스트가 모든 등록 항목의 `implementation`을 임포트해 공개 정의가 있는지 구조적으로 확인한다.

**전략 선택 루프**: selector가 registry에서 design_generation 후보를 받아 **싼 것부터, 같으면 충실도 높은 순, 같으면 이름 순**으로 사다리를 만든다. 정체(기본 3회 비개선)하면 한 칸 올라간다. 마지막 칸에서 정체하면 순환하지 않고 **소진되었다고 보고한다**. 이름 타이브레이크는 장식이 아니다. 그게 없으면 사다리가 dict 순서에 의존해 실행이 재현되지 않는다.
selector는 `parametric_section`이나 `topology_stress`를 **언급하지 않는다.** 문제에 적용 가능한 design_generation 방법이 무엇인지 물을 뿐이다. 방법을 하나 등록하면 이 파일을 고치지 않고도 사다리에 들어온다.

**여전히 휴리스틱이다. LLM이 아니다.** 확장점은 명확하다. LLM reasoner를 꽂으면 정렬 규칙이 바뀔 뿐 **같은 registry를 라우팅**한다.

**⚠️ 라우팅한 것과 실행한 것을 구분해 기록한다.** Phase 4 루프 엔진은 파라메트릭 단면 설계를 평가한다. 토폴로지 방법은 아직 실행하지 못한다. selector가 `topology_compliance`로 올라갔을 때 에피소드의 `strategy_used`에 그 이름을 찍으면 **그 설계는 그 방법이 만든 게 아니므로 로그가 거짓 기록이 된다.** 그래서 실행 가능한 방법만 찍고, 권고는 hypothesis에 적고 `unmet_recommendations`로 센다. registry가 라우팅할 수 있는 범위와 루프가 실행할 수 있는 범위의 간극이 **침묵하는 한계가 아니라 보이는 숫자**가 된다.

**Brain 전략기억**: 어떤 문제류에서 어떤 registry 방법이 이겼는지 일반화한다. **런 단위로** 승자를 센다(에피소드 단위로 세면 단지 더 많이 시도된 전략이 더 나아 보인다). 일관된 승자가 임계치(0.7) 미만이면 **아무것도 승격하지 않는다.** 추천할 일관된 수가 없을 때 지어내는 것이 이 사다리가 막으려는 실패 그 자체다.
증거수준은 주장이 아니라 기존 사다리에서 **유도**된다. 단일 런이면 SIMULATED, 독립 런이 쌓이면 REPEATED까지 오른다. **EXPERIMENTALLY_VALIDATED에는 영원히 도달하지 못한다.** 그 관문은 물리시험 증거로만 열리고 이 경로는 그런 증거를 만들 수 없다. "방법 A가 B보다 낫다"는 진술은 모델에 대한 것이지 세계에 대한 것이 아니다.

## Phase 14.5-A: 흑백 토폴로지 (Heaviside 투영) (구현 완료)
Phase 13은 grey fraction 0.59, Phase 14a는 0.80을 남겼다. 밀도 0.4인 요소는 아무도 만들 수 없는 재료이고, 사후 임계처리로 얻은 형상은 옵티마이저가 평가한 적 없는 형상이다. **3-필드 정식화**로 옵티마이저 자신이 흑백 설계를 내게 한다.

    설계변수 x  --밀도필터-->  x_tilde  --투영-->  x_bar  -->  강성

투영은 tanh 형태의 매끄러운 Heaviside(임계 eta=0.5, 선명도 beta)다. **beta는 반드시 낮게 시작해 올려야 한다.** 처음부터 날카로우면 거의 모든 곳에서 미분이 0이라 옵티마이저가 움직이지 못한다. 그게 연속화의 존재이유다.

**🔑 민감도 연쇄 재검증(관문)**: Phase 13·14a에서 검증한 수반해는 이제 **x_bar에 대한** 미분이지 설계변수에 대한 미분이 아니다. 설계변수까지 되돌리는 연쇄 `H^T (dx_bar/dx_tilde * dc/dx_bar)`를 유한차분으로 다시 검증했다. **최악 스케일 오차: beta=1에서 2.8e-07, beta=4에서 1.2e-07, beta=16에서 5.0e-08.** 검증하지 않았다면 다른 문제의 기울기를 내려가면서도 그럴듯한 형상을 냈을 것이다.
필터는 성긴 행렬로 짓는다. 연쇄가 **정확한 전치**를 필요로 하는데, 전치를 두 번째 루프로 손으로 쓰면 순전파와 역전파가 어긋난다. 내적 항등식 `<Hx, y> == <x, H^T y>`를 테스트로 박았다.

**beta가 바뀌면 체적을 복원한다.** beta를 올리면 설계는 그대로인데 사상이 바뀌어 물리 체적이 움직인다. 그냥 두면 move limit 때문에 여러 이터레이션에 걸쳐 돌아오고, 그 사이의 모든 반복이 **잘못된 체적에서 잰 컴플라이언스**를 보고한다. 서로 비교할 수 없는 값이다. 그래서 beta가 바뀐 직후 설계를 균일 이동시켜 물리 체적을 목표로 되돌린다. 설계를 개선하는 스텝이 아니라 새 사상으로 다시 표현하는 것이므로 move limit 대상이 아니다. 시작 설계도 이분탐색으로 목표 체적 위에 올려놓는다.

**결과: 더 검고 동시에 더 뻣뻣하다** (L-bracket 512요소, 체적 0.4, 필터 반경 2요소, 200 이터레이션)
| 설계 | 컴플라이언스 | 최대응력 | grey | connected | 모서리밀도 |
|---|---|---|---|---|---|
| plain SIMP | 5.53e-02 | 17.12 MPa | 0.6172 | 0.989 | 0.7296 |
| 3-필드 | **3.33e-02** | 17.59 MPa | **0.0625** | 1.000 | 0.6509 |
| 3-필드 + 응력제약 | 5.41e-02 | **12.35 MPa** | 0.2969 | 0.982 | 0.8576 |

grey가 **0.617에서 0.0625로 약 10배** 줄었고, **컴플라이언스는 40% 좋아졌다.** 후자는 우연이 아니다. SIMP는 회색을 x^3로 벌하므로, 같은 체적을 흑백으로 쓰면 실제로 더 뻣뻣해진다. 캔틸레버에서도 같은 방향이다(grey 0.4115 -> 0.1094).

**⚠️ grey fraction만 보면 속는다. 설계가 조각날 수 있다.** 이걸 거의 놓칠 뻔했다. 필터 반경 1.5요소에서 투영 설계는 grey 0.109로 훌륭해 보이지만 **고형 재료의 76%만 최대 연결성분에 속한다.** 나머지는 하중경로에 붙어 있지 않아 아무것도 나르지 않는다. 그건 회색 설계보다 나은 설계가 아니라 **부서진 설계**다. 그래서 `connected_fraction`을 도입해 grey와 **함께** 보고한다.
반경을 넓히면 붙는다. 24x8x2 캔틸레버 200 이터레이션 실측:
| 필터 반경 | 컴플라이언스 | grey | connected |
|---|---|---|---|
| plain 1.5 | 1.084e-02 | 0.4115 | 100% |
| 3-필드 1.5 | 9.805e-03 | 0.1094 | **76.1%** |
| 3-필드 2.0 | 1.117e-02 | 0.0833 | 91.6% |
| 3-필드 3.0 | 1.771e-02 | 0.0573 | 100% |
연결성은 반경으로 사고 컴플라이언스로 지불한다. 양쪽 다 테스트로 고정했다.

**⚠️ checkerboard 지표는 흑백 설계에서 올라가고 그게 정상이다.** 이웃 간 밀도차를 재는 지표인데 날카로운 고체-공동 경계에서는 그 차이가 크다. 회색 장 위의 교대 아티팩트를 잡으라고 만든 것이라 진짜 모서리와 구별하지 못한다. **여기서 의미 있는 지표는 연결성이다.**

**❗ 응력제약의 모서리 거동이 Phase 14a와 반대로 뒤집혔고, 이쪽이 물리적인 답이다.**
Phase 14a(회색 허용)에서 응력제약은 모서리 밀도를 **낮췄다**(0.799 -> 0.711). 3-필드(흑백 강제)에서는 오히려 **높인다**(0.651 -> 0.858). 해석: 회색이 허용되면 옵티마이저가 밀도를 낮춰 완화항 x^q로 응력을 줄이는 **수치적 탈출구**를 쓸 수 있다. 흑백으로 강제하면 그 길이 막히므로 **재진입 모서리에 재료를 더 넣어** 물리적으로 해결해야 한다. 재진입 모서리를 두껍게 만드는 것은 필렛이 하는 일 그대로다. 최대응력이 17.59에서 **12.35 MPa로 30% 떨어졌다.**
**그래도 "필렛을 얻었다"고는 쓰지 않는다.** 20x20 격자는 필렛 반경을 표현하기에 거칠다. 정직한 진술은 이것이다. 흑백 응력제약 설계는 **모서리 영역에 재료를 추가하는 방향으로** 응력을 줄이며, 이는 필렛이 작동하는 방향과 같지만 기하학적 반경을 분해한 것은 아니다. 진짜 반경을 보려면 더 고운 메시가 필요하고, 그 크기대에서는 GPU가 이미 유리하다(14.5-C 참조).

**여전히 남는 한계**: 흑백이어도 유기적 형상의 깨끗한 STEP은 표면 재구성 문제다. 출력은 복셀 표면이고 결과는 **SIMULATED**이며 설계 개념이지 검증된 부품이 아니다.

## Phase 14.5-B: 루프가 토폴로지를 실제로 실행한다 (구현 완료)
14b가 남긴 "라우팅 != 실행" 간극을 닫는다. 이전에는 selector가 `topology_compliance`를 골라도 루프는 파라메트릭 솔버를 돌렸고, 권고는 `unmet_recommendations`에 쌓일 뿐이었다. 이제 루프의 설계 단계가 **선택된 method의 실제 구현으로 디스패치**한다.

**설계 결과에 출처를 강제로 붙였다**: `DesignOutcome`은 **정확히 하나**의 표현을 들고 있어야 한다. 파라메트릭이면 설계벡터, 토폴로지면 밀도장. 둘 다 없으면 아무것도 만들지 않은 것이고, 둘 다 있으면 출처가 모호하다. 이 불변식 덕분에 에피소드의 `strategy_used`가 **주장이 아니라 검사 가능한 기록**이 된다. 토폴로지라고 적힌 에피소드는 밀도장을 들고 있어야 하고, 토폴로지 최적화가 실제로 돌지 않으면 밀도장은 존재할 수 없다. 테스트가 이 대조를 확인한다.
에피소드의 `design_genome`도 방법에 맞춰 쓴다. 토폴로지 설계에 `wall_thickness_m`을 적지 않는다. 다른 설계에서 나온 벡터로 그 칸을 채우면 로그가 만들지도 않은 형상을 만들었다고 말하게 된다.

**질량 최소화는 체적분율 탐색으로 한다.** SIMP는 고정 체적에서 컴플라이언스를 최소화할 뿐 질량 목적함수가 없다. 그래서 토폴로지 실행기는 체적분율에서 SIMP를 돌리고, 결과 밀도장을 **문제의 진짜 제약(3D FEM 처짐)** 으로 평가하고, 제약을 만족하는 가장 가벼운 분율을 이분탐색한다. 토폴로지 이터레이션이 비싼 이유가 이것이고, registry가 이 방법들을 HEAVY로 매긴 근거다.

**실행 결과(MVP 링크, 6 에피소드)**
```
 #                        strategy_used           repr   mass kg  feas  best
 0   parametric_section:initial-exploit  design_vector    0.2518  True  True
 1  parametric_section:exploit-jittered  design_vector    0.2518  True False
 2  parametric_section:exploit-jittered  design_vector    0.2518  True False
 3 topology_compliance:explore-on-stall  density_field    2.6344  True False
 4     topology_stress:explore-on-stall  density_field    2.6344  True False
unmet recommendations: []
```
**실행 가능한 전략의 unmet이 0으로 떨어졌다.** 전략 선택이 권고에서 실제 실행으로 승격했다. 메커니즘 자체는 남겨 둔다. registry는 실행기보다 빨리 자라도록 만든 것이고, 다음에 추가되는 방법은 누군가 배선하기 전까지 다시 unmet이 된다.

**⚠️ 토폴로지가 파라메트릭에 크게 졌고, 그게 정직한 결과다.** 0.252 kg 대 2.634 kg, 10배 차이. 원인은 튜닝 부족이 아니다. 파라메트릭 설계는 **얇은 벽 중공 박스**이고 캔틸레버에 대해 대단히 질량효율적인데, 16x6x2 복셀 격자는 높이 방향으로 6요소뿐이라 **그 얇은 벽을 표현할 수 없다.** 루프는 파라메트릭을 best로 올바르게 유지한다.
`topology_compliance`와 `topology_stress`가 **같은 질량**(2.6344 kg)을 낸 것도 확인했다. 밀도장은 실제로 다르고(최대 차이 0.215) 두 최적화가 각각 돌았지만, 질량은 체적분율만으로 정해지고 두 이분탐색이 같은 분율(0.1875)에 도달했다. 이 문제에서 응력제약은 두 자릿수만큼 헐겁다(최대 응력 0.943 MPa 대 허용 120 MPa). 제약이 물지 않으면 벌점항이 0이라 응력 경로가 컴플라이언스 경로로 환원되는 것이 정상 동작이다.

## Phase 14.5-C: dof 기준 디바이스 선택 (구현 완료)
"연산이 느리다"의 원인을 실측했다. **CPU 폴백이 아니었다.** Warp는 `cuda:0`을 쓰고 있었다. 문제는 **우리 메시가 GPU 손익분기점보다 작다**는 것이었다.

워밍업 후, 두 디바이스에서 CG 반복수 동일:
| dofs | GPU ms | CPU ms | GPU/CPU |
|---|---|---|---|
| 1,071 | 43.5 | 23.2 | **1.88 (GPU 느림)** |
| 3,267 | 86.6 | 65.4 | **1.32 (GPU 느림)** |
| 9,996 | 142.9 | 246.9 | 0.58 |
| 24,375 | 396.5 | 1410.3 | 0.28 |
| 45,198 | 715.5 | 3435.7 | **0.21 (GPU 4.8배)** |

손익분기점은 약 10,000 dofs다. 이유는 둘이다. 작은 solve는 **커널 런치 오버헤드**가 지배하고(토폴로지 실행 중 SM 점유율 40%, 14W/35W), 이 소비자용 부품은 **fp64를 fp32의 약 1/64 속도**로 돈다. 솔버는 float64를 쓰는데, 이건 Phase 7에서 세장 보의 조건수 때문에 float32 CG가 정체해서 택한 것이라 낮출 수 없다.

그래서 `solve_linear_elasticity`가 dof 수로 디바이스를 고른다. 10,000 미만이면 CPU, 이상이면 cuda:0. **명시적으로 넘긴 device는 절대 덮어쓰지 않는다.**
**관문은 정확도 중립이다.** 속도를 위해 디바이스를 고르는 것은 답을 바꿀 수 없을 때만 허용된다. 같은 문제를 두 디바이스에서 풀어 변위와 응력이 **상대 1e-6 이내**로 일치함을 테스트로 고정했다. 같은 커널, 같은 float64, 같은 알고리즘, 같은 반복수이고 부동소수 축약 순서만 다르다.
실측 효과: L-bracket SIMP 60 이터레이션이 **14.6초에서 11.0초로**(이터레이션당 244 ms에서 183 ms), 예측한 1.32배와 일치.

**성능 로드맵(순서만 기록, 지금 구현하지 않음)**
1. (완료) dof 기준 디바이스 선택.
2. **독립 solve 배칭**: 체적분율 이분탐색의 solve들은 서로 독립이므로 한 번에 묶어 런치 오버헤드를 상각한다. 현재 병목이 산술이 아니라 런치이므로 여기가 다음 이득이다.
3. **혼합정밀도(fp32 CG + fp64 잔차보정)**: 메시가 커진 뒤에 한다. 지금 크기에서는 SM 점유율이 40%라 **산술 바운드가 아니어서 효과가 작다.** 고운 메시로 진짜 필렛을 볼 때는 GPU가 이미 4.8배 빠르고, 그 영역에서 제 값을 한다. 정확도는 깎지 않고 잔차보정으로 fp64를 유지하는 형태여야 한다.

## Phase 15: MCP 노드 아키텍처와 분산 capability registry (구현 완료)
지금까지 노드 간 통신은 Claude 세션이 수동으로 중계했다. 이걸 정식 프로토콜 경계로 바꾼다. **새 프로토콜을 발명하지 않고 MCP를 쓴다.** 퓨전이 이미 MCP로 붙는다는 사실과 정합하고, 표준을 하나 더 만드는 것은 그 자체가 부채다.

**14b registry를 대체하지 않고 확장한다.** capability = 등록된 method + 그것을 제공하는 노드. 라우터의 규칙은 하나다: **method가 문제에 적용 가능하고 AND 제공 노드가 사용 가능하면** 후보다. 이 프로세스 안이든, stdio로 붙은 자식 프로세스든, HTTP 너머 다른 기계든 판단에 들어오지 않는다.
이 통일성이 핵심이다. 경로가 둘로 갈리면 로컬을 정직하게 지켜 주는 유효범위 선언이 원격을 덮지 못한다. 세장비 6짜리 문제에서 `beam_eb`가 배제되는 것은 그것이 노드 capability가 된 뒤에도 그대로다.
배제 사유는 **둘 다** 보고한다. 범위 밖이면서 노드도 못 쓰는 capability는 이유를 두 개 단다. 노드를 살렸는데 여전히 무효인 후보가 조용히 살아나면 안 된다.

**사용 불가는 발견하는 게 아니라 선언한다.** 유료 권한이 없는 노드는 미리 그렇게 말하고, 라우터는 보 이론을 세장비로 배제하듯 **사유를 붙여** 배제한다. 호출해 보고 에러로 알게 되는 것은 그보다 나쁘고, **그럴듯한 숫자를 받아서 알게 되는 것은 최악이다.** 사용 불가 노드는 명세에서 빠지지 않고 사유와 함께 남는다. 안 만든 capability와 막힌 capability는 대응이 다르기 때문이다.

**노드 로스터**
- **Daedalus 엔진 노드** (in-process, MCP 서버로 노출 가능): `list_capabilities`, `route`, `evaluate_beam_section`.
- **추론 노드**: 계약만. 지금 추론은 CLI를 모는 운영자 세션이 담당하며, 이는 구조적으로는 노드지만 프로토콜상으로는 아니다. 세션을 감싸는 서버를 쓰는 것은 **반대편에 아무것도 없는 경계를 발명하는 일**이라 하지 않았다. 로컬 모델로 바꾸는 것이 라우팅 재설계가 아니라 이 capability 제공이 되도록 계약만 선언해 둔다.
- **퓨전 검증 노드**: 계약·워크플로우 완비, 구현 비움(아래).

**🔑 MCP 왕복 실증**: 엔진의 보 해석을 MCP 클라이언트에서 MCP 서버로 실제 호출하고, 결과가 in-process 호출과 **상대 1e-12로 일치**함을 테스트로 고정했다. 래퍼가 아니라 진짜 프로토콜 경계다. 전송이 결과를 조용히 바꾸면 전송이 없는 편이 낫다. `route`도 왕복시켜 **배제 사유까지 함께 건너오는지** 확인한다. 무엇을 쓸 수 있는지만 받은 호출자는 "없는 것"과 "배제된 것"을 구별할 수 없다.

**퓨전 검증 노드: 워크플로우만, 구현은 비움**
퓨전은 유료이고 권한을 아직 사지 않았다(두 달 뒤쯤 결제 예정). 호출 자체를 뺀 **전부**가 여기 있다. capability 선언, 요청·보고 계약, registry 등록 자리, 설계가 이 노드로 흘러가 결과가 되돌아오는 경로. 나중에 함수 본문 하나를 MCP 호출로 채우면 되고 주변은 움직이지 않는다.
**🔑 stub은 절대 숫자를 내지 않는다. 이것이 이 단계의 정직성 관문이다.** 0도, 빈 보고서도, 부분 결과도 아니다. 셋 다 파이프라인에서는 **실측과 구별되지 않는다.** `CapabilityUnavailable`을 던지고, **어떤 인자로도 결과가 나오지 않음**을 테스트로 고정했다. 반환 타입 주석은 계약이고, 노드가 사용 불가인 동안 의도적으로 한 번도 지켜지지 않는다.
`FusionVerificationReport`는 정의만 되어 있고 **이 모듈은 그것을 한 번도 생성하지 않는다.** 소비 코드를 고정된 계약에 대고 미리 쓰고 검토할 수 있게 하기 위한 것이다.

**검증 없는 설계는 그렇게 표시된다.** `VerificationStatus`가 불변식을 강제한다. 보고서 없이 외부검증을 주장할 수 없고, 외부검증이 아니라면서 보고서를 들고 있을 수도 없다(미검증 숫자가 검증된 것처럼 흘러다니게 된다). 검증이 없으면 **이유를 반드시 적어야 한다.** 지금 모든 설계는 `self_fem_only`, 즉 **우리 자체 FEM으로만 검증됨**이고 그 사실이 에피소드와 Brain에 사유와 함께 기록된다.

`mcp`는 선택적 의존성이다(`requirements-nodes.txt`). 해석 스택은 전부 in-process로 돌아 이것을 필요로 하지 않는다. CLI는 `dae nodes`로 로스터와 capability, 그리고 배제 사유를 보여준다.

**앞으로 채울 자리**: 권한 결제 후 `nodes/fusion_node.py`의 `verify` 본문에 실제 MCP 호출을 넣고, 노드를 `available=True`로 선언하면 라우터가 자동으로 후보에 넣는다. 14.5-A가 남긴 회색·복셀 표면 문제의 계획된 해법이 이 노드의 **클린 B-rep 지오메트리**다.

## Phase 16: 피로(S-N, Goodman)와 좌굴(오일러) (구현 완료)
method 라이브러리 확장의 첫 걸음. 이 둘을 고른 이유는 **PASS의 정직성을 직접 바꾸기 때문**이다. 지금까지 설계는 최대응력이 항복 아래이고 처짐이 한계 아래면 통과했다. 그건 **하중을 한 번 걸었을 때**에 대한 진술이다. 백만 번 걸었다 뺐다 하는 부품이나, 세장한 부재에 압축이 걸리는 부품은 그 두 검사가 볼 수 없는 파괴모드를 갖는다.

### 16-A: 고주기 피로
**🔑 Goodman 손검증**: Se=159, Su=572 MPa, σ_a=50, σ_m=70 MPa에서
`50/159 + 70/572 = 0.314465 + 0.122378 = 0.436843`, `n = 1/0.436843 = 2.28914`.
코드가 상대 1e-12로 일치한다. Soderberg(평균을 항복에 대고 계산)는 항상 더 보수적임도 확인했다.

**압축 평균응력은 크레딧을 주지 않는다.** Goodman·Soderberg 선은 인장 평균응력에 대해 유도된 것이고, 음의 σ_m으로 연장하면 **완전교번보다 높은 안전율**을 뱉는다. 그건 틀려도 되는 방향이 아니다. σ_m ≤ 0이면 평균항을 버리고 `Se/σ_a`로 간다. 어느 쪽이 일어났는지 `mean_stress_charged`로 기록한다.

**⚠️ 알루미늄·마그네슘에 무한수명을 주지 않는다.** 진짜 내구한도는 S-N 곡선이 평평해지는 재료(탄소강·저합금강)에만 있다. 알루미늄 곡선은 계속 내려가므로 DB의 `fatigue_strength_pa`는 **약 5e8 사이클에서의 응력**이지 무한수명 보장이 아니다(이건 `data/materials.yaml`이 이미 적어 둔 사실이고 여기서 코드로 강제한다).
내구한도 보유 목록은 **[ASSUMED]** 이고 `steel_s45c`, `steel_scm440`만 넣었다. **오스테나이트 스테인리스(ss_316)와 티타늄(ti_6al_4v)은 문헌이 갈려서 일부러 뺐다.** 목록에 없는 id는 내구한도 없음으로 처리한다. **모르는 재료가 조용히 무한수명을 주장하는 일이 없도록** 하는 보수적 기본값이다.

**압축-압축 사이클도 검사한다.** 테스트를 쓰다가 발견한 것이다. 0에서 -50 MPa를 오가는 하중은 인장 피크가 없지만 **교번응력이 25 MPa로 실재한다.** "인장이 없으면 피로 없음"으로 처리하면 이걸 건너뛰는데, 압축-압축 반복도 균열을 키운다. 진짜로 검사가 없는 경우는 **변동이 아예 없는** 경우뿐이다.

### 16-B: 좌굴
**🔑 오일러 손검증**: E=200 GPa, I=1e-8 m⁴, L=2 m, K=1에서
`P_cr = π²·200e9·1e-8/4 = 4934.8022 N`, 코드가 상대 1e-15로 일치.
유효길이계수는 **이론값**을 쓴다(고정-자유 2.0, 핀-핀 1.0, 고정-핀 0.699, 고정-고정 0.5). P_cr이 1/K²로 스케일하는 것도 확인했다(고정-자유는 핀-핀의 1/4). 설계기준은 실제 단부 구속이 이상적이지 않다는 이유로 더 높은 값을 권고하며(AISC는 고정-고정 0.65, 고정-핀 0.80), 그 값이 필요한 호출자는 `k_factor`를 직접 넘긴다.

**약축이 결정한다.** 기둥은 단면 2차모멘트가 작은 축으로 좌굴하므로 `min(I_x, I_y)`를 쓴다. 강축 값을 쓰면 임계하중을 두 값의 비만큼 과대평가한다.

**임계세장비로 오일러 유효성을 판정한다.** `C_c = sqrt(2π²E/Sy)`, 오일러 응력이 항복의 절반이 되는 지점이다. 7075-T6에서 **C_c = 53.0**. 세장비가 그 아래면 탄성 임계하중에 도달하기 전에 항복이 시작되므로 **오일러는 과대예측이고 `euler_valid=False`로 보고한다.**

**⚠️ 무효한 오일러 값이 지배 마진이 되지 못하게 막았다.** 이걸 통합하다가 잡았다. 짧은 기둥(L=0.2 m, λ=25.7 < 53.0)에서 오일러 SF가 46.4로 나오는데, 그게 후보에 끼면 **과대예측이 구속 마진 행세를 한다.** 유효성 플래그를 만든 이유가 바로 그건데 무의미해진다. 유효한 오일러 결과만 지배 후보가 되고, 무효한 값은 경고와 함께 결과에 남기만 한다.

**⚠️ 축방향 항복 검사가 빠져 있던 것도 같이 잡았다.** 횡하중이 없는 부재는 정적 SF가 무한대로 나오는데, 그 부재가 압축을 나르고 있었다. 압축응력을 항복에 대고 재는 검사를 따로 추가했다.

### 통합: 이제 설계가 피로나 좌굴로 탈락한다
`physics/failure_modes.py`가 duty가 가능하게 만드는 검사만 돌리고 **가장 작은 안전율**을 지배모드로 보고한다. **적용 불가한 검사는 큰 숫자가 아니라 `None`이다.** 적용되지 않은 검사가 큰 안전율을 보고하면 잘 설계된 부품처럼 읽히는데, 실제로는 아무것도 안 본 것이다.

**실증 1: 정적 통과, 피로 탈락.** 7075-T6, 40x40x2 mm, L=0.5 m, 완전교번 179.9 MPa.
정적 SF **2.796**(항복 503 MPa 대비 여유), 피로 SF **0.884** → **FAIL**. 항복만 봤으면 통과시켰을 설계다.

**실증 2: 정적 통과, 좌굴 탈락.** 같은 단면, L=1.5 m, 압축 7000 N, 고정-자유.
축방향 항복 SF **21.8**(항복 근처에도 못 감), 좌굴 SF **0.824** → **FAIL**. λ=193.1로 오일러 유효.

**실증 3: 세장비가 지배모드를 바꾼다.** 같은 단면·같은 하중에서 **길이만** 바꾼다.
L=1.5 m → **좌굴**이 지배(SF 0.824). L=0.2 m → **축방향 항복**이 지배(SF 21.8), 오일러는 무효.

### registry 등록과 정직성
`fatigue_sn`과 `buckling_euler`를 capability registry에 등록했다(applicability·fidelity·cost). 적용범위가 이 둘의 핵심이다. **정적 단일하중에는 피로가 없고, 인장 부재는 좌굴하지 않는다.** 반복하중이 없으면 `fatigue_sn`이, 압축이 없으면 `buckling_euler`가 사유와 함께 후보에서 빠진다.
**3D FEM 선형좌굴은 등록하지 않았다.** 이 형상이 못 보는 국부좌굴(박벽 웹·플랜지)을 잡아 주겠지만 **구현되어 있지 않다.** 가짜 카탈로그 금지 규율 그대로다.

**남는 한계를 적어 둔다**
- S-N은 **고주기 전용**이다. 소성변형이 지배하는 저주기 피로는 변형률-수명 모델이 필요하고 없다. **모듈은 자기가 범위 밖이라고 말해 주지 않으므로** 호출자가 알아야 한다.
- **노치·표면조도·치수·온도 계수가 전부 없다.** 실제 내구한도는 핸드북 값에 이것들(각각 1 미만)을 곱한 값이다. 따라서 여기 숫자는 실물 대비 **낙관적**이다.
- 피로 데이터는 `reference_typical`이고 **산포가 크다.**
- 오일러는 **이상 기둥**이다. 초기 굽음·하중 편심·잔류응력이 전부 실제 붕괴하중을 낮추며 **knock-down 계수를 적용하지 않았다.** 안전율 1 근처는 안전한 설계가 아니다.
- **굽힘과 축압축을 따로 검사하고 합치지 않는다.** 둘 다 걸리는 부재는 상관식이 필요하고, 여기 개별 안전율은 그 경우에 낙관적이다. 모듈은 그런 duty를 알아채지 못하므로 호출자가 알아야 한다.
- 전부 **SIMULATED**다.

CLI는 `dae failure-modes`로 셋을 한 번에 본다.

## Phase 17: 샤프트 설계와 베어링 L10 수명 (구현 완료)
Phase 11이 관절 토크를 내고 Phase 12가 그걸 낼 모터·감속기를 골랐다. **그 토크는 거기서 멈추지 않는다.** 샤프트로 나가고, 샤프트는 베어링이 잡는다. 이 단계가 그 구간을 메워 duty cycle에서 실제로 하중을 받는 부품까지 경로를 잇는다.

### 17-A: 샤프트 (복합하중, DE-Goodman)
**비틀림만으로 지름을 고르면 틀린다.** 기어나 풀리를 단 샤프트는 전달하는 힘에 의해 **비틀리는 동시에 굽는다.** 더 중요한 것은, **회전축이 정상 횡하중을 받으면 그 굽힘이 완전교번**이라는 점이다. 매 회전마다 한 섬유가 최대 인장에서 최대 압축까지 간다. duty에 변하는 것이 하나도 없어 보여도 샤프트는 피로 문제이고, 정적 비틀림 검사는 그걸 못 본다.

**🔑 DE-Goodman 손검증**: 일반형
`1/n = (16/πd³)·[ sqrt(4(Kf·Ma)²+3(Kfs·Ta)²)/Se + sqrt(4(Kf·Mm)²+3(Kfs·Tm)²)/Sut ]`
가 회전축·정상토크에서 손계산 가능한 축약형
`1/n = (16/πd³)·[ 2·Kf·M/Se + √3·Kfs·T/Sut ]`
과 **상대 1e-15로 일치**한다. 지름식은 같은 관계를 d에 대해 푼 것이고, 두 식이 **상대 1e-12로 서로를 역산**한다(사이징과 검사가 같은 관계여야 한다. 아니면 둘 중 하나가 틀린 것이다).

**독립 교차검증 두 개**로 교과서 대조를 대신했다. 축약형이 두 개의 **독립적으로 알려진 결과**로 환원되는지 확인한 것이다.
- **순수 교번굽힘**(토크 0): `n = Se/(Kf·σ_a)`로 환원. 이건 Phase 16 완전교번 피로검사에 응력집중을 붙인 것과 같다. **다른 유도가 같은 수에 도달**하는 것이 요점이다.
- **순수 정상비틀림**(굽힘 0): `n = Sut/(Kfs·√3·τ)`로 환원. 순수전단의 von Mises 등가를 극한강도에 대고 잰 정적 검사다.

**응력집중 기본값을 1.0으로 두지 않았다.** `Kf=1.7`, `Kfs=1.5`이고 **[ASSUMED]** 다. 무언가를 잡아 주는 샤프트에는 그 자리에 어깨 필렛이나 키홈이나 스냅링 홈이 있고 전부 국부응력을 올린다. 1.0으로 두면 **아무 특징 없는 봉**을 모델링하는 것이고 실제로 기어를 물릴 수 없는 지름이 나온다. 실제 설계는 자기 형상을 재야 한다.

**⚠️ 회전/정지의 배분이 이 모듈에서 가장 결과를 크게 바꾸는 사실이다.** `ShaftLoads.rotating`은 굽힘을 **전부 교번**, 토크를 **전부 평균**에 넣는다. 이걸 뒤집으면 교번응력이 내구한도가 아니라 극한강도에 대고 계산되어 **안전율이 대략 두 배로 부풀어 오른다.** 테스트로 배분과 그 결과(회전 케이스가 더 가혹함)를 둘 다 고정했다.

**위험속도는 의도적 단순화이고 답을 좋게 만든다**: `ω₁ = (π/L)²·sqrt(EI/(ρA))`, 균일 단면·단순지지·**부착질량 없음**. 기어·풀리·로터가 바로 그 무거운 것들이고 위험속도를 크게 낮춘다. 운전속도가 공진 근처인지 보는 용도이지 안전하게 아래라고 결론 내리는 용도가 아니다.

### 17-B: 베어링 L10
**🔑 L10 손검증**: C=20300 N, P=2000 N, p=3에서 `(20300/2000)³×10⁶ = 1.045678375e9` 회전, **상대 1e-15 일치**. 시간 환산 `L10h = L10/(60n)`도 확인. 볼(p=3)과 롤러(p=10/3) 지수 차이로 **하중을 반으로 줄일 때 롤러가 더 많이 얻는 것**까지 확인했다.

**등가동하중** `P = X·Fr + Y·Fa`. 깊은홈 볼베어링의 X·Y는 Fa/C0에 따른 **표준 표**(ISO 281)에서 보간한다. 이건 발행된 표준이지 제조사별 카탈로그가 아니라서 부품번호와 달리 여기 들어올 수 있다. Fa/Fr이 e 이하면 추력이 접촉기하를 바꾸지 않으므로 등가하중은 Fr 그대로다.
**원통 롤러 베어링은 추력을 거부한다.** 플랜지가 없어 축하중을 받지 못하므로, 추력이 들어오면 등가하중에 흡수하지 않고 **예외를 던진다.** 조용히 무시하면 베어링이 실제로 받지 않는 하중조합에 대한 수명을 돌려주게 된다.

**베어링 DB: 치수는 표준, 정격은 카탈로그가 아니다.** 608·6000·6002·6004·6206·NU206은 **ISO 경계치수 코드**이지 벤더 부품번호가 아니다. 6206은 어느 제조사에서든 내경 30·외경 62·폭 16 mm이고, 그걸 적는 것은 표준을 진술하는 것이지 부품을 지어내는 것이 아니다. **C와 C0는 다른 문제라서** 제조사·등급마다 10% 넘게 다르고, 따라서 여기 값은 크기등급의 대표값이며 `illustrative` 태그가 붙는다. 모터·감속기 아키타입, 재료 DB와 같은 규율이다.

**⚠️ 검증기를 하나 고쳤다.** 처음에 `C > C0`를 강제했는데, 이건 여기 있는 소·중형 볼베어링에서만 성립하고 **일반 규칙이 아니다.** 대형 롤러 베어링은 C0가 더 큰 경우가 흔하다(예: NU 2320). 그대로 뒀으면 **정당한 실물 베어링을 거부**했을 것이다. 비율이 [0.1, 20] 범위에 드는지로 바꿔서, kN을 N 자리에 넣은 단위 오류나 두 필드가 뒤바뀐 경우는 여전히 잡으면서 실물을 막지 않게 했다.

### 하중경로 연결
`drivetrain/loadpath.py`가 선정된 구동계를 샤프트·베어링 하중으로 잇는다. 모델은 흔한 배치, **두 베어링에 오버행 하중**이다. 로봇 관절이 대체로 그 모양이고(감속기 출력이 지지되고 링크가 지지점 밖으로 뻗는다), 주어진 하중에 대해 **최대 굽힘모멘트를 만드는 배치**라서 모델링할 가치가 있다.
반력 `R_B = P(L+a)/L`, `R_A = P·a/L`. 손계산·평형 둘 다 확인했다. **가까운 베어링은 가해진 하중의 일부가 아니라 그보다 많이 받는다.** 오버행 하중은 먼 베어링을 **들어올린다.**
`use_peak_torque`는 기본값이 아니라 **결정**이어야 한다. 피로와 베어링 수명은 duty에 걸쳐 누적되므로 연속토크가 맞고, 피크토크로 수명을 계산하면 비의 세제곱만큼 수명을 과소평가한다.

### 검사가 실제로 변별한다
**샤프트: 정적 통과, 피로 탈락.** 굽힘 20 N m, 토크 25.6 N m, SCM440.
d=10 mm에서 정적 SF **2.15**(어느 검토라도 통과시킬 값), 피로 SF **0.784** → **FAIL**. 정적 응력으로 사이징했으면 그대로 출하됐을 샤프트다. 11 mm면 통과한다.
그리고 이건 한 지름의 우연이 아니다. **두 안전율이 모두 d³로 스케일해서 비가 고정**이므로, 어떤 지름에서도 정적 검사가 구속조건이 되지 않는다. 정적만 보는 설계 절차는 **한계 근처가 아니라 모든 크기에서** 틀린다.

**베어링: 같은 부품, 같은 하중, 감속기 반대편에서 반대 판정.** 608에 650 N.
관절측(3 rad/s, 29 rpm): L10 **79,644 h**, 여유 3.98 → PASS.
모터측(300 rad/s, 2865 rpm): L10 **796 h**, 여유 0.04 → **FAIL**.
**회전수 기준 수명은 동일**하다(같은 하중이므로). 감속기가 토크를 속도와 맞바꾸므로 모터측은 그 회전을 100배 빨리 소모하고, 시간 기준 수명이 100분의 1이 된다. 테스트가 `L10_회전수`가 같고 `L10_시간`이 정확히 비만큼 다른 것을 고정한다.

### registry와 정직성
`shaft_combined`(ANALYSIS), `bearing_l10`(SELECTION)을 등록했다. **인장 봉은 샤프트가 아니고, 회전 지지가 없으면 베어링 수명이 없다.** 각각 사유와 함께 배제된다.
**미구현은 등록하지 않았다**: 3D 샤프트 FEA, 상세 접촉피로, 기어 AGMA, 볼트 이음. 전부 등록되어 있지 않다.

**남는 한계**
- 샤프트: **중실 원형 단면만**. Kf·Kfs는 **[ASSUMED]** 이고 실물 대비 낙관적일 수 있다. 위험속도는 **부착질량 무시**로 낙관적이다. 피로는 고주기 전용(Phase 16 상속).
- 베어링: **L10은 통계다.** 열 중 하나는 그 전에 고장 나며, 특정 베어링에 약속된 수명은 없다. **ISO 281의 신뢰도·윤활·오염·온도 계수를 적용하지 않았고**, 각각이 답을 한 자릿수 넘게 움직일 수 있다. 카탈로그는 `illustrative`.
- 전부 **SIMULATED**.

CLI는 `dae loadpath`로 선정부터 샤프트 지름·베어링 수명까지 한 번에 본다.

## Phase 18: 모터 정상상태 열과 구조 열응력 (구현 완료)
Phase 12가 "subject to thermal validation"으로 남긴 프록시를 진짜 열 모델로 대체한다. 연속토크 정격 자체가 **열에 대한 진술**이고, 하나의 주위온도와 하나의 장착조건에서 매긴 것이라, 그와 다른 duty는 그 정격이 보증하지 않는다.

### 18-A: 모터 정상상태 열
**🔑 손검증**: `P_cu = k·T_rms²`, `P_fe = k·ω`, `T_rise = P_loss·R_th`. 전부 상대 1e-15 일치.

**⚠️ 평균토크가 아니라 RMS 토크여야 한다.** 동손이 토크의 **제곱**으로 가기 때문이다. 절반은 1 N m, 절반은 정지인 duty는 평균 0.5, RMS 0.7071이고, 평균을 쓰면 발열을 **두 배 과소평가**한다. duty가 뾰족할수록 오차가 커지므로, **오차가 가장 큰 곳이 정확히 문제가 되는 곳**이다.

**⚠️ 아키타입 열계수를 재보정했다.** 처음에 손실계수와 열저항을 서로 독립으로 골랐더니 연속토크에서 **온도상승이 6 K**로 나왔다. 그러면 어떤 열검사도 통과하고 계산 전체가 장식이 된다. **모터의 연속토크 정격은 곧 열 정격**이므로, 연속토크·정격속도에서 절연등급 한계 근처에 떨어지도록 세 계수를 맞췄다. 결과가 물리적으로 정합한다: 상승 약 95 K, 40 °C 주위에서 권선 135 °C(F등급 155 °C 대비), 효율 70~77%. 테스트가 이 자기정합성을 고정한다.

**실증 1: 프록시가 통과시키는 duty가 권선을 태운다.** bldc_100w(연속 0.32 N m).
duty: 절반 0.60 N m, 절반 정지. **평균 0.30 N m → 연속토크 프록시 통과.** RMS는 **0.4243 N m**, 권선 **178 °C**(한계 155) → **FAIL**.
프록시는 duty의 **평균만 보고 모양을 못 본다.** 이것이 Phase 12가 미뤄 둔 검증 그 자체다.

**실증 2: 주위온도가 마진을 1:1로 먹는다.** 같은 duty(0.30 N m)에서 25 °C 주위 → 권선 111 °C PASS, 70 °C 주위 → **156 °C FAIL**. duty는 하나도 안 바뀌었다. 벤치에서 25 °C로 사이징한 드라이브가 자기 드라이버 옆에 설치되면 45 K의 마진을 잃는다. 그래서 기본 주위온도를 상온이 아니라 **40 °C**로 뒀다.

### 18-B: 구조 열응력
**🔑 손검증**: `σ = -E·α·ΔT`. 7075-T6, 71.7 GPa, 23.6e-6/K, 60 K → **-101.527 MPa**, 상대 1e-15 일치.

**부호가 장식이 아니다.** 구속된 부재를 **가열하면 압축**, **냉각하면 인장**이다. 이게 기계적 인장에 더해지는지 덜어지는지, 좌굴을 유발할 수 있는지를 결정한다. 크기만 돌려주면 **답의 쓸모 있는 절반을 버리는 것**이다.

**실증 3: 냉각이 위험한 방향이고, 그게 직관과 반대다.** 7075-T6 브래킷, 기계적 인장 350 MPa(항복 503, SF 1.44로 통과).
`ΔT = +100 K` → 열응력 **-169 MPa**, 합성 181 MPa, **SF 2.78** (완화된다).
`ΔT = -100 K` → 열응력 **+169 MPa**, 합성 **519 MPa**, **SF 0.97 → FAIL**.
기계적 하중은 하나도 안 바뀌었는데 **식으면 항복을 넘는다.** 크기를 더하는 모델이었다면 이 두 경우를 구별하지 못했을 것이다.

**⚠️ 강이 알루미늄보다 켈빈당 응력이 크다.** 직관과 반대이고 계수만으로 재료를 줄 세우면 틀리는 지점이다. 강은 팽창이 절반이지만 강성이 세 배라 **구속 비용이 더 크다**: 2.3985 MPa/K 대 1.6921 MPa/K. 탄성계수를 빼놓을 수 없는 이유다.

**구속계수는 모델의 필수 입력**이다(0=자유, 1=완전구속). 같은 온도변화가 장착방식에 따라 응력 0에서 항복 수준까지 나온다. **자유팽창은 변형만 있고 응력이 없다.** 열응력을 만드는 것은 온도가 아니라 구속이다.

**이종재료 접합**: `(α_a - α_b)·ΔT`. 외부 구속이 없어도 **서로가 서로를 구속**한다. 알루미늄-강 100 K에서 **1190 마이크로스트레인**, 알루미늄 자체 팽창의 절반이다.

### 재료 DB에 열팽창계수 추가
15종 전부에 `thermal_expansion_1_k`를 넣었다(reference_typical, 1/K). **기존 값은 하나도 바꾸지 않았다**(diff로 확인).

**⚠️ CFRP 때문에 필드를 양수로 제약할 수 없었다.** 탄소섬유는 가열하면 길이방향으로 **수축**한다. α₁ = **-0.5e-6**(음수), α₂ = +25e-6. **50배 차이에 부호가 반대**다. `gt=0.0` 제약을 걸었다면 물리적으로 옳은 값을 거부했을 것이고, 단일 값을 쓰면 어느 값을 고르든 한 방향에서 틀린다. 그래서 방향별 필드를 두고, 직교이방 재료가 단일 α만 선언하면 검증기가 거부한다(단일 항복강도가 무의미한 것과 같은 이유이고, 부호까지 다르므로 더 나쁘다).
**고분자 값은 산포가 크다**(±20%, 등급·충전재에 따라). 헤더에 명시했고 자릿수 수준으로 보라고 적었다. **CTE 자체가 온도의존**이며 여기 값은 상온 기준이라는 것도 적었다.

### registry와 정직성
`motor_thermal`, `thermal_stress` 등록. **duty cycle이 없으면 모터를 데울 것이 없고, 온도변화가 없으면 열응력이 없다.** 각각 사유와 함께 배제된다.
**미구현 미등록**: 과도 열해석, 상세 열저항망, CFD, 접촉 열저항.

**남는 한계**
- **정상상태 전용.** 짧은 과부하는 여기서 걸려도 실제로는 괜찮을 수 있고(열용량이 못 따라감), 평균으로 통과한 긴 과부하가 권선을 태울 수도 있다. 과도 해석은 없다.
- **열저항이 단일 집중 상수**다. 실제로는 권선-케이스와 케이스-주위가 직렬이고 **장착이 후자를 지배**한다. 큰 알루미늄 브래킷에 볼트로 조이면 절반이 되고 공중에 세워두면 두 배가 된다. 한 숫자로 표현할 수 없으며 **어느 방향으로든 두 배 틀릴 수 있다.**
- **철손이 속도에 선형**이다. 실제로는 히스테리시스(대략 선형)와 와전류(대략 제곱)로 갈리므로 **고속에서 과소평가**한다.
- 열응력은 **균일 온도변화·균일 구속**이다. 실제 부품에는 **온도 구배**가 있고, 구배는 **구속이 전혀 없어도 응력을 만든다.** 이 모델은 그걸 표현하지 못한다.
- 계수는 `reference_typical`(CTE)·`illustrative`(R_th, 손실계수). 전부 **SIMULATED**.

CLI는 `dae thermal`로 둘을 한 번에 본다.

## Phase 19: 볼트 이음과 기어 이 (구현 완료)

### 19-A: 볼트 이음
**볼트 이음은 볼트 강도 문제가 아니라 프리로드 문제다.** 이음이 되느냐를 결정하는 것은 외부하중이 오기 전에 볼트를 얼마나 세게 당겨 놓았는가다. 제대로 프리로드된 볼트는 외부하중의 **일부만** 본다. 나머지는 볼트로 가지 않고 **부재에 이미 걸려 있던 압축을 덜어내는 데** 쓰인다.

**🔑 손검증**: A_t(M8)=36.6 mm², 8.8등급 proof 580 MPa → proof하중 **21228 N**, 0.75 프리로드 **15921 N**, 토크 `T=K·F·d` = **25.474 N m**. 강성 `k_b = A_t·E/l`, `k_m`은 Wileman 실험식, **C = k_b/(k_b+k_m) = 0.184**. 전부 상대 1e-12 일치.
부재가 볼트보다 **4.4배 뻣뻣**하고, 그래서 볼트는 외부하중의 **18%만** 느낀다. 이것이 프리로드된 이음이 피로에서 살아남는 이유 전부다.

**⚠️ `F_b = F_i + C·P`는 이음이 아직 물려 있을 때만 유효하다.** 구현 중에 잡았다. **분리되고 나면 나눠 가질 상대가 없어져 볼트가 전부를 받는다.** 물린 상태의 식을 분리 이후에 그대로 쓰면 볼트 하중을 심하게 과소평가하고, 하필 **그게 문제가 되는 바로 그 경우**(프리로드 부족)에서 그렇게 된다. 최소하중과 최대하중이 서로 다른 영역에 있는 혼합 케이스도 각각 판정한다.

**⚠️ 볼트 피로에는 일반 Goodman을 쓰면 안 된다.** 처음에 샤프트와 같은 형태로 썼더니 **프리로드를 낮추면 피로수명이 좋아진다**고 보고했다(평균응력 항이 내려가므로). 공학적 조언으로는 **정반대**다. 프리로드의 존재이유는 이음을 물려 두는 것이고, 프리로드가 낮으면 분리되어 볼트가 외부 범위 전체를 받는다.
그래서 **하중선이 프리로드 점에서 출발하는** 볼트 표준형을 쓴다:
`n = Se(Sut − σ_i) / (Sut·σ_a + Se(σ_m − σ_i))`
프리로드는 사이클이 시작되기 전에 이미 존재하는 정적 응력이므로, 사이클 성분이 그 위에 **올라타는** 것이지 그것과 **평균되는** 것이 아니다.

**실증: 분리가 교번응력을 배가한다.** M8/8.8, 20 mm 그립, 외부 8000 N(0→8000 반복).
프리로드 75%: 물림 유지, 볼트하중 17396 N, 교번 **20.1 MPa** → PASS.
프리로드 15%: **분리**(P_sep 3904 < 8000), 볼트하중 **8000 N**(전부), 교번 **65.8 MPa** → FAIL.
교번응력이 **3.3배**로 뛴다(한계값은 1/C = 5.4배). 물린 식으로 계산했다면 편안한 수명을 보고했을 것이다.

**ISO 898-1 등급과 나사 응력단면적은 발행 표준**이다. 8.8등급은 어느 공급자에게서든 같은 것을 뜻하므로, 부품번호가 들어올 수 없는 자리에 이 값들은 들어올 수 있다. **A_t는 봉 단면적이 아니다**(나사가 재료를 깎아낸다). 공칭 지름을 쓰면 용량을 약 20% 과대평가한다.

### 19-B: 기어 이
기어 이는 **서로 독립인 두 가지 방식**으로 부서지고, 각각 다른 것이 치수를 정한다. **굽힘**은 이뿌리를 부러뜨리고 모듈과 치폭이 지배한다. **피팅**은 접촉응력에 의한 표면피로이고 피치원 지름과 표면경도가 지배한다.

**🔑 손검증**: d = m·N = 40 mm, W_t = 2T/d = **1250 N**. Lewis `σ = W_t/(b·m·Y)` = **97.05 MPa**(Y=0.322). 탄성계수 `Z_E` = **189.7 √MPa**(강-강 교과서 값 약 190). 기하계수 `I(비 3)` = **0.12052**. Hertz `σ_c = Z_E√(W_t/(b·d·I))` = **682.88 MPa**. 전부 상대 1e-12 일치.

**실증: 모듈이 지배모드를 바꾼다.** 굽힘은 `1/(b·m·Y)`, 접촉은 `√(1/(b·m·N))`으로 가므로 응력비가 `√(2·T·I)/(√b·m·Y·Z_E)`이다. 모듈에 반비례하므로:
`m = 1.0 mm` → 굽힘 776 / 접촉 1932 MPa, **굽힘 지배**(SF 0.26 vs 0.36).
`m = 4.0 mm` → 굽힘 48 / 접촉 483 MPa, **피팅 지배**(SF 4.12 vs 1.45).
**세밀 피치 기어는 이가 부러지고, 굵은 피치 기어는 표면이 패인다.** 한쪽만 검사하는 것이 기어세트가 검토를 통과한 뒤 **검사하지 않은 쪽으로** 고장 나는 방식이다.
하중이 4배가 되면 굽힘은 4배, 접촉은 **2배**만 된다(제곱근). 이 차이가 설계를 스케일할 때 지배모드가 뒤집히는 이유다.

**⚠️ 보정계수 기본값 1.0은 중립이 아니라 낙관이다.** 이건 Lewis와 초등 Hertz이지 AGMA가 아니다. AGMA는 동하중·하중분포·용도충격·치수 계수를 곱하고 **전부 1.0 이상**이다. 그래서 계수를 **보이는 인자로 노출**했다(없애면 1.0이 중립인 척하게 된다). 실측: 무보정 SF_b 2.06/SF_c 1.03 **PASS** → 로봇관절 수준 보정(1.8/1.3) SF_b 1.14/SF_c 0.79 **FAIL**.
**두 허용응력은 별개 입력**이다. 관통경화강이 굽힘 200·접촉 700 MPa라면 침탄은 380·1250이다. 하나로 두면 두 모드 비교 자체가 무의미해진다.

### registry와 정직성
`bolted_joint`, `gear_tooth` 등록. **프리로드된 볼트 연결이 없으면 검사할 이음이 없고, 토크를 전달하는 메시가 없으면 기어 검사가 없다.**
**미구현 미등록**: 전체 AGMA, 볼트군, 유성/하모닉 내부.

**남는 한계**
- 볼트: **너트계수 K가 산포 ±30%** 라서 토크로 얻은 프리로드는 완벽한 토크렌치로도 그만큼 불확실하다. **각도법·직접 장력측정이 더 정확하고 여기 없다.** 축방향 하중을 받는 **단일 볼트**만 다루며 볼트군·편심·전단 이음은 없다.
- 기어: **AGMA 전체가 아니다**. 보정 없는 결과는 실물 대비 낙관적이다. 표면처리는 호출자가 넘기는 허용응력으로만 표현된다. 유성·하모닉 내부는 없다.
- 전부 **SIMULATED**.

CLI는 `dae bolt`(프리로드 스윕)와 `dae gear`(두 모드)로 본다.

## Phase 20: 다목적 최적화와 Pareto frontier (NSGA-II) (구현 완료)
단일목적 최적화는 "제약을 만족하는 가장 가벼운 설계는 무엇인가"에 답한다. 실제 설계 질문은 그 모양이 아니다. 티타늄 링크는 강철보다 가볍고 kg당 40배 비싸며, CFRP는 더 가볍고 60배다. **그중 어느 것도 정답이 아니고**, 가중치를 미리 정해 하나의 숫자로 뭉개는 것은 결정을 알려주는 게 아니라 **감추는** 것이다.

### 유효범위를 먼저 적었다 (Phase 19 관찰에 따른 새 규율)
새 식마다 **어디서 유효하지 않게 되는지를 구현 전에** 명시했다.
- **지배(dominance)는 모든 목적이 최소화라고 가정한다.** 최대화 열(예: 고유진동수)을 그대로 넣으면 **가장 낮은 진동수 설계를 최적이라고 조용히 반환**한다. `to_minimisation`을 별도의 이름 있는 단계로 두고, 감각(sense)을 안 적으면 거부한다.
- **crowding distance는 한 front 안에서만 의미가 있다.** front 간 비교는 무의미하다(rank가 이미 분리한다).
- **반환되는 front는 유한 population 근사**다. 참 front는 대개 연속체이고 무한히 많은 점을 갖는다. 평가한 것들에 대해서만 비지배이며 전역 최적성은 증명하지 않는다.
- **SBX·다항식 변이는 상자 제약된 실수 변수 전용**이다. 정수·범주형(재료 id 같은)에 쓰면 **알루미늄과 티타늄 사이의 중간값** 같은 무의미한 값이 나온다.
- **제약 처리는 constrained domination**이라 위반량이 단일 비음수 스칼라여야 하고, 작은 위반이 실제로 허용되는 상황에는 맞지 않는다.

### 🔑 검증
**1. 비지배 정렬 vs 브루트포스**: 모든 쌍을 직접 비교하는 독립 구현과 대조. 랜덤 (200,2)·(150,3)·(80,5) 및 **중복이 많은 데이터**(동점이 흔한 경우)에서 front 0이 **완전히 일치**.
**⚠️ 이 대조가 실제로 버그를 잡았다.** 첫 구현이 `dominated_by_count[i]`에 **i가 지배하는 개수**를 넣었다(i를 지배하는 개수여야 하는데). 정렬이 뒤집혀 **거의 최악 층이 front 0으로** 나왔다. 코드는 그럴듯해 보였고 ZDT1 거리가 7.74로 나와서야 이상함을 알았다.

**2. 해석적 Pareto와 대조**: ZDT1의 참 front는 `f2 = 1 − √f1`로 닫힌 형태다. 실측 **평균 거리 0.00133, 최대 0.0159**, f1이 0.0000~1.0000 전 구간에 퍼짐.

**3. front 성질**: 반환된 front의 **어느 두 점도 서로 지배하지 않고**(정의), front 밖의 모든 점은 front의 **어떤 점에 지배된다**(정의의 나머지 절반). 둘 다 실제 출력에 대해 확인.

**4. 다양성**: f1이 0.05 미만에서 0.95 초과까지 퍼지고, **어떤 간격도 전체 범위의 1/4을 삼키지 않는다**. 수렴했지만 뭉친 front는 한 가지 질문에만 답한다.

**5. 결정론**: 시드가 같으면 결과가 완전히 동일하고, 다르면 다르다.

### 재료 가격 추가
15종 전부에 `price_per_kg_usd`를 넣었다. **기존 값은 하나도 바꾸지 않았다**(값 라인 diff 완전 동일).
**⚠️ 유효범위: 이것은 원소재 가격이지 부품 가격이 아니다.** 질량에 곱하면 **재료비 명세**가 나오지 부품 가격이 나오지 않는다. 가공·후처리·조립·공구·스크랩·수량할인이 전부 빠져 있고, 작은 절삭 브래킷은 **가공비가 재료비를 여러 배 압도한다.** 이 숫자로 고른 설계가 더 싼 부품이라는 보장이 없다. **고정된 공정 아래에서 재료끼리 줄 세우는 용도**이며 그건 답할 수 있는 질문이다.
가격 자체의 산포도 이 DB의 어떤 필드보다 크다(상품시장·형태(봉/판/분말)·소량 프리미엄).

### 재료는 탐색변수가 아니다
연산자가 값 사이를 보간하는데 **알루미늄과 티타늄 사이에는 아무것도 없다.** 그래서 연속 탐색을 **재료마다 따로 돌리고 front를 합친다.** 설계벡터에 재료 인덱스를 넣었다면 분수 재료가 나오고 어딘가에서 조용히 반올림됐을 것이다.

### 결과: 실제 트레이드오프 (MVP 링크, 4재료)
| 재료 | 병합 front 위 | 최경량 (kg) | 최저 재료비 (USD) |
|---|---|---|---|
| steel_s45c | 32 | 0.5076 | **0.508** |
| al_6061_t6 | 32 | 0.3250 | 0.975 |
| al_7075_t6 | 23 | **0.3051** | 1.830 |
| ti_6al_4v | 17 | 0.3519 | 14.076 |

목적별 승자: 질량 **al_7075_t6**, 처짐·응력·비용 **steel_s45c**. **한 재료가 전부를 이기지 않는다.** front를 계산하는 이유 전부다.

**❗ 티타늄이 7075보다 무겁고 비싸다.** 직관과 반대이고 실제 결과다. **처짐이 지배하는 설계에서는 비강성이 결정하는데 티타늄(114 GPa / 4430)과 알루미늄(71.7 / 2810)의 비강성이 25.7 대 25.5로 거의 같다.** 그래서 티타늄이 사는 것은 없고 7배 비싸다. 티타늄이 front에 17점 남아 있는 것은 다른 목적의 어떤 영역에서 비지배이기 때문이지 가벼워서가 아니다.
가중치를 질량 1·비용 1로 주면 **어느 목적도 단독으로 이기지 않는 al_6061_t6**가 선택된다. 가중치는 **front를 안 뒤에** 적용한다. 그것이 front를 계산하는 목적이다.

### registry와 정직성
`nsga2`를 등록하고 **유효범위를 조건으로 걸었다**: 목적이 하나면 매핑할 트레이드오프가 없고, 이산변수가 있으면 연속 연산자를 쓸 수 없다. 둘 다 사유와 함께 배제된다.
**남는 한계**: front는 유한 근사, NSGA-II는 확률적(시드로 결정론), **비용은 재료비만**, 제조성은 목적에 넣지 않았다(단순 프록시가 될 뿐이므로). 전부 **SIMULATED**.

CLI는 `dae pareto`로 본다.

## Phase 21: 고전적층판이론 (CLT) (구현 완료)
Phase 8이 솔버에 직교이방 탄성을 줬다. 그건 **무엇으로 만들지 이미 정한 뒤에** 복합재를 해석하게 해 준다. CLT는 **그 결정을 내리는 방법**이다. 적층재는 **적층 순서**로 설계하며, 같은 ply를 다른 순서·다른 각도로 쌓으면 **강성도 강도도 먼저 깨지는 ply도 달라진다.**

### 유효범위를 구현 전에 적었다
- **얇은 판, Kirchhoff 가정.** 중립면에 수직인 직선이 변형 후에도 직선·수직을 유지한다. 두꺼운 적층재는 두께 방향으로 전단하고 **CLT는 그걸 못 본다.**
- **모든 ply에서 평면응력**(σ₃=τ₁₃=τ₂₃=0). **이 가정이 가장 심하게 깨지는 곳이 자유단(free edge)** 이고, 실제 적층재가 층간분리하는 곳이 정확히 거기다. **CLT는 자유단에서 분리될 적층재를 편안하다고 보고하며 아무 경고도 하지 않는다.**
- **완전접착.** ply 간 미끄러짐도 층간 컴플라이언스도 층간분리도 없다.
- **파손까지 선형탄성.** 기지 소성도 손상진전도 없다.
- **first-ply failure는 최종파손이 아니다.** 실제 적층재는 첫 ply가 깨진 뒤에도 대개 더 버틴다(깨진 ply가 자기 몫을 나머지에 넘긴다). 따라서 강도 예측으로는 **보수적**이고 때로 크게 그렇다. **반대 오류도 가능하다**: 첫 파손이 주하중 방향의 섬유파손이면 적층재는 정말로 끝난 것이고, 거기서 "보수적"이라고 여기면 틀린다. **그래서 파손 모드를 함께 보고한다.**
- **습열(hygrothermal) 항 없음.** 경화 잔류응력과 수분 팽윤은 모든 적층재에 박혀 있고 모델에 없다.

### 🔑 검증
**1. Q와 변환(Q̄)**: `Q11 = E1/(1−ν12·ν21)` 등 손검증 일치. `Q̄(0°)=Q`, `Q̄(90°)`는 11↔22가 정확히 교환. 45°에서 `Q̄16`이 크게 나오고(31.5 GPa) **−45°에서 부호가 뒤집힌다**(균형 적층이 상쇄하는 이유).
**⚠️ Q̄16(90°)이 "0이 아니다"로 처음 나왔다.** `cos(90°)`가 6.12e-17이지 0이 아니라서다. 값은 1.8e-7 Pa, **Q11 대비 1.3e-18**. Pa 단위(1e11 규모)에 기본 절대허용오차 1e-8을 쓴 내 판정이 틀린 것이지 코드가 틀린 게 아니었다. 테스트를 상대 기준으로 고쳤다.

**2. ABD 손검증**: `[0/90]s`에서 `A11 = (Q̄11(0)+Q̄11(90))·2t` 상대 1e-12 일치.
**대칭이면 B=0**: `[0/90]s`에서 |B|/|A| = **2.3e-21**. 비대칭 `[0/90]`에서는 **5.4e-5**로 실재하고 `couples_extension_to_bending`이 True가 된다. **이건 ply 높이를 중립면에서 재기 때문에 성립한다.** 다른 기준점에서 재면 B가 사라지지 않고 커플링이 진짜처럼 보인다.

**3. 등방 극한**: 등방 ply를 **임의 각도**로 쌓아도(`[0/30/60/90]s`) A가 등방이 나와야 한다. 실측 `A11 = 7.855460e+07`이 `E·t/(1−ν²)`와 상대 1e-12 일치, `A22=A11`, `A16/A11 = 9.9e-34`, `A66=(A11−A12)/2`. 변환이나 적분이 틀렸다면 임의 각도 스택이 등방으로 나오지 않는다.

**4. Tsai-Wu 손검증**: F1·F2·F11·F22·F66·F12 전부 정의와 일치. **정의적 성질 확인**: 5개 단축 강도 각각에서(인장·압축·전단 양방향 6케이스) **파손지수가 정확히 1.000000, 강도비도 정확히 1.000000.** 자기가 맞춰진 데이터를 재현하지 못하는 기준은 조합상태를 보기 전에 이미 틀린 것이다.

**5. first-ply failure 지목**: 배향에 따라 먼저 깨지는 ply와 모드가 바뀌는 것을 확인(아래 실증).

### ⚠️ 파손지수는 안전율의 역수가 아니다
Tsai-Wu에는 응력의 **1차 항**이 있어서, 지수가 0.5라고 하중을 두 배 걸 수 있다는 뜻이 아니다. 그 뜻을 갖는 양은 **강도비(strength ratio)**, 즉 비례하중을 파손까지 스케일하는 배수이고 그것을 따로 계산한다.
`R` 은 `aR² + bR − 1 = 0`의 양근이며, 스케일한 응력을 다시 넣으면 지수가 정확히 1이 되는 것을 테스트로 확인했다. **지수를 안전율의 역수로 읽는 것은 흔한 오류이고, 재료가 인장·압축 비대칭인 경우(즉 1차 항이 중요한 바로 그 경우) 비보수적인 방향으로 틀린다.**

### ⚠️ 압축강도를 인장강도로 가정할 수 없어 DB에 추가했다
Tsai-Wu는 각 방향의 **인장과 압축 강도를 모두** 요구하는데 DB에는 방향당 하나뿐이었다. 단방향 복합재는 **두 축에서 서로 반대 방향으로** 비대칭이다. 섬유 방향으로는 압축이 **약하고**(섬유 미소좌굴), 횡방향으로는 압축이 **몇 배 강하다**(횡인장은 기지를 그냥 찢고 압축은 아니다).
`cfrp_ud`에 `Xc=1200`, `Yc=200 MPa`를 추가했다(기존 값 diff 완전 동일). **Yc/Yt = 4.0배, Xc/Xt = 0.80배.** 대칭을 가정했다면 횡인장 용량을 **4배 과대평가**했을 것이다. 다섯 값이 다 없으면 **거부한다**(가정하지 않는다).

### 실증: 같은 ply, 다른 적층 (Nx = 200 kN/m)
| 적층 | Ex (GPa) | Ey (GPa) | 강도비 | 먼저 깨지는 ply | 모드 |
|---|---|---|---|---|---|
| [0]4s | 135.9 | 10.1 | **7.500** | 0° | fibre_tension |
| [0/90]2s | 73.0 | 73.0 | 1.822 | **90°** | matrix_tension |
| [45/-45]2s | 43.0 | 43.0 | **0.643 (FAIL)** | 45° | shear |
| [0/45/-45/90]s | 58.0 | 58.0 | 1.296 | **90°** | matrix_tension |

**같은 ply·같은 하중인데 강성이 3.2배, 강도가 11.7배 벌어지고, 먼저 깨지는 ply의 각도와 모드가 바뀐다.** 이것이 CLT가 존재하는 이유다.
`[0]4s` 손검증: 200 kN/m ÷ 1 mm = 200 MPa, Xt=1500 MPa → **R = 7.5 정확히**.
`[0/90]2s`에서 90° ply가 먼저 간다. **고전적 복합재 결과**로, 횡인장강도 50 MPa 대 섬유방향 1500 MPa라 **기지가 섬유보다 훨씬 먼저 갈라진다.**

**준등방 적층 `[0/±45/90]s`가 면내 등방 거동을 낸다**: `A11=A22` 정확히, `A16/A11 = 0`, `A66=(A11−A12)/2`. ply 자체는 13:1로 강한 이방성인데 **스택은 아니다.**

### registry와 정직성
`laminate_clt` 등록. **직교이방 재료만으로는 부족하고 layup이 있어야 한다.** CLT는 스택을 설계하는 것이고 등방·단일 ply에는 설계할 스택이 없다.
**미구현 미등록**: 진행성 손상, 자유단 응력, Hashin, Puck, 습열.
**Tsai-Wu는 파손 모드를 구별하지 못한다.** 하나의 매끄러운 곡면이라 ply가 깨졌는지는 말하지만 **섬유가 끊어졌는지 기지가 갈라졌는지는 말하지 않는다.** 결과가 완전히 다른데도(갈라진 기지 ply는 섬유 방향으로 계속 하중을 나르고, 끊어진 섬유 ply는 아니다). 모드 기반 기준(Hashin·Puck)이 그걸 구별하며 여기 없고, 그래서 **진행성 손상 해석을 이 위에 지을 수 없다.** 최대응력 기준을 함께 돌려 **어느 성분이 지배하는지**는 보고한다.
**F12(상호작용 항)는 [ASSUMED]**. 단축 시험으로는 아예 구할 수 없고 잘 하지 않는 이축 시험이 필요하다. 기본값 −0.5√(F11·F22)를 쓴다.
ply 물성은 `reference_typical`이고 산포가 크다. 전부 **SIMULATED**.

CLI는 `dae laminate`로 적층별 비교를 본다.

## Phase 22: 통합 캐스톤 (어셈블리 전체 자율 설계) (구현 완료)
지금까지의 모든 method를 **하나의 어셈블리에 한 번에** 적용하고, 그 전체에 대해 **하나의 결합 판정**을 낸다. 요점은 개별 검사가 아니라(전부 이미 있었다) **설계가 그 전부를 동시에 견뎌야 한다는 것**과 **빠진 것이 이름을 갖는다는 것**이다.
문제: 구동 회전관절("무릎"), 페이로드 5 kg를 0.35 m에서, 3 rad/s, 20000 h 베어링 수명.

### 집계 자체의 유효범위를 먼저 적었다
이 단계가 새로 도입하는 것이 **집계**이므로, 가장 오독되기 쉬운 것도 그것이다.
- **어셈블리 판정은 가장 약한 method만큼만 신뢰할 수 있다.** 모든 검사가 이상화를 안고 있고 어셈블리는 그것들을 **동시에 전부** 물려받는다. 보고된 최소 안전율은 실제 마진의 **상한**이지 추정치가 아니다.
- **안전율의 최솟값은 시스템 안전율이 아니다.** 독립적인 모드 여러 개가 각각 1.1이면, 하나가 1.1인 시스템보다 **더 자주** 고장 난다. 여기서 시스템 신뢰도를 계산하지 않으며 최솟값을 그렇게 읽으면 안 된다.
- **커버리지는 등록된 method에 대한 것이지 현실에 대한 것이 아니다.** 아무도 구현하지 않은 파손모드는 검사할 수 없고, **누가 이름 붙이지 않았다면 목록에도 못 오른다.** 그래서 `assessed`는 `safe`가 아니라 "method가 돌았다"는 뜻이다.
- **순차 사이징이지 동시 최적화가 아니다.** 링크를 정하고, 그 관성으로 구동계를 고르고, 그 토크로 샤프트·베어링을 검사한다. 더 무거운 링크는 더 큰 토크를 요구하고 그건 관절에 더 많은 질량이다. **그 루프를 닫지 않았다.** 결과는 **정합적인** 설계이지 결합 최적해가 아니다.
- **하중경로가 모든 인계점에서 이상화된다.** 링크 선단하중이 가정된 오버행을 거쳐 샤프트 굽힘모멘트가 되고, 마운트 반력이 가정된 레버암을 거쳐 볼트 하중이 된다. 그 기하는 **입력이지 유도가 아니다.**
- **검사들이 서로 독립이라고 가정한다.** 베어링 마모가 축 정렬을 바꾸고 그것이 기어 하중분포를 바꾸는 일을 모델링하지 않는다.

### 🔑 결합(conjunctive) 판정
**어셈블리 PASS = 적용 가능한 모든 검사가 전부 통과.** 평균도 가중치도 없다. **베어링이 기계보다 오래 가고 볼트가 첫 사이클에 분리되는 관절은 "4분의 3쯤 괜찮은" 관절이 아니다.**
상태를 **셋**으로 둔다. `PASSED`(적용 가능한 것 전부 검사되고 통과), `PASSED_WITH_GAPS`(검사한 것은 통과했고 검사 못 한 것이 있음), `FAILED`. 둘째를 첫째에 합치면 **"전부 확인했다"와 "확인할 수 있는 건 다 했다"의 차이가 사라진다.** `passes`는 `PASSED`에서만 참이다.

**⚠️ NOT_ASSESSED는 통과가 아니다.** 이것이 통합 레이어가 저지를 수 있는 **가장 위험한 일**이다. 없는 검사를 만족한 검사로 취급하면 **사람이 보기를 멈추는 바로 그 지점에서 무지가 확신으로 바뀐다.** 판정에는 method 이름이 반드시 있어야 하고(없으면 생성 거부), 미검토에는 이유가 반드시 있어야 한다(없으면 생성 거부).

### 🔑 결함 주입 검증 (핵심)
집계 레이어가 **항상 "통과"라고 말해도 겉보기는 똑같다.** 그래서 각 파손모드를 **하나씩 주입**해 어셈블리가 거부되고 지배검사가 정확히 지목되는지 확인했다. 6건 전부 통과:
| 주입 | 결과 | 지배 |
|---|---|---|
| 베어링 수명 요구 1e11 h | FAILED | bearing/l10_life |
| 주위온도 120 °C | FAILED | drivetrain/winding_temperature |
| M3 볼트 1개, 레버 6 mm | FAILED | mount/separation |
| 샤프트 6 mm | FAILED | shaft/fatigue (0.446) |
| 페이로드 40 kg | FAILED | drivetrain/torque_and_speed |
| 잘못된 기어 메시 | FAILED | gearbox/tooth_bending |
주입이 **스위치가 아니라 실제 물리효과**임도 확인했다(샤프트를 20 mm로 키우면 그 실패가 사라진다).

### ⚠️ 구현 중 잡은 것 둘
**1. 기어박스 내부를 지어내고 있었다.** 처음 버전이 "대표 메시"를 발명해 그 검사 결과를 **기어박스의 판정으로 보고**했고 FAILED가 나왔다. **카탈로그는 비·토크정격·질량을 주지 기어 이 형상을 주지 않는다.** 없는 기어에 대한 수치를 만들어 낸 것이고, **측정된 결과와 똑같은 옷을 입은 조작된 결과**다. 이제 메시가 주어지지 않으면 **NOT_ASSESSED**로 보고하고, 주어지면 실제로 검사한다.
**2. 최적화기의 활성 제약을 제 손으로 거부하고 있었다.** 최적화기는 활성 제약을 **정확히 한계까지** 몰아붙이므로, 돌려받은 설계의 비율은 자기 수렴허용오차 이내에서 1.0이고 **부동소수에 따라 양쪽 어디로든 떨어진다.** 1.0과 엄격 비교하는 집계 레이어는 **자기가 받은 최적해를 무작위로 거부한다.** 제약 평가기가 이미 쓰는 허용오차(1e-4)를 공유하게 해서 두 레이어가 "만족"의 뜻에 합의하도록 고쳤다.

### 커버리지: 빠진 것에 이름을 붙인다
이 프로젝트가 **이름 붙일 수 있지만 method가 없는** 파손모드 14개를 명시적으로 나열한다: 링크 국부좌굴·충격, 샤프트 키홈 실측 Kt·비틀림진동, 베어링 접촉피로 상세·윤활오염, 기어박스 유성 하중분배·백래시 마모, 마운트 볼트군 편심·진동 풀림, 어셈블리 제조편차·공차누적·부식/프레팅, 그리고 기어 이(형상 미상).
**아무도 생각하지 못한 모드는 구조상 이 목록에 없다.** 그것이 커버리지 주장이 뜻할 수 있는 한계다.

### §29 엔지니어링 리뷰 출력
판정은 리뷰가 아니다. "1.13으로 통과"는 **어느 검사가 그 숫자를 냈는지, 그 검사가 무엇을 가정했는지, 무엇을 아예 안 봤는지**를 말해 주지 않는다. 리뷰가 넷을 낸다: **지배 제약**(설계를 정하는 것), **체인에서 가장 하중을 지는 가정**(가장 놀라운 가정이 아니라 **지배 검사 뒤의** 가정), **미검토 목록**, **다음에 검증할 것**.
그래서 각 검사가 **자기 자신의 가장 낙관적인 가정**을 들고 다닌다(단계별이 아니라 검사별로). 어느 검사가 지배할지 미리 모르기 때문이다.

**기준선 실행 결과**: `PASSED_WITH_GAPS`, 검사 13건 통과·미검토 14건. **지배: link/deflection at 1.000**(최적화기가 몰아붙인 활성 제약). 가장 하중을 지는 가정: *"강체 근부. 실제 마운팅 컴플라이언스가 여기 없는 처짐을 더한다."*

### STEP과 정직성
`dae design --step`은 **구조 링크만** STEP으로 낸다. 모터·감속기·베어링·체결구는 **형상이 없는 선정 카탈로그 항목**이고, 그것들에 solid를 지어내면 **아무도 설계하지 않은 형상이 STEP 파일에 들어간다.**
전부 **SIMULATED**. 물리시험이 없으므로 어느 것도 검증된 부품이 아니고, **개별 이상화들이 곱해지므로 실제 마진은 보고된 지배 안전율보다 낮다**(얼마나 낮은지는 여기서 추정하지 않는다).

CLI는 `dae design`.

## Phase 23: 체결·기계요소와 ISO 286 끼워맞춤 (구현 완료)
키·스플라인·압입·용접, 그리고 치수공차. **공차 없는 치수는 사양이 아니다.** 20 mm 구멍에 20 mm 축을 지정하는 것은 만들 수 없는 조립을 지정하는 것이다.

### ISO 286: 표를 베끼지 않고 식으로
표준 공차단위 `i = 0.45·∛D + 0.001·D` [μm], D는 치수구간의 기하평균. 등급은 그 배수다. 손으로 베낀 표는 **아무도 알아채지 못할 오타**를 품을 수 있어 식으로 계산한다.

**⚠️ 하지만 발행된 표를 정확히 재현하지 못하고, 도면에 쓰면 안 된다.** ISO는 **선호수로 반올림한 값**을 표로 낸다. 3~500 mm의 IT6~IT9에 대해 실측한 일치도는 **평균 1.2%, 최악 8.4%**이고, **48개 중 36개만** 발행 정수로 반올림된다. 끼워맞춤을 **비교**하고 압입을 **사이징**하기에는 충분하고, **지정**하기에는 부족하다. 도면에는 표가 필요하다.
처음에 docstring에 "표를 정확히 재현한다"고 적었다가 IT7 @ 6-10 mm에서 **14.37 대 발행 15**가 나와서 정정했다. 측정하지 않고 주장한 것이었다.

**⚠️ 3 mm 이하는 거부한다.** 식은 3 mm 초과에서 정의되고, 그 아래에서는 ISO가 식과 다른 값을 표로 낸다(IT9에서 **13% 어긋남**). 외삽하면 **지정한 것보다 헐거운 공차 쪽으로 조용히 틀린다.**
**닫힌 형태가 있는 편차만 구현했다**: H·h(정의상 0), g·k·n(발행된 간단식). **p·r·s·u는 표로만 주어지는 증분**을 갖고 공식이 없어 **이름을 대며 거부한다.** 근사하면 도면에 틀린 공차가 올라간다.

### 키와 스플라인
**🔑 손검증**: `F = 2T/d = 10000 N`, `τ = F/(bL) = 31.250 MPa`, `σ = F/((h/2)L) = 71.429 MPa`. DIN 6885 / ISO 773 표준 단면(30 mm 축 → 8×7 mm).
**키 높이의 절반만 지지한다**(축 홈에 절반, 허브에 절반). 전체 높이를 쓰면 **지압 용량이 두 배로 부풀어 오른다.**
**⚠️ 길이를 무한정 인정하지 않는다.** 실제 키는 맞물림 양 끝에서 하중을 대부분 받는다(축이 허브에 대해 길이 방향으로 비틀리므로). 축경의 약 1.5배를 넘으면 더 긴 키가 용량을 거의 더하지 않는데, 균일 하중 모델은 **선형으로 계속 인정한다.** 그래서 유효길이를 **캡**한다. 실측: 200 mm 키가 SF 12.6이 아니라 **2.835**(45 mm로 캡).
**키홈이 축에 미치는 영향은 여기 없다.** 재료를 덜어내고 응력집중을 더하는 것은 **샤프트 검사**의 몫이다. 자기 키홈에서 부러지는 축에 든 튼튼한 키는 해답이 아니다.

### 압입 (Lamé)
**🔑 독립 교차검증**: 일반 2재료 Lamé 식이 동일재료·중실축 닫힌형 `p = δ·E·(D²−d²)/(2·d·D²)`로 **상대 1e-12 환원**. 서로 다른 유도가 같은 수에 도달한다.
`T = μ·p·π·d²·L/2`, `F_축 = μ·p·π·d·L`, 허브 보어 후프응력 `σ_θ = p(D²+d²)/(D²−d²)`.
**후프응력은 항상 접촉압력보다 크다**(테스트로 고정). 그래서 **얇은 허브는 압력이 놀라워 보이기 전에 쪼개진다.** 30 mm 축·32 mm 허브에서 80 μm는 견디고(후프 519 MPa 대 항복 655) **120 μm에서 항복한다**(이 지점을 테스트에 숫자로 적었다. 처음 추측한 80 μm는 항복하지 않았다).
**남는 한계**: **표면조도를 차감하지 않는다.** 압입은 조도 돌기를 눌러 평탄화하므로 **압력을 만드는 간섭량은 측정한 간섭량보다 작다**(통상 두 조도합의 1.2배). 작은 끼워맞춤에서는 전체의 큰 부분이다. 따라서 여기 값은 **낙관적**이다. **마찰계수가 체인에서 가장 약한 숫자**이고 토크 용량이 여기에 정비례하므로, 계수가 2배 불확실하면 용량도 2배 불확실하다. 정적 유지만 다루며 프레팅·열에 의한 간섭 상실은 없다.

### 필릿 용접
목두께 `a = 0.707·z`, `τ = F/(a·L)`. 손검증 일치.
**⚠️ 정적 전용이고, 용접 피로는 모재 피로보다 훨씬 나쁘다.** 용접부 내구한도는 모재의 일부분이고 **강재 자체 강도와 거의 무관**하다(형상이 지배). 모재 피로 검사를 용접에 적용하면 수명을 크게 과대평가한다. 이 모듈은 피로를 아예 다루지 않는다.
루트·토우의 응력집중 없음, 목두께까지 완전용입 가정(**루트 융합불량이 흔한 실제 결함**이고 이 계산이 있다고 믿는 목 면적을 없앤다), 용접 잔류응력 무시(작지 않다. 용접부는 항복 근처 인장으로 식는다).

### registry와 정직성
`key_joint`, `press_fit`, `fillet_weld`, `iso_fit` 등록(총 29개 method). 축-허브 연결이 없으면 키·압입이, 용접이 없으면 용접이, 공차가 필요 없으면 ISO 끼워맞춤이 사유와 함께 배제된다.
**미구현 미등록**: 용접 피로, GD&T 공차누적, 프레팅, 나사 스트리핑.
스플라인 하중분담률 0.75는 **[ASSUMED]** 다(치 분할·간격 오차 때문에 모든 이가 균등하게 받지 않으며, 실제 값은 여기가 모르는 가공정밀도에 달렸다). 전부 **SIMULATED**.

CLI는 `dae elements`.

## Phase 24: 재료역학 완성 (구현 완료)
응력변환·비원형 비틀림·압력용기·헤르츠 접촉·응력집중. 이들은 **다른 검사가 쓰는 라이브러리 기본요소**라 전용 CLI를 두지 않았다(샤프트·기어·베어링 검사가 소비한다).

### 응력 상태: 면내 최대전단이 절대 최대전단이 아니다
**🔑 이번 단계에서 가장 중요한 함정이고, 조용하다.** 평면응력 상태는 **제3 주응력이 0**이다. 두 면내 주응력의 **부호가 같으면** 절대 최대전단은 그 0을 포함해 `(σ_max − 0)/2`가 되고, 면내 값 `(σ₁−σ₂)/2`보다 **크다**.
실측: **등이축 100/100 MPa에서 면내 전단은 정확히 0이고 절대 전단은 50 MPa다.** 면내 값을 읽은 설계는 **전단이 아예 없다고 결론 내린다.** 두 값을 모두 반환하고 이름을 붙였다. 부호가 갈리는 상태(120/−40)에서는 둘이 같다는 것도 테스트로 고정했다.
**독립 교차검증**: 2D 닫힌형과 3D 고유값 경로가 주응력·von Mises 모두 일치. 주방향에서 전단이 0인 것도 확인(그것이 주방향의 정의다).

### 비원형 비틀림
`τ = Tr/J`는 **원에만** 적용된다. 비원형 단면은 비틀리면 **면외로 뒤틀리고(warping)** 응력분포가 반지름에 선형이 아니다.
**🔑 실증: 열린 단면과 닫힌 단면은 자릿수가 다르다.** 50 mm 정사각 튜브, 2 mm 벽:
| | τ_max | J |
|---|---|---|
| 닫힌 튜브 | 54.25 MPa | 2.21e-07 m⁴ |
| 길이방향으로 절개 | 1953.12 MPa | 5.12e-10 m⁴ |
**절개가 강성을 432배, 응력을 36배 악화시킨다.** 닫힌 식을 열린 단면에 적용하거나, **이음매·슬릿이 단면을 열린 것으로 만든다는 사실을 잊는 것**이 이 모듈에서 가능한 가장 크고 위험한 오류다.
**자유 뒤틀림을 가정한다.** 끝판을 용접하거나 강체 지지에 물리면 뒤틀림이 구속되고, 그때 생기는 축응력은 비틀림 응력을 넘을 수 있으며 **전혀 모델링하지 않는다.** 실제 구조에서 흔한 구속이다.
사각형 계수는 발행된 표를 보간하고 범위 밖에서는 **외삽하지 않고 클램프**한다(양쪽 다 1/3로 수렴).

### 압력용기
**⚠️ 얇은벽 식의 오차는 "어느 반지름을 쓰느냐"에 따라 자릿수가 달라진다.** 실측:
| r/t | 내반지름 사용 | **평균반지름 사용** |
|---|---|---|
| 2 | 23.08% | 3.85% |
| 5 | 9.84% | 0.82% |
| 10 | **4.98%** | 0.23% |
| 20 | 2.50% | 0.06% |
익숙한 "r/t > 10이면 오차 5%" 지침은 **내반지름 형태에 대한 것**이고, 얇은벽 이론 일반에 대한 진술로 읽으면 틀린다. 여기서 쓰는 평균반지름 형태는 **r/t = 2에서도 4% 이내**다. 이 규칙을 **측정하기 전에 docstring에 적었다가** 숫자가 뒷받침하지 않아 정정했다. 그래서 규칙이 아니라 표를 넣었다.
Lamé 대수 검증: **보어에서 반경응력이 정확히 −p**(참조값 없이 확인 가능한 체크). 후프응력이 보어에서 최대.
**내압 전용이다. 외압은 다른 문제**이고, 외압 용기는 **재료 강도보다 훨씬 낮은 압력에서 좌굴**한다. 여기서 검사하지 않는다. 노즐·헤드·지지부에서 멀리 떨어진 곳에만 유효하며, **실제 용기 규격은 대부분 그 디테일에 관한 것**이다.

### 헤르츠 접촉과 응력집중
**🔑 손검증**: 구-평면 접촉 a·p_max 모두 일치. **하중을 두 배로 해도 최대압력은 26.0%만 오른다**(세제곱근). 접촉응력은 하중에 놀랍도록 둔감하고 **기하가 지배한다.**
**⚠️ 최대전단은 표면이 아니라 표면 아래에 있다**(접촉반경의 약 0.48배 깊이, 최대압력의 약 0.31배). 그래서 **구름접촉 피로는 표면 아래 균열로 시작해 나중에 표면이 떨어져 나가고**, 표면경도만으로 피팅을 막지 못한다.
**Kt는 탄성 집중계수다.** 피로 계산이 원하는 것은 **Kf**(노치 민감도로 줄어든 값)이고 항상 더 작다. Kt를 피로에 직접 쓰면 보수적이고, **연성 재료의 정적 검사에 쓰면 매우 보수적**이다(국부 항복이 피크를 재분배하므로). 어느 쪽도 단순히 틀린 것은 아니지만 **어느 쪽도 검사가 실제로 필요로 하는 값이 아니다.**
구멍 있는 판의 Kt는 d/w → 0에서 **정확히 3.0000**(무한판 고전해)으로 수렴하는 것을 확인했다. 어깨 필렛 Kt는 반지름이 작아질수록 가파르게 올라간다(**날카로운 어깨가 축이 부러지는 자리인 이유**).

### registry
`stress_transformation`, `noncircular_torsion`, `pressure_vessel`, `hertz_contact` 등록(총 33개). 전부 **SIMULATED**.

## Phase 25: 열전달과 열저항망 (구현 완료)
Phase 18은 모터를 **단일 집중 열저항**으로 모델링하고 "장착이 지배하며 어느 방향으로든 2배 틀릴 수 있다"고 적었다. 이 단계가 **그 숫자를 가정이 아니라 조립**할 수 있게 한다.

### 🔑 Biot 수: 집중모델의 딱딱한 유효조건
`Bi = h·Lc/k`는 **몸체 밖으로 나가는 저항 대 몸체 안에서 움직이는 저항**의 비다. 0.1 미만이어야 내부가 사실상 등온이고 집중모델이 성립한다.
**⚠️ 이것은 지침이 아니라 딱딱한 조건이고, 위반해도 계산은 완벽하게 그럴듯해 보인다.** 그래서 **계산해서 보고하고, 가정하지 않는다.**
실측(같은 형상·같은 냉각): 알루미늄 **Bi = 0.00075 (유효)**, 고분자 **Bi = 0.50000 (무효)**. 두꺼운 몸체나 전도가 나쁜 재료에 집중모델을 쓰면 **내부 구배가 실재하는데 단일 온도가 그것을 기술하지 못한다.** 무효일 때도 숫자는 돌려준다(여전히 유용하므로). 다만 **호출자는 통보받았다.**
`τ = R·C` 확인(450 s), 한 시정수 뒤 **63.2%**(= 1−1/e) 도달, 충분한 시간 뒤 정상상태 수렴 확인.

### 🔑 복사는 사람들이 생각하는 온도에서 이미 무시할 수 없다
표면 100 °C, 주위 25 °C, 자연대류 h=10 W/m²K, ε=0.8:
대류 **7.500 W**, 복사 **5.210 W** → **복사가 전체의 41.0%**.
**복사를 빼면 온도상승을 과대예측한다.** 즉 복사 없는 모델은 **안전해 보이면서 부품을 기술하지 않는다.**
**복사는 절대온도로 계산해야 한다**(4제곱). 섭씨로 계산하면 조금 틀리는 게 아니라 **1% 미만으로 터무니없이** 틀린다는 것을 테스트로 고정했다.
선형화 계수 `h_rad = εσ(Ts²+Tsur²)(Ts+Tsur)`가 **평가점에서 4제곱 법칙을 정확히 재현**하는 것을 확인했다(저항망은 선형이라 복사를 넣으려면 선형화가 필요하다). 평가점에서 멀어지면 점점 틀린다.
**⚠️ 방사율은 재료 물성이 아니라 표면 물성이다.** 연마 알루미늄 약 0.05, 양극산화 알루미늄 약 0.8로 **같은 금속에서 16배**다. 마감을 모르고 재료의 방사율을 가정하는 것이 여기서 가장 쉽게 틀리는 방법이다.
**⚠️ 테스트를 하나 고쳤다**: 방사율이 16배 낮으면 복사 **출력**은 정확히 16배 줄지만 **비율**은 그만큼 줄지 않는다(대류 분모가 그대로이므로). 비율에 16배를 적용한 단언은 **산술이 틀렸고** 0.1015 대 0.1000으로 실패했다. 실제로 스케일하는 양을 시험하도록 고쳤다.

### 열저항망
직렬·병렬 조합, 그리고 **지배 저항 식별**. 이것이 망의 가치 대부분이다. **지배항이 서 있는 한 다른 것을 개선해도 거의 아무것도 사지 못한다.**
실증(Phase 18의 주장을 구체화): 권선→철심 0.35, 철심→케이스 0.55, 케이스→브래킷 0.90, **브래킷→공기 1.60** K/W. 총 3.40 K/W, 40 W에서 136 K 상승. **장착이 47%로 지배**한다. 주장이 아니라 숫자다.
병렬 조합이 **항상 가장 작은 가지보다 작다**는 것도 고정했다(망의 유용한 정합성 검사).

### 남는 한계
- **각 가지가 1차원 흐름을 가정한다.** 측면 확산(spreading)을 표현하지 못하는데, **작은 발열원이 큰 방열판에서 작동하는 이유가 바로 확산**이다. 순수 직렬망은 확산을 과소평가해 온도를 과대예측하므로 **안전한 방향이지만 매우 보수적일 수 있다.**
- **부품 간 접촉저항이 없다.** 볼트 접합면의 열저항은 압력·평면도·그리스 유무에 따라 실재하며 **접합하는 부품의 전도저항과 맞먹는 경우가 흔하다.** 빼면 온도를 **과소예측**한다.
- **대류계수가 거의 모든 열 모델의 지배적 불확실성이고 재료 물성이 아니다.** 공기 자연대류 5~25, 강제대류 10~300 W/m²K. h가 2배 불확실하면 답도 2배 불확실하다. `CONVECTION_RANGES`에 **값이 아니라 산포**를 넣어 고르기 전에 폭을 보게 했다.
- 원통 저항이 **두께에 로그**라는 것도 확인했다(선형이 아니다). 여기서 **임계 절연반지름** 효과가 나온다. 작은 파이프에 절연을 더하면 오히려 열손실이 **늘 수 있다.**

### registry
`thermal_network`, `lumped_transient` 등록(총 35개). **미구현 미등록**: CFD, 접촉 열저항, 확산 저항, 열 FEM. 전부 **SIMULATED**.

## Phase 26: 유체역학 (구현 완료)
내부유동·항력·유공압 액추에이터·냉각유량. 로봇 맥락에서 유공압 구동과 냉각 루프에 쓰인다.

### 🔑 층류와 난류 사이에는 유효한 마찰식이 없다
Re < 2300은 층류로 `f = 64/Re`가 **정확하다**(상관식이 아니라 해석적 속도분포에서 나온다). Re > 4000은 난류로 상관식이 필요하다.
**그 사이에서는 유동이 간헐적이고 마찰계수가 Re의 단일값 함수가 아니다.** 거기서 평가한 어떤 식도 물리가 아니라 보간이다. 무언가는 돌려줘야 하므로 난류 상관식을 쓰되 **`friction_is_interpolated`로 표시**한다. 조용히 돌려주면 **보간을 물리로 제시하는 것**이 된다.
실측: 8 mm 관 물 16.83 mL/s → Re 3000 → `transitional`, `interpolated=True`, `certain=False`. 1/3로 줄이면 Re 1000 → `laminar`, `certain=True`.

### 🔑 Haaland를 반복 Colebrook과 대조했다
Colebrook은 f에 대해 **음함수**라 반복이 필요하고, 그래서 명시적 근사식들이 존재한다. 하나를 다른 하나에 대고 재면 "근사적으로"가 숫자가 된다.
Re 1e4~1e7 × 상대조도 0~1e-2 격자에서 **최악 편차 1.344%**. 정확히 일치하지 **않는다**는 것도 테스트로 고정했다(일치를 기대하는 것 자체가 틀린 주장이므로).
**완전거친 영역에서 f가 Re에 무관**해지는 것도 확인했다(조도만으로 결정). 매끈한 관은 Re가 커지면 계속 떨어진다.

### 압력강하
`Δp = f(L/D)(ρV²/2)`, **속도의 제곱**. 유량을 반으로 줄이면 압력강하가 **3.41배** 떨어진다(정확히 4배가 아닌 것은 마찰계수도 Re와 함께 변하기 때문). 냉각 라인을 넉넉히 잡는 것이 그토록 효과적이고, 조금 부족한 것이 보기보다 훨씬 나쁜 이유다.
**"minor loss"는 관습적 이름일 뿐이다.** 피팅이 몇 개 있는 짧은 구간에서는 부차손실이 관마찰을 **넘는다**. minor라고 부르는 것이 그것을 빠뜨리게 되는 경로다. 실증: 0.3 m 구간에 K=6을 넣으면 압력강하가 2배 넘게 뛴다.
**완전발달 유동을 가정한다.** 도달에 지름의 수십 배가 필요하고, 짧은 관이나 피팅이 많은 관은 **입구효과와 피팅이 지배**하지 여기서 계산하는 벽면마찰이 아니다.

### 유공압 액추에이터
**⚠️ P·A는 이론 힘이고 실제 실린더는 그것을 내지 못한다.** 실 마찰과 배기측이 밀어내야 하는 배압이 빠진다. 실제로는 **80~95%**다. 효율을 **명시적 인자**로 두고 기본값 1.0(낙관값)으로 해서 **부재가 보이게** 했다.
**⚠️ 후퇴는 항상 전진보다 약하다.** 로드가 환형 면적의 일부를 차지하므로 같은 압력이 더 작은 면적에 작용한다. 실측: 40 mm 보어·16 mm 로드에서 후퇴가 전진의 **84.0%**. **전진 힘으로 사이징한 실린더가 복귀 행정에서 부족할 수 있다.** 굵은 로드일수록 더 손해라는 것도 고정했다.
유량 `Q = A·v`가 밸브와 라인을 사이징하고, **대개 달성 가능한 속도를 제한하는 것은 힘이 아니라 이 유량**이다.

### 항력과 냉각
`F = ½ρV²C_d A`. 속도를 두 배로 하면 **힘 4배, 동력 8배**(3제곱)임을 고정했다.
**⚠️ 항력계수는 Re 의존성이 강해서 인용된 단일 값은 범위에서만 유효하다.** 구의 익숙한 0.47은 대략 Re 1e3~2e5에서 성립하고, 그 위에서 경계층이 난류로 바뀌면 **약 3배 떨어진다.** 그 전이를 가로질러 단일 값을 쓰면 크고 놀라운 방식으로 틀린다. 그래서 **계수는 호출자가 주고** 여기서는 범위를 진술한다.
냉각 `ṁ = Q/(c·ΔT)`. **필요조건이지 충분조건과는 거리가 멀다**: 냉각수가 열을 실어 나를 수 있다는 말이지 **열이 냉각수로 들어갈 수 있다는 말이 아니다.** 그건 옆 동네의 열저항 문제이고, 유량이 넉넉해도 접촉면이 나쁘면 뜨겁게 돈다. 승온 0을 요구하면 **거부한다**(무한 유량이 필요하고 그건 설계가 아니라 모델링 오류다).

### registry
`pipe_flow`, `external_drag`, `fluid_actuator` 등록(총 38개). **미구현 미등록**: CFD, 압축성 유동, 펌프 곡선 정합, 캐비테이션. 전부 **SIMULATED**.

## Phase 27: ISO 규격 준수 레이어 (구현 완료)
"ISO 준수 설계"는 **두 가지 다른 것**이고 섞으면 안 된다. 하나는 표준에서 가져온 값이 **실제로 그 표준에 있는가**(존재하는 나사 규격, 정의된 볼트 등급, 범위 안의 공차 등급). 다른 하나는 자유 치수가 **선호수 위에 앉아** 재고에서 만들 수 있는가. 둘을 따로 보고한다.

### ⚠️ 준수는 정확성이 아니다
**이것이 이 레이어에서 가장 중요한 한계다.** 표준 나사만 쓰고, 정의된 볼트 등급만 쓰고, 선호 치수만 써도 **duty에 전혀 맞지 않을 수 있다.** 여기서 설계가 작동하는지는 아무것도 평가하지 않는다. **공급자가 알아보는 숫자인지**를 평가할 뿐이다.
API 자체가 그것을 표현하도록 했다: `ComplianceReport`에는 안전율도, 판정도, 해석도 없다(테스트로 고정).

### 🔑 세 가지 상태: "확인 불가"는 통과가 아니다
- `STANDARD`: 표준에 있음을 확인했다.
- `NON_STANDARD`: 표준에 **없음을 확인**했다.
- `NOT_CHECKABLE`: **이 프로젝트에 표가 없다.**
M7 나사는 우리 표에 없다. 그건 **표에 대한 진술이지 ISO에 대한 진술이 아니다.** `NON_STANDARD`로 보고하면 코드가 뒷받침할 수 없는 주장이 되고, `STANDARD`로 보고하면 **준수를 날조**하는 것이다. 그래서 세 번째 상태가 있고, **`fully_conformant`는 확인 불가가 하나라도 있으면 거짓**이다. 확인 불가는 통과가 아니다.

### 🔑 스냅 방향은 필수 인자다. 보편적으로 안전한 방향이 없기 때문이다
치수를 반올림하는 것은 **설계 변경**이다.
실측: 37.4 mm를 R20에 스냅하면 **UP → 40.00 mm, DOWN → 35.50 mm, NEAREST → 35.50 mm**(내려간다).
하중을 받는 두께라면 **아래로 내리는 것은 재료를 없애 그 치수를 정한 검사를 무효화**한다. 틈새라면 **위로 올리는 것이 열려 있어야 할 간극을 닫는다.** 그래서 방향에 보편적으로 옳은 기본값이 없고, **호출자가 자기 양에 대해 어느 쪽이 보수적인지 진술**한다. 자유 치수 검사의 기본값은 UP인데, 이 프로젝트의 자유 치수 대부분이 하중을 받기 때문이다.
**스냅은 제안이지 편집이 아니다.** 리포트는 치수가 무엇이 될지 말하고, 적용은 호출자의 결정이다. 치수를 옮기면 그것을 만들어낸 해석이 무효가 될 수 있고 **어느 방향이 보수적인지는 호출자만 안다.**

### ⚠️ 발행된 선호수는 기하급수가 아니라 그것을 반올림한 것이다
R10은 명목상 10의 10제곱근(**1.25893**)씩 가지만 표준은 **1.25**라고 적는다. 급수를 계산해서 그대로 쓰면 **표준을 따르는 것처럼 보이면서 비표준 숫자를 도면에 올린다.** 그래서 발행값을 쓰고 반올림 편차를 **측정**했다: R5·R10 **0.95%**, R20 **1.22%**, R40 **1.26%**. 0이 아니므로 구별이 의미 있다.
**R5의 마지막 값은 8.00이 아니라 6.30**이다(다섯 단계면 마지막이 10^(4/5)). 모든 급수에 성립하는 검사는 "한 단계 더 가면 10에서 닫힌다"는 것이고, 이것이 급수가 깨끗하게 반복되는 이유다. 처음에 R10의 끝값을 모든 급수에 단언했다가 R5에서 실패해 고쳤다.
거친 급수가 고운 급수에 **포함**되는 것(R5 ⊂ R10 ⊂ R20 ⊂ R40)도 고정했다. 그것이 이들을 하나의 족으로 만든다.

### 참조하는 표준
ISO 261/262(미터 보통나사, Phase 23의 응력단면적), ISO 898-1(볼트 등급, Phase 19), ISO 286(끼워맞춤, Phase 23. 3 mm 이하와 500 mm 초과는 확인 불가로 보고), DIN 6885/ISO 773(키 단면, Phase 23), ISO 3(선호수).
**표준 치수가 보편적으로 재고되는 것은 아니다.** 급수는 어떤 치수가 선호되는지 말하지 공급자가 무엇을 갖고 있는지 말하지 않는다. 스냅은 만들 수 있을 **가능성을 높이지 확실하게 만들지 않는다.**

전부 **SIMULATED**.

## Phase 28: 다중설계검토, 최소치수, 연성해석 (구현 완료)

### 🔑 최소치수: 이분법이 기대는 가정을 검사한다
`evaluate(치수) -> 안전율`을 이분해 목표를 만족하는 최소 치수를 찾는다. 손검증: 굽힘 축이 `d = (n·M·32/(Sy·π))^(1/3)`와 상대 1e-4 일치.
**⚠️ 이분법은 안전율이 치수에 대해 단조라는 가정에 기댄다. 그건 탐색이 아니라 물리에 대한 가정이다.** 강도에서는 대체로 성립한다(두꺼운 벽이 강하고 굵은 축이 뻣뻣하다). **항상은 아니다.**
**측정한 반례**: 외형이 고정된 단면에서 벽을 키우면 강성도 늘고 **질량도 는다**. 외팔보 1차 고유진동수에서는 **질량이 이긴다**: 40 mm 단면 0.5 m에서 벽이 1 mm→20 mm로 갈 때 진동수 여유가 **3.60에서 2.61로 떨어진다**. 이분법은 거기서도 근을 찾고 **그건 최소가 아니다.** 그래서 탐색 전에 구간을 **샘플링해 가정을 확인**하고, 결과에 플래그를 실어 보낸다.
**처음 든 반례는 틀렸다**: 단면계수는 공동이 닫히면서 **평평해지지만 감소하지는 않는다**(여전히 단조). 그 사실도 테스트로 남겼다.
**⚠️ 최소치수는 제약 경계에 정확히 앉고, 그것은 정의상 마진이 0이라는 뜻이다.** 그게 "최소"의 의미이고, **그 자체로는 설계가 아니다.** 모델링 오차·제조 편차·가정을 넘는 하중 중 어느 하나에도 넘어간다. 그래서 목표 안전율이 인자이고 **기본값이 1.0이 아니라 1.5**다(1.0을 기본으로 주면 그렇게 하도록 부추긴다).
**요소별 최소화는 국소적 결과다.** 요소들은 결합돼 있다(가벼운 링크 → 더 작은 토크 → 더 작은 샤프트 → 더 가벼움). 한 번의 통과로는 그 루프가 닫히지 않으므로 결과는 **개별적으로 최소인 부품들의 집합**이지 최소인 어셈블리가 아니다.
도달 불가한 목표는 **`converged=False`로 보고**한다(구간 상단을 조용히 돌려주면 목표를 만족하는 설계처럼 보인다).

### 🔑 다중설계검토: 기준들이 불일치하고, 그 불일치가 출력이다
**순위에는 진술된 기준이 필요하고 중립적인 기준은 없다.** 지배 안전율로 줄 세우면 **가장 보수적인** 설계를 고르고, 질량으로 줄 세우면 **가장 가벼운**(대개 파손에 가장 가까운) 설계를 고른다.
실증(4개 설계):
| 기준 | 1위 |
|---|---|
| 지배 마진 | steel |
| 질량 | aluminium |
| 비용 | steel |
| 미검토 최소 | aluminium |
**기준들이 일치하지 않는다.** 단일 승자를 보고하면 **답이 무엇을 맞바꾸느냐에 달렸다는 사실을 감춘다.** `disagreement()`가 각 기준이 무엇을 고르는지 보여준다.
**FAIL한 설계는 순위에 넣지 않고 거부 목록에 넣는다.** 마진으로 정렬해 목록에 끼워 넣으면 **단지 더 나쁘다**는 위치를 암시하는데, 그것은 애초에 채택 가능하지 않다.
**설계들은 둘 다 실제로 돌린 검사에서만 비교 가능하다.** 한쪽은 피로를 평가했고 다른 쪽은 안 했다면 같은 축 위의 두 점이 아니다. 그래서 미검토 개수가 각 설계와 함께 다닌다.

### 연성해석: 단방향이고, 그것이 이 모듈의 요점이다
열해가 온도를 정하고, 온도가 열응력을 만들고, 열응력이 기계응력에 합쳐진다. **반대 방향은 없다.**
**⚠️ 실제 결합은 단방향이 아니다.** 구조의 변형이 접촉면 압력을 바꾸고, 그것이 접촉 열저항을 바꾸고, 그것이 변형을 일으킨 온도를 바꾼다. 그 루프를 닫으려면 수렴까지 반복해야 하고 **구현하지 않았다.**
**⚠️ 단방향 결합은 알려진 방향으로 보수적이지 않다.** 마운팅으로 팽창해 들어가는 부품은 접촉면적을 얻어 예측보다 **시원하게** 돌고, 방열판에서 휘어 떨어지는 부품은 접촉을 잃어 **뜨겁게** 돈다. 어느 쪽인지는 기하가 정하고 여기서는 구별할 수 없으므로 **오차에 믿을 만한 부호가 없다.**
**중첩은 선형탄성이라야 정당하다.** 무언가 항복하는 순간 둘을 더할 수 없다(열응력은 재료가 흐르면서 완화되고 기계응력은 아니다).
**실증: 결합 결과에는 크기만이 아니라 방향이 있다.** 구속된 부품을 가열하면 **압축**이 되어 인장 기계응력을 **덜어낸다**. 그래서 안전율이 열이 기계를 상쇄하는 지점에서 **최댓값**을 갖고 그 너머에서 다시 떨어진다(10 W에서 SF 3.19 → 40 W에서 **16.34** → 80 W에서 3.63). 크기만 다루는 모델은 이것을 표현할 수 없다.
**열응력은 운전온도가 아니라 조립 기준온도와의 차이에서 나온다.** 뜨겁게 조립해 뜨겁게 도는 부품에는 열응력이 **없다**. 운전온도를 쓰면 있지도 않은 응력을 발명하게 되고, 그 구별을 테스트로 고정했다.

전부 **SIMULATED**.

## CalculiX 외부 FEA 노드: 독립 솔버와의 교차검증 (구현 완료)
지금까지 모든 FEM 결과는 **보 이론, 패치 테스트, 또는 자기 자신**에 대고 확인했다. 그것들은 많은 것을 잡지만 **요소 정식화에 대한 공유된 오해**는 잡지 못한다. CalculiX는 포트란·C로 **독립적으로 작성된** 솔버이고 다른 선택을 한 사람들이 만들었으므로, 그것과 일치하는 것은 **다른 종류의 증거**다.

### 설치: sudo 없이, 시스템을 건드리지 않고
배포판 패키지를 받아 **사용자 prefix에 풀었다**(`apt-get download` + `dpkg-deb -x` → `~/.local/opt/calculix`). 누락된 것은 `libspooles`뿐이라 같은 방식으로 받았고, `LD_LIBRARY_PATH`를 설정하는 런처를 `~/.local/bin/ccx`에 뒀다. **`/usr`에 아무것도 설치하지 않았고** 총 7.1 MB다. `ccx -v` → **Version 2.17**.

### 🔑 교차검증 결과
같은 메시·같은 재료·같은 경계·같은 하중을 두 솔버에 넣었다. 우리 정식화는 Wilson 비적합 모드이고 CalculiX의 대응 요소는 **C3D8I**다.
| 문제 | 전 절점·전 자유도 최대 상대오차 | 최대 von Mises 상대오차 |
|---|---|---|
| 세장 캔틸레버 20:1 (640요소) | **1.130e-07** | **0.000%** |
| 3D 블록 3.2:1 (288요소) | **2.541e-07** | **0.000%** |
| L-bracket 재진입 모서리 (312요소) | **8.077e-08** | **0.000%** |
**독립적으로 작성된 두 솔버가 유효숫자 7자리로 일치한다.** 팁 처짐 하나가 아니라 **모든 절점의 모든 자유도**에서다. 스칼라 하나의 일치는 운이거나 오차 상쇄일 수 있지만, 변위장 전체와 응력의 일치는 요소 정식화에 대한 증거다.

### 🔑 대조군: 이 비교가 차이를 감지할 수 있음을 보였다
**모든 요소가 일치했다면 이 비교는 차이를 감지할 능력이 없다는 뜻이고 일치가 아무것도 증명하지 못한다.** 그래서 완전적분 **C3D8**과도 비교했다:
| 세장비 | C3D8이 과도하게 뻣뻣한 정도 |
|---|---|
| 20:1 | **+10.72%** |
| 8:1 | +3.03% |
| 4:1 | +1.50% |
**전단 잠김(shear locking)이고**, 세장할수록 심해지는 것이 그 서명이다. Phase 2에서 비적합 모드를 넣은 이유가 정확히 이것이며, **C3D8과 비교했다면 양쪽 솔버의 버그가 아닌 그 요소의 성질을 보고 놀랐을 것이다.**
세장 극한에서 두 솔버가 모두 오일러 보 이론에 **2% 이내**로 수렴하는 것도 확인했다(독립 구현 둘이 세 번째 해석해로 수렴).

### ⚠️ 이것이 뜻하지 않는 것
- **CalculiX도 시뮬레이션이지 물리시험이 아니다.** 일치는 "이 구현이 확립된 솔버와 같은 답을 낸다"는 뜻이지 **"답이 현실과 같다"가 아니다.** 일치한 설계도 여전히 만들어진 적이 없다.
- **두 솔버가 모델링 가정을 공유하므로 공통원인 오류는 살아남는다.** 둘 다 선형탄성·미소변형이고 같은 메시·같은 요소족이다. **가정 자체가 문제에 틀렸다면 둘이 함께 틀리고 아름답게 일치한다.** 이것은 **구현**을 검사하지 **모델**을 검사하지 않는다.
- **덱을 우리 메시에서 생성하는 것은 의도적이고 제한적이다.** 같은 절점·요소를 쓰면 솔버와 요소 정식화를 이산화에서 분리해 볼 수 있다. 대신 **메싱 오류는 보이지 않고**, 두 해석자가 독립적으로 얻을 일치보다 훨씬 타이트하다.
- **CalculiX에도 자체 가정과 기본값이 있다.** 확립되었다는 것이 기준 표준이라는 뜻은 아니다. **다른 구현의 두 번째 의견**이다.

### 검증 상태 승격
`self_fem_only` → **`cross_validated`**. 승격에는 **측정값이 함께 따라간다**(`CrossValidation`이 변위·응력 상대오차와 허용오차를 들고 다닌다). 측정 없는 "교차검증됨"은 주장이고, 두 번째 솔버를 돌리는 목적 전체가 믿음 대신 수치를 갖는 것이다.
**불일치는 조용히 강등하지 않는다.** 사유에 측정된 오차를 실어 무엇이 실패했는지 보이게 한다.
**🔑 어떤 양의 솔버 일치도 물리검증에 도달하지 못한다.** `is_physically_validated`는 **항상 False**이고, 부재를 알아채야 하는 대신 명시적인 "아니오"를 받도록 프로퍼티로 뒀다. 시뮬레이션 둘이 일치하는 것은 하나보다 강한 증거이고 **여전히 측정이 아니다.**
**이름에 대한 주의도 적었다**: 기존 `EXTERNALLY_VERIFIED`는 실제보다 많은 것을 약속한다. 그것이 쓰이는 퓨전 노드도 FEA를 돌리고 **그것도 시뮬레이션**이다. 이 사다리는 **몇 개의 독립 구현이 일치하는지**에 관한 것이지 현실과의 접촉에 관한 것이 아니다.

registry에 `analysis.fea.calculix`를 **available=True로** 등록했다(퓨전 stub과 달리 실제 동작하며, 가용성을 플래그가 아니라 **파일시스템에서** 보고한다). 총 41개 capability.

---

## OpenFOAM 외부 CFD 노드: 유체 상관식의 첫 독립 검증 (구현 완료)
`physics.fluids`의 모든 결과는 **닫힌 형태 상관식**이었다. 이 프로젝트는 Navier-Stokes를 **한 번도 푼 적이 없으므로** 유체 쪽에는 독립적인 확인이 **전혀 없었다**. 마찰계수의 부호 오류 하나가 조용히 전파될 수 있었다. OpenFOAM은 독립적으로 작성된 유한체적 솔버이고, **정답을 아는 유동**에 대고 셋을 나란히 비교했다.

### 설치 (sudo 없음, Docker 없음)
배포판 .deb 두 개(`libopenfoam`, `openfoam`, 합계 **64.4 MB 다운로드**)를 `apt-get download`로 받아 `dpkg-deb -x`로 `~/.local/opt/openfoam`에 풀었다(**전개 후 415 MB**). 시스템 경로는 건드리지 않았고 `/usr/bin/blockMesh`는 여전히 없다. MPI는 시스템에 이미 있었다. 배포는 **ESI(openfoam.com) 계열 v1912 patch 200626**, 우분투 jammy universe. 노드는 래퍼 스크립트에 의존하지 않고 `WM_PROJECT_DIR`·`FOAM_ETC`·`LD_LIBRARY_PATH`를 **스스로 구성한다**.

### 🔑 두 기준이 사실은 같은 식이었다
셋을 비교하려다 발견한 것이다. 우리 `solve_pipe_flow`는 층류 압력강하를 **Darcy-Weisbach + f = 64/Re**로 계산한다. 여기에 `f = 64ν/(VD)`를 대입하면

    dp = f (L/D) ρV²/2 = 32 μ L V / D²

즉 **Hagen-Poiseuille 그 자체**다. 우리 공식과 "해석해 기준"은 **한 식을 두 가지로 쓴 것**이고, 둘을 비교하면 **대수 전개가 맞았다는 것만** 증명된다. 측정값도 그렇게 나왔다: **2.22e-14 %** 일치. 세 기준 중 **독립적인 것은 OpenFOAM 하나뿐**이며, 코드와 문서가 이것을 순환논증으로 제시하지 않고 그대로 말한다.

### 유효범위를 먼저 적고 구현했다
전부 정상·비압축·뉴턴·등온·층류·완전발달이다. 유동방향에 **cyclic 경계**를 써서 "완전발달"이 근사가 아니라 **정확**해지고 입구길이(`L_e = 0.05 Re D`)가 문제에서 사라진다.

| 벤치마크 | 해석해 | 유효범위 |
|---|---|---|
| 평면 Couette | `u = U y/h` | 무한 평행평판, 압력구배 없음 |
| 평면 Poiseuille | `u = (G/2ν) y(h−y)` | `Re = V(2h)/ν < 2300` |
| Hagen-Poiseuille 관 | `u = (G/4ν)(R²−r²)`, `V = GR²/(8ν)` | `Re = VD/ν < 2300` |

### 일치 수치

| 케이스 | 격자 | Re | 결과 |
|---|---|---|---|
| 평면 Couette | 20셀 | 1000 | 최대 절대오차 **1.5e-11 m/s** (1.5e-9 %) |
| 평면 Poiseuille | 20셀 | 16.7 | 오프셋 **3.125000e-05**, 예측 **3.125000e-05** |
| Hagen-Poiseuille 관 | 80셀, 웨지 1° | 313 | 평균속도 **+0.0002 %** |
| 3자 대조 (관, 압력강하) | | | OpenFOAM 0.500000000 Pa / 해석해 0.500000977 Pa / 우리 0.500000977 Pa |

### 이산화 오차를 재기 전에 예측했다
"나온 숫자를 기록하는" 테스트는 옳은 이유로 실패할 수 없다. 그래서 두 오차를 **닫힌 형태로 먼저 예측하고** 그 예측을 시험했다.

**벽면 오프셋.** 포물선의 벽면 면 구배를 한쪽으로만 잡으므로 프로파일 **전체가** `Δy²|u''|/8` 만큼 평행이동한다. 모양은 변하지 않는다. 20·40·80셀 세 격자에서 **여섯 자리까지** 예측과 일치했고(3.125000e-05 → 7.812500e-06 → 1.953125e-06), 프로파일 내 편차는 **1e-18**, 즉 순수한 오프셋이다. 2차 수렴.

**웨지 각도 (대조실험).** 축대칭 웨지의 바깥 면은 호가 아니라 **현**이므로 벽이 중앙 방위각에서 `R cos(θ/2)`에 놓인다. Poiseuille 속도는 `R²`에 비례하므로 평균속도가 약 **`θ²/4`** 만큼 낮게 나온다. 이 오차는 **반경방향 격자를 아무리 세분해도 줄지 않는다**.

| 웨지 각 | 측정 | 예측 `−θ²/4` |
|---|---|---|
| 5.0° | −0.1825 % | −0.1904 % |
| 2.5° | −0.0398 % | −0.0476 % |
| 1.0° | +0.0002 % | −0.0076 % |

처음에 반경 20 → 40 → 80셀로 세분했을 때 오차가 −0.066 % → −0.159 % → −0.183 %로 **커졌다**. 세분이 반경방향 오차(부호가 반대)를 걷어내면서 **기하 오차가 드러난 것**이다. 이것을 "솔버 불일치"로 보고했다면 틀렸을 것이다. OpenFOAM 튜토리얼 관례인 5°를 그대로 썼다면 0.19 %를 영원히 안고 갔을 것이므로, 노드는 **1°를 기본값으로** 한다.

### ⚠️ 이것이 뜻하지 않는 것
- **OpenFOAM도 시뮬레이션이지 풍동이 아니다.** 자체 이산화 오차와 자체 층류 종결을 갖는다. 일치는 **같은 수학의 두 구현이 일치한다**는 뜻이다.
- **케이스는 층류가 결과가 아니라 구성이다.** 평면 케이스는 **한 셀 깊이**이므로 실제 천이를 일으키는 3차원 교란이 **존재할 수 없다**. 평면 Couette는 모든 Re에서 선형안정이지만 실험에서는 Re ≈ 360 부근에서 난류가 된다. 우리 케이스는 **Re = 1000에서 완벽히 층류**인데, 이것은 **격자의 성질이지 물리적 발견이 아니다.**
- **두 해가 가정을 공유한다.** 비압축·뉴턴·매끈한 강체벽·입구효과 없음. 전부 **함께** 틀릴 수 있다.

registry에 `analysis.cfd.openfoam`을 **available=True로** 등록했다. 새 gate `flow_is_laminar`를 추가했는데, 난류 관에 층류 종결로 돌린 CFD는 **실패하는 대신 자신 있는 오답을 돌려주기** 때문이다. 규정하지 않으면 **닫히는 쪽으로** 실패한다(층류로 기본값을 두지 않는다). 난류에서는 CFD 교차검증이 사라지고 해석식만 남는다.

검증 상태에는 `FlowCrossValidation`을 따로 뒀다. 구조용 `CrossValidation`을 넓히지 않은 이유는, 유동 비교에는 **변위도 요소 타입도 없고** 해당 없는 필드를 들고 다니는 기록은 **측정한 적 없는 값으로 채워달라는 초대장**이기 때문이다. 규칙은 같다: 측정값이 주장과 함께 이동하고, 불일치는 조용히 강등하는 대신 잰 오차를 사유에 싣는다. `is_physically_validated`는 여기서도 **항상 False**다.

---

## Pinocchio 다물체 검증 노드: 연속체 밖의 첫 외부 검증 (구현 완료)
지금까지 외부 검증은 **연속체만** 덮고 있었다(구조는 CalculiX, 유체는 OpenFOAM). 다물체 운동학·동역학은 **2링크 닫힌형식과 손검증**으로만 확인했는데, 그것들은 많은 것을 잡지만 **검사 대상과 저자를 공유한다**. Pinocchio는 INRIA에서 독립적으로 작성됐다.

### 모델을 옮겨 적지 않고 내보냈다
`core/assembly/urdf.py`를 추가해 Assembly를 **URDF로 내보내고 다시 읽어들인다**. 두 번째 서술을 손으로 옮겨 적으면 **오타를 일치로 착각하는** 방식으로 비교가 무의미해진다. 내보내는 것과 내보내지 않는 것을 구현 전에 먼저 적었다:
- **정확히 매핑**: 링크·관절 이름, 부모/자식, revolute·prismatic, 축, 4x4 원점(URDF의 xyz + 고정축 rpy), 위치 한계, 질량, 질량중심, 링크 로컬축 기준 관성텐서. 동역학이 여기서만 나오므로 비교가 이 위에 선다.
- **이 프로젝트에 없으므로 사실로 내보내지 않음**: effort·velocity 한계. URDF가 요구하지만 이 프로젝트는 둘 다 모델링하지 않는다(모터 토크는 드라이브트레인 선정에서 나오고 Assembly의 일부가 아니다). **그럴듯한 숫자 대신 일부러 터무니없는 1e9**를 쓰고, 생성된 파일 안 주석에 설계 한계가 아니라고 적는다.
- **전혀 매핑 안 함**: 시각·충돌 형상. 파라메트릭 단면이라 CAD 경로의 두 번째 구현이 되고, 둘 중 어느 쪽이 틀렸는지 드러나지 않은 채 어긋날 수 있다.
- **구조 요건**: URDF는 트리다. 닫힌 루프는 **Assembly 모델이 먼저 거부**하므로 이 모듈까지 오지 못한다. 그래도 다시 검사한다. 관절을 조용히 빠뜨린 내보내기는 **돌아가면서 틀린 모델**을 만들기 때문이다.

### 🔑 URDF가 담지 못하는 것 하나가 이 비교 전체를 무의미하게 만들 수 있었다
이 프로젝트는 **y-up이고 중력이 -y**다. URDF에는 중력 방향을 적는 자리가 없고 Pinocchio 기본값은 **z 아래**다. 그냥 두면 **평면 팔의 중력 토크가 0으로 나오는데도 그럴듯해 보인다**. 노드가 중력을 명시적으로 설정하고, 테스트가 **대조실험**으로 이것을 실증한다: z 아래 중력을 넣으면 Pinocchio의 G가 0이 되고 우리 값은 0.1 N·m를 넘으므로, 실수가 있었다면 **총체적 불일치로 드러난다**. 즉 통과는 "중력이 마침 안 중요했다"가 아니라 "규약이 실제로 건너갔다"는 뜻이다.

### 일치 수치 (여섯 종)

| 케이스 | FK | J | M | G | C | RNEA | 토크 규모 |
|---|---|---|---|---|---|---|---|
| 2링크 nominal | 5.6e-17 | **0** | 6.9e-18 | 1.4e-17 | 1.5e-12 | 1.0e-12 | 0.5721 |
| 2링크 folded | 1.1e-16 | 5.6e-17 | 3.5e-18 | 5.6e-17 | 7.1e-13 | 1.4e-12 | 0.3140 |
| 2링크 fast | 1.1e-16 | 2.8e-17 | 6.5e-19 | 1.1e-16 | 2.6e-12 | 1.1e-11 | 0.3428 |
| 3링크 | 1.1e-16 | **0** | 6.9e-18 | 2.2e-16 | 2.4e-12 | 3.5e-12 | 1.0814 |

자세 하나는 사인이 사라지는 곳에서 **우연히** 맞을 수 있으므로 다섯 상태로 돌린다. 2자유도가 감출 수 있는 인덱싱 오류를 위해 **3링크로도** 확장했다. Phase 11에서 손검증했던 M/C/G가 독립 라이브러리와도 맞았다.

### 유일하게 느슨한 항을 진단했다
C만 1e-12대이고 나머지는 1e-16 이하다. **버그가 아니라 우리가 수치미분하기 때문**이다. 이 프로젝트는 C를 **중심차분한** 질량행렬의 Christoffel 기호로 만들고 Pinocchio는 해석적으로 미분한다. 주장하지 않고 **쟀다**: step 1e-2, 1e-3, 1e-4에서 오차/step²가 **1.5985e-04로 일정**하다. 정확히 2차, 즉 절단오차다.

측정하면서 딸려 나온 것: 기본 `CHRISTOFFEL_STEP = 1e-6`은 **최적점을 지나쳐 있다**. 1e-5에서 8.3e-14인데 1e-6에서 1.5e-12로 **17배 나쁘다**(반올림오차가 지배하는 영역). 절대오차가 어떤 물리적 불확실성 옆에서도 무의미한 크기라 상수는 **바꾸지 않았고**, 범위를 벗어나 조용히 고치는 대신 여기 적어 둔다.

### ⚠️ 이것이 뜻하지 않는 것
- **Pinocchio도 측정이 아니다.** 그리고 **우리와 똑같이 이상화한다**: 강체, 이상적 무마찰 관절, 백래시 없음, 유연성 없음. 일치가 반올림 수준으로 나오는 이유가 바로 그것이고, 동시에 일치가 **실제 기구에 대해 아무 말도 못 하는** 이유이기도 하다. 실제 기구에서는 마찰과 컴플라이언스가 오차를 지배하는데 **양쪽 다 그것을 모델링하지 않는다.**
- **접촉·마찰 동역학은 여기 범위 밖**이고 별도 노드(MuJoCo/PyBullet) 몫으로 로드맵에 남긴다.

registry에 `analysis.multibody.pinocchio`를 available=True로 등록했다. 새 gate `has_articulated_chain`을 추가했는데, 브래킷 하나에는 질량행렬이 없고 그리로 라우팅하면 **거절하는 대신 0x0 답**이 나오기 때문이다. 규정하지 않으면 닫히는 쪽으로 실패한다.

---

## Gmsh 메싱 검증 노드: CalculiX가 구조적으로 못 보던 자리 (구현 완료)
CalculiX 노드는 구멍 하나를 **의도적으로** 열어 뒀다. 덱을 **우리 메시에서** 생성하므로 솔버와 요소 정식화는 분리해 볼 수 있지만 **메싱 오류는 보이지 않는다**. 두 솔버가 **틀린 형상을 놓고 아름답게 일치**할 수 있다. Gmsh가 그 자리를 메운다. 메시를 독립적으로 생성하고, 형상을 셀을 세는 대신 **독립 CAD 커널(OpenCASCADE)로 측정**한다.

### 유효범위를 먼저 적었고, 좁다
우리 솔버는 **구조화·균일 격자 위의 8절점 hex**다. `solve_linear_elasticity`가 `mesh.dx, dy, dz`로 **요소강성행렬을 하나만** 만들어 모든 요소에 재사용한다. 여기서 두 가지가 따라 나오고 둘 다 Gmsh의 한계가 아니다.
- **먹을 수 있는 것**: 축정렬·균일 hex. Gmsh가 박스·압출형에서 transfinite + recombine으로 만들어 준다.
- **못 먹는 것, 그리고 된다고 말하지 않는다**: **사면체**. 일반 형상에서 Gmsh가 내놓는 것이 이것이고, 우리에게 **사면체 요소가 없으므로 아예 돌릴 수 없다**. 등급 격자(graded)도 마찬가지다. 모든 요소가 강성행렬 하나를 공유한다는 것이 바로 등급 격자가 깨뜨리는 가정이다. 둘 중 어느 쪽도 "조금 나쁜 답"이 아니라 **틀린 답**이 된다.

그래서 이 노드의 쓸모는 "아무 형상이나 메싱"이 아니다. **솔버가 먹을 수 있는 유일한 메시족을 독립 생성해 우리가 같은 것을 만드는지 확인하고, 우리가 근사하는 형상을 정확히 재는 것**이다. 이 경계는 주장이 아니라 **측정**해 뒀다: 테스트가 구를 메싱해 결과가 사면체임을 확인한다.

### 우리 메시 생성기가 독립적으로 확인됐다
Gmsh가 만든 메시를 우리 `Mesh`로 변환할 때 **Gmsh의 절점 순서를 믿지 않고 기하학적으로** 재정렬한다(각 요소의 최소 코너 기준 위치로 식별). 순서 규약이 조용히 바뀌면 **뒤집히거나 비틀린 요소**가 되는데, 그것은 오류가 아니라 **그럴듯한 틀린 강성**으로 나타난다.

절점 번호가 서로 다른데도 FEM 결과가 일치한다는 것은 **연결성이 같은 고체를 서술한다**는 뜻이다.

| 격자 | 우리 처짐 | Gmsh 메시 처짐 | 상대차 |
|---|---|---|---|
| 8x3x2 | -2.303238540483e-05 | -2.303238540483e-05 | 2.8e-14 |
| 16x6x4 | -2.342043181137e-05 | -2.342043181137e-05 | 5.4e-14 |

### 🔑 L-브래킷이 요청한 것과 다른 브래킷을 메싱하고 있었다
`hollow_rect_mesh`의 체적은 모든 해상도에서 **정확**했다(OCC 대비 1e-15). 벽두께가 셀 경계에 정확히 떨어지도록 설계돼 있어서 재료를 잃지 않는다.

`l_bracket_mesh`는 다르다. 팔 두께를 `round(n * thickness_fraction)` **셀 단위로 반올림**하므로 `n * thickness_fraction`이 정수가 아니면 **요청한 것과 다른 두께의 브래킷**이 만들어진다.

| n | thickness_fraction | 요청 두께 | 실제 메싱된 두께 | 체적 오차 |
|---|---|---|---|---|
| 20 | 0.40 | 0.040000 | 0.040000 | 2.1e-16 |
| 10 | 0.35 | 0.035000 | 0.040000 | **+10.8 %** |
| 12 | 0.30 | 0.030000 | 0.033333 | **+8.9 %** |
| 10 | 0.25 | 0.025000 | 0.020000 | **-17.7 %** |
| 11 | 0.30 | 0.030000 | 0.027273 | **-7.6 %** |

이것이 **CalculiX가 볼 수 없는 오류의 정확한 종류**다. 덱이 이 메시에서 나오므로 두 솔버는 **틀린 형상 위에서 완벽히 일치**한다. 반올림된 두께를 넣으면 체적 오차가 1e-12로 사라지므로 원인도 완전히 설명된다.

**이제 지을 수 없는 요청은 예외를 던진다.** 벤치마크 형상이 조용히 달라지는 것이 위험하기 때문이다. 오류 메시지는 거절만 하지 않고 **실제로 지어질 두께와, 요청을 정확히 지을 수 있는 n 값들을 함께** 알려준다(추측 게임을 남기지 않는다). 의도적인 스냅은 `allow_snapping=True`로 여전히 가능하고, `realised_arm_thickness()`가 그때 무엇이 지어질지 알려준다.

기존 호출자 셋이 전부 스냅되고 있었다는 점이 이 변경의 값어치를 보여준다: `test_calculix`(n=16, 0.4 -> 6.4셀), `test_topology_stress`(n=8 -> 3.2셀), 그리고 **CLI 기본값**(nx=32 -> 12.8셀)까지. 앞의 둘은 정확한 두께가 비교에 무관하므로 호출부에서 **명시적으로** 스냅을 받아들이도록 고쳤고(이제 콜사이트에 보인다), CLI는 사용자가 고른 격자를 거절하는 대신 스냅하되 **실제로 메싱된 두께를 출력한다**.

### ⚠️ 이것이 뜻하지 않는 것
- **Gmsh는 메셔이지 실제 부품의 측정이 아니다.** 체적 일치는 **같은 이상화된 고체의 두 서술이 일치한다**는 뜻이다. 실제로 가공된 부품에는 공차·필렛·공구 자국이 있고 **양쪽 다 그것을 서술하지 않는다.**

registry에 `meshing.gmsh`를 available=True로 등록했다.

---

## 순동역학 + MuJoCo 접촉 노드: 이상관절 밖으로 (구현 완료)
Pinocchio 노드는 **이상적 무마찰 관절**의 열린 사슬을 검증한다. 실제 기구가 이상적이기를 그만두는 바로 그 지점, **접촉과 마찰**을 이 노드가 덮는다. 그리고 이건 **로봇 전용이 아니다**. 등록한 capability는 임의 강체 다물체계의 접촉이다(링키지, 낙하 부품, 경사면 블록, 지그에 안착하는 조립품).

### 순동역학
`M(q) q̈ = τ − C(q,q̇)q̇ − G(q)`를 풀고 적분한다. Pinocchio의 ABA와 **상대오차 1e-12 이하**로 일치한다(가속도가 수백 rad/s²에 이르는 상태에서 최악 1.0e-12).

적분기는 둘이고 **에너지에 대한 취급이 서로 다르다**. 무구동·무마찰이면 정확해는 에너지를 정확히 보존하므로, 적분기가 에너지를 어떻게 다루느냐가 긴 실행의 의미를 정한다.
- **rk4**: 4차, 비심플렉틱. 고정 창에서 최대 에너지 편차가 스텝을 절반으로 줄일 때마다 **16.0배, 14.5배**로 떨어진다(예측 16배). 지표를 `E(end)−E(0)`이 아니라 **`max|E(t)−E0|`**로 쓴 이유는 전자가 진동의 어느 위상에서 멈추느냐에 좌우되어 차수 측정에 부적합하기 때문이다. 실제 궤적이 쓰는 작은 스텝에서는 오차가 반올림 수준(0.39 J 중 1e-13)이라 절단오차 법칙이 아예 관측되지 않는다.
- **semi_implicit_euler**: 심플렉틱, 1차. 2초 동안 에너지가 **14회 방향을 바꾼다**(단조 드리프트가 아니라는 심플렉틱 서명). 다만 이것은 **관측 창에서의 진동 증거이지 임의 구간에서 유계라는 증명이 아니며**, 모듈은 더 강한 주장을 하지 않는다. 1차라는 대가는 실재한다: 2 ms 스텝에서 밴드가 총에너지의 25 %에 달했다.

마찰은 **0이다**(friction_torques가 의도적으로 0을 반환). 결과는 중립적이지 않다. **시뮬레이션된 기구는 영원히 흔들리고 실제 기구는 그러지 않는다.**

### MuJoCo 접촉: 정답 아는 벤치마크 세 가지
같은 URDF를 읽으므로 세 구현이 **같은 모델**을 비교한다. 무접촉에서 우리·Pinocchio·MuJoCo의 가속도가 **1e-13** 수준으로 일치한다. 여기서도 규약 둘을 고쳐야 했다: MuJoCo 기본 중력은 z 아래 9.81이고 우리는 y-up 9.80665이며, MuJoCo는 URDF의 관절한계를 **제약으로 강제**하지만 우리는 기록만 하므로 껐다.

| 벤치마크 | 정답 | 측정 |
|---|---|---|
| 정지 접촉력 | 무게 9.806650 N | **9.806650 N, 오차 0.0000 %** |
| 마찰 임계각 | `atan(μ)` | μ=0.2/0.4/0.7에서 **-0.18 / -0.25 / -0.35도** |
| 반발 | (MuJoCo에 없음) | solref에서 **측정**해 읽어냄 |

마찰 편차는 **한쪽으로만** 나타난다. 항상 이론값보다 **일찍** 미끄러진다. 소프트 제약이 마찰 원뿔 안에서 약간의 미끄럼을 허용하기 때문이다. μ=1.0에서는 부족분이 **1.28도**까지 커지므로, 그 근처를 쓰는 사람은 오차가 몇 배 크다는 것을 알아야 한다.

### 🔑 변위로는 정지와 미끄럼을 구분할 수 없다
첫 시도가 **10도에서 블록이 미끄러진다**고 보고했다. 한계는 21.8도다. 소프트 접촉이 정지한 물체도 **기어가게** 만들기 때문에 변위 임계값은 크리프를 미끄럼으로 오판한다. 판정을 **속도**로 바꿨다: 정지한 물체는 일정한 미세 속도로 기고, 미끄러지는 물체는 **가속한다**. 같은 길이의 두 구간에서 속도를 비교하면 갈린다. 대조 테스트가 이 함정을 그대로 재현해 둔다(10도에서 실제로 움직이지만 미끄러지지는 않는다).

### 🔑 타임스텝이 접촉 강성과 맞지 않으면 조용히 에너지를 만든다
낮은 감쇠비에서 반발을 재다가 발견했다. dampratio 0.05, 타임스텝 2e-4에서 공의 총에너지가 **0.158 J에서 17.07 J로, 108배** 늘었다. **예외도 경고도 없이 정상처럼 돌았다.**

MuJoCo의 결함이 아니다. 같은 모델을 2e-5와 2e-6으로 돌리면 **에너지가 정확히 보존된다.** solref와 타임스텝은 결합돼 있고, 그 결합을 어기면 실패가 아니라 **틀린 답이 그럴듯하게** 나온다. 그래서 이 노드의 모든 접촉 결과는 최종 위치가 아니라 **에너지 이력**을 들고 다니고(`ContactRun.energy_grew`), 대조 테스트가 불안정 케이스를 일부러 재현해 이 검사에 이빨을 남긴다.

### ⚠️ 이것이 뜻하지 않는 것
- **접촉은 소프트 제약**이다. 강체 비관통 조건이 아니라 정규화된 스프링·댐퍼다. 정지한 물체는 바닥에 조금 잠기고, 멈춘 물체도 기어간다.
- **반발계수가 없다.** 목표 반발과의 일치는 재료가 아니라 **튜닝에 대한 진술**이다.
- 강체·마찰계수 하나뿐이다. 변형·마모·응착·윤활은 없다. **정답 아는 벤치마크에서 맞았다는 것이 임의의 복잡한 접촉에서 맞다는 뜻이 아니다.**

registry에 `simulation.multibody_contact`를 등록했고 새 gate `has_contact`를 추가했다. 접촉은 무접촉 모델의 정교화가 아니라 **다른 문제**다. 여기 강체 방법들은 이상관절과 무접촉을 가정하므로, 접촉이 있는 문제는 그것을 모델링하는 곳으로 가야 한다.

---

## 관절한계 강제: 기록만 되고 검사되지 않던 값 (구현 완료)
관절한계는 처음부터 모델에 있었고 **딱 한 곳에서만** 참조됐다. 역기구학이 조용히 clamp하는 것뿐이다. `within_limits` 헬퍼는 존재했지만 **아무도 호출하지 않았다.** 나머지 전부는 어떤 자세든 받아들였다.

### 🔑 도달 불가능한 자세에서 모터를 사이징하고 있었다
`worst_gravity_pose`가 관절이 허용하는 범위와 무관하게 **-π에서 π 전체를 훑고 있었다.** 어깨를 ±0.5 rad로 제한해도 여전히 `q0 = -3.1416`을 돌려줬다.

이게 왜 위험한지가 핵심이다. **도달 불가능한 최악 케이스는 보수적인 것이 아니라 알 수 없는 방향으로 틀린 것**이다. 진짜 최악의 **도달 가능한** 자세는 다른 자세이고 다른 토크를 갖기 때문이다. 측정으로 보이면:

| 어깨 허용 범위 | 최악 자세 q0 | 첨두 토크 [N·m] |
|---|---|---|
| [-3.14, 3.14] | -3.1416 | 0.7931 |
| [-0.50, 0.50] | 0.0000 | 0.7931 |
| [1.00, 1.20] | 1.0000 | 0.4285 |
| [1.40, 1.60] | 1.4000 | 0.1348 |
| [1.50, 1.57] | 1.5000 | **0.0561** |

어깨가 수직 근처로 제한되면 중력 토크는 **14배 작다**. 예전 코드는 0.7931을 보고했을 것이고 모터를 **14배 과대 사이징**했을 것이다. (평면 팔이라 [-0.5, 0.5]에서는 0과 -π가 둘 다 수평이어서 토크가 우연히 같다. 이것도 그대로 적어 둔다.)

수정된 탐색은 관절 범위 안에서만 샘플링하고, **다른 관절을 0에 고정하는데 그 0이 해당 관절의 범위 밖이면** 모든 샘플이 무효이므로 예외를 던진다.

### 하중케이스도 거부한다
`evaluate_case`가 도달 불가능한 자세를 **평가하지 않고 거부한다**. 위반한 관절과 **얼마나 벗어났는지**를 함께 알려준다. "infeasible"이라는 말만으로는 살짝 벗어난 것인지 한참 벗어난 것인지 알 수 없다.

의도적으로 한계 밖 자세를 연구하는 것은 정당한 일이므로 `require_reachable=False`로 여전히 가능하다. 다만 **명시적으로 요청해야 한다.**

`Assembly.limit_violations(q)`는 **첫 번째가 아니라 전부**를 돌려준다. 하나 고치고 다음 것을 다시 발견하는 것은 모델을 배우는 나쁜 방법이다.

### ⚠️ 유효범위
- 한계는 **위치**에만 적용된다. 속도·가속도·토크 한계는 모델에 없고, 따라서 검사하지도 않는다.
- 한계는 **제약이지 물리가 아니다.** 실제 관절 스톱은 부딪히면 힘을 내지만 여기서는 그 자세를 계산 대상에서 뺄 뿐이다. 스톱과의 충돌은 접촉 문제이고 MuJoCo 노드 몫이다.
- 경계값은 **도달 가능**으로 본다(배타적 상한이 아니다).

---

## 일반 형상 -> CalculiX 라우팅: 우리가 메싱 못 하는 것을 위한 유일한 경로 (구현 완료)
우리 Warp 솔버는 **구조화 균일 격자 위의 8절점 hex**다. 그 격자가 덮지 못하는 형상은 지금까지 **해석 경로가 아예 없었다**. Gmsh가 사면체로 메싱하고 CalculiX가 푼다.

### 🔑 이 경로에는 두 번째 의견이 없다
`analysis.fea.calculix`(기존)와 `analysis.fea.general_shape`(신규)는 **서로 다른 질문에 답한다**. 전자는 두 솔버가 공유하는 메시 위에서 Warp를 교차검증한다. 후자는 우리가 메싱조차 못 하는 영역의 **유일한 경로**이므로 **대조할 대상이 없다**. 단일 솔버·단일 메시다. 노트에 그렇게 적어 두었고 registry가 두 capability를 **상호배타적으로** 라우팅한다(구조화 문제는 general로 가지 않고, 일반 문제는 hex 교차검증으로 가지 않는다).

### 첫 일반 형상을 박스로 고른 것은 의도적이다
박스는 **두 메셔가 모두 덮는 유일한 영역**이다. 그래서 사면체 답을 아무것도 아닌 것이 아니라 **구조화 hex 답과 대조**할 수 있다.

| 요소 | 크기 [m] | 요소수 | 팁 처짐 [mm] | hex 대비 |
|---|---|---|---|---|
| C3D4 | 0.012 | 907 | -0.019181 | **-18.31 %** |
| C3D4 | 0.008 | 2467 | -0.020903 | **-10.98 %** |
| C3D10 | 0.012 | 907 | -0.023597 | +0.50 % |
| C3D10 | 0.008 | 2467 | -0.023626 | +0.62 % |

**선형 사면체(C3D4)는 굽힘에서 심하게 뻣뻣하다.** 11~18 % 낮게 나오고 세분해도 느리게만 좁혀진다. 구현 전에 유효범위로 적었던 그대로다. 그래서 **기본값은 C3D10**이고 C3D4는 대조군으로만 남긴다. 이 경로가 존재하는 이유가 바로 굽힘이 있는 실제 형상인데 거기에 C3D4를 기본으로 두면 **자신 있게 틀린 처짐**을 준다.

### 절점 순서가 틀리면 조용히 실패한다
Gmsh와 CalculiX는 10절점 사면체의 **중간절점 순서가 다르다**. Gmsh는 꼭짓점 다음 모서리 (0,1)(1,2)(0,2)(0,3)(2,3)(1,3), CalculiX C3D10은 mid(1,2) mid(2,3) mid(3,1) mid(1,4) mid(2,4) mid(3,4)이다. 순열 `(0,1,2,3,4,5,6,7,9,8)`을 **명시적으로 적용**한다.

대조실험으로 확인했더니 실패 방식이 예상과 달랐다. 순열을 되돌리거나 중간절점 둘을 바꾸면 **미묘하게 틀린 답이 아니라 정확히 0**이 나온다. CalculiX가 덱을 거부하고 **빈 결과 파일**을 쓰기 때문이다.

**그래서 어댑터를 고쳤다.** 예전에는 그 경우 0으로 채워진 배열을 그대로 돌려줬다. 0 변위는 "완벽히 강한 구조"로 읽히는 **그럴듯한 값**이고, `converged` 플래그를 확인하지 않은 호출자는 실패를 답으로 받는다. 이제 **예외를 던진다.** 모니터링 패널에서 "읽을 수 없는 값을 0으로 만들지 않는다"고 한 것과 같은 원칙이다.

### ⚠️ 유효범위
- **우리 솔버는 이 메시를 못 먹는다.** 사면체 요소가 없다. 라우팅은 **단방향**이다.
- 곡면 영역에서 `volume_m3()`는 **직선 사면체 근사**다(꼭짓점 4개만 쓴다).
- 교차검증이 가능한 것은 **두 메셔가 모두 덮는 형상뿐**이다. 그 밖에서는 이 경로가 유일하고 검증되지 않은 채로 남는다. 그것을 감추지 않는다.

---

## 카탈로그 형상: 있는 것과 없는 것을 갈라 놓기 (구현 완료)
카탈로그는 깔끔하게 둘로 갈린다. 이 모듈은 그 경계를 덮지 않고 **보이게** 유지한다.

**베어링은 실제 형상을 갖는다.** 6206은 어느 제조사에서든 30 x 62 x 16 mm이므로 그것은 발명이 아니라 **표준**이다. 여섯 개 전부 solid 부피가 링 공식과 **1e-16**으로 일치하고, 바운딩 박스가 (OD, OD, 폭)과 맞는다.

**모터와 감속기는 형상이 아예 없다.** 카탈로그에 토크·속도·관성·질량만 있고 치수는 한 줄도 없다. 그래서 solid를 만들면 그것은 **지어낸 것**이고, 이 모듈은 그것을 잊을 수 없게 만든다: 이름에 `PLACEHOLDER_NO_GEOMETRY`가 박히고, 물리적 내용은 **질량 하나뿐**이다. 치수는 질량과 **호출자가 명시해야 하는 가정 밀도**에서 유도되므로, 블록은 올바른 양의 공간을 차지하고 그 이상 아무것도 주장하지 않는다. 검증: `부피 x 가정밀도 == 카탈로그 질량`이 정확히 성립한다.

### 🔑 왜 "그럴듯한 치수"를 쓰지 않았나
**그럴듯한 것이 위험한 쪽이기 때문이다.** 실제 모터처럼 생긴 블록은 누군가 그것에 대고 간섭 검사를 하게 만들고, 그 답은 **구분할 방법 없는 허구**가 된다. 질량만 맞고 라벨이 붙은 블록은 그것이 잘하는 일(패키징 산술, 무언가가 공간을 차지한다는 사실)에는 여전히 쓸 수 있다.

가정 밀도 기본값 4000 kg/m3도 묻어두지 않고 상수로 드러냈다. 모터는 강판·구리와 사이의 공기라 실측 밀도가 어느 쪽보다도 낮다. 호출자가 동의하지 않을 권리가 있다.

### 간섭 검사
쌍마다 **겹치는 부피**를 돌려준다(불리언이 아니라). "닿았다"와 "하나가 다른 것 안에 파묻혔다"는 다른 문제이고 예/아니오로는 구분되지 않는다. 정답 아는 케이스로 검증했다: 같은 자리의 동일 베어링 두 개는 **링 부피 전체**, 폭의 절반만큼 어긋나면 **정확히 절반**, 폭보다 멀면 0이다. 작은 베어링(OD 26)이 큰 것의 내경(30) 안에 **닿지 않고 들어가는** 경우도 확인했다. 거짓 음성이 아니라 옳은 답이다.

`InterferenceReport`는 **placeholder가 끼어 있는지**를 함께 들고 다닌다. 지어낸 형상과의 간섭은 하드웨어가 아니라 **가정에 대한 진술**이고, 둘을 같게 읽으면 안 된다.

### ⚠️ 유효범위
- 베어링 solid는 **외피**다. 볼도 케이지도 실도 궤도도 없다. 클리어런스와 장착에는 맞고 베어링 **내부 하중경로에 대해서는 아무 말도 하지 않는다.**
- placeholder는 **질량과 그 밀도에서의 부피**에만 맞다. 종횡비는 여기서 정한 관례이고, 장착 피처는 존재하지 않으며, 질량중심은 측정값이 아니라 도심이다.
- **벤더 CAD가 이 전부를 대체한다.** 그때까지 placeholder에 대고 하는 간섭 검사는 **가정에 대고 하는 검사**다.

---

## DesignReference: 바깥 프라이어를 들이되 아무것도 결정하지 못하게 (구현 완료)
외부 LLM 레퍼런스 엔진이 쓸모 있는 공학 프라이어를 줄 수 있다. 어떤 부품군의 전형적 비례, 주조·가공에서 기대되는 벽두께, 응력집중을 피하는 필렛 관례, 제품군이 공유하는 실루엣. 그중 **어느 것도 측정이 아니고**, 일부는 **뒤에 아무것도 없는 그럴듯한 문장**이다.

그래서 이 계층은 그런 재료를 **들이되 아무것도 결정하지 못하게** 한다. 규칙은 한 문장이고 나머지는 전부 여기서 따라 나온다.

> 레퍼런스는 탐색이 **어디서 시작하는지**와 **무엇을 아름답다고 여기는지**를 옮길 수 있다. **무엇이 안전한지는 옮길 수 없다.**

### 🔑 없음이 곧 장치다
`DesignReference`에는 `OptimizationProblem`을 받아 **수정된 것을 돌려주는 함수가 없다**. 잊어버릴 수 있는 검사로 막은 것이 아니라 **구조적으로 도달 불가**하게 했다. 프라이어가 허용응력을 완화할 수 있다면 모델이 생성한 문장이 물리를 뒤집을 수 있고, 그것이 이 프로젝트 전체가 막으려는 단 하나의 결과다.

테스트가 이 부재를 지킨다. 공개 메서드 집합을 고정해 두었으므로 **누군가 그런 메서드를 추가하면 실패한다**(실제로 `as_dict`를 추가했을 때 이 테스트가 잡았다). 레퍼런스에 `OptimizationProblem`이 들어가는 유일한 메서드는 **배열을 돌려주지 문제를 돌려주지 않는다**. 설치할 것 자체가 없다.

### 출처 없는 프라이어는 생성 자체가 실패한다
모든 항목이 `source` + `confidence` + `provenance`를 들고 다니며 없으면 **생성이 실패한다**. 한 번 기록되고 나면 **출처 없는 프라이어는 지어낸 것과 구별할 수 없고**, 이 작업의 위험 전체가 "유창한 추측이 저장되면서 데이터의 권위를 얻는 것"이다.

신뢰도는 여기서 새로 만든 척도가 아니라 **기존 evidence 사다리에 걸어** 상한을 둔다.

| provenance | 뜻 | 상한 |
|---|---|---|
| `ASSUMED` | 확인 가능한 출처 없음 | UNVERIFIED 천장 **0.20** |
| `CITED` | 출처는 있으나 우리가 확인한 적 없음 | SIMULATED 천장 **0.60** |
| `MEASURED` | 우리가 계산한 것에서 유도 | 같은 **0.60** |

**REPEATED 이상에는 아무것도 닿지 못한다.** 그 등급은 독립 실행이 서로 맞아야 얻는 것이고, **레퍼런스를 읽는 것은 실행이 아니다**. `ASSUMED` 항목은 사람이 읽는 모든 자리에 **`[ASSUMED]` 태그**를 달고 나온다.

### 중심 불변식: 프라이어가 낙제를 합격으로 못 바꾼다
가장 중요한 테스트는 "레퍼런스가 동작한다"가 아니라 **"레퍼런스가 합격/불합격을 바꾸지 못한다"**이다. 턱없이 얇은 단면을 강하게 가리키는 프라이어(그럴듯하지만 틀린 프라이어가 할 법한 바로 그 일)를 넣어도 평가는 **미동도 하지 않는다**: 같은 설계가 여전히 infeasible이고, **제약값과 안전율이 문자 그대로 동일**하다.

부수 규칙도 같은 방향이다. 프라이어가 제안한 시작점은 **기존 bounds 안으로 clip**되므로 bounds가 이미 배제한 설계에는 **닿을 수 없다**. 공동이 남지 않는 불가능한 단면을 제안하면 옵티마이저에 넘기지 않고 **되돌린다**.

허용되는 영향은 정확히 둘이고, 산문이 아니라 **데이터로** 적어 뒀다(`describe_influence()`가 2개, `forbidden_influence()`가 5개이며 테스트가 개수를 고정한다).

### 미학 가중치는 신뢰도로 스케일된다
`FormTarget`은 구조화된 기하 제약이 아니라 **자유 텍스트 타깃 + 가중치**다. 이 모듈은 **무엇이 보기 좋은지에 대한 이론을 담지 않는다.** 여기에 그런 이론을 인코딩하면 주관적 선호가 **명세의 외양**을 갖게 된다. 가중치는 신뢰도로 곱해지므로 **추측(0.2)이 인용된 관례(0.6)보다 약하게 당긴다.**

번들의 신뢰도는 평균이 아니라 **가장 약한 항목**으로 보고한다. 평균을 쓰면 **탄탄한 인용 하나가 추측 여러 개를 세탁**한다.

### Brain 연동
스키마 변경이 필요 없다. `record_run(meta=...)`이 이미 있다. 중요한 건 **무엇을 저장하느냐**다. 항목마다 출처·신뢰도·provenance를 그대로 남기고 **상한과 `physically_validated: False`를 함께** 저장한다. 데이터베이스에서 꺼낸 숫자는 **자신을 제한했던 맥락을 잃어버리기** 때문이다. 그래야 나중에 "어떤 프라이어가 좋은 결과와 함께 있었는가"를 물으면서도 **추측을 발견으로 오인할 수 없다.**

### ⚠️ 유효범위
- **레퍼런스는 부품군을 서술하지 이 부품을 서술하지 않는다.** 어떤 부품군에 전형적인 비례가 **이 하중조건에 옳은지는 아무 말도 하지 않으며**, 그것을 정하는 것은 옵티마이저다. 시작점 편향은 **어느 국소최적을 먼저 만나는지**만 바꿀 수 있고, 볼록 문제에서는 **반복 횟수 말고 아무것도 바꾸지 않는다.**
- **미학은 주관적이고 이 모듈은 아닌 척하지 않는다.** 무엇이 요청됐고 누가 요청했는지를 기록할 뿐이다.
- **레퍼런스는 영감이지 복제가 아니다.** 제품군에서 관찰된 비례 범위를 저장하는 것은 설계를 베끼는 것과 다르며, 여기 어디에도 **원본 산출물을 보관하거나 재현하지 않는다.**

---

## 휴머노이드 포먼암: 레퍼런스 엔진 첫 end-to-end 실증 (구현 완료)
외부 LLM이 웹에서 찾은 레퍼런스를 `DesignReference`로 넘기고, 그것으로 실제 링크를 설계한 첫 실행이다. `projects/humanoid_arm/`에 재현 가능한 형태로 있다.

문제 파라미터 중 레퍼런스에 없던 것은 **우리가 골랐고 그렇게 명시한다**: 길이 0.25 m, 팁 페이로드 2 kg, al_7075_t6, 최대응력 120 MPa, 처짐 0.5 mm, 안전율 하한 2.0.

### 프라이어는 경로를 바꾸고 목적지를 바꾸지 않았다
| | b [mm] | h [mm] | t [mm] | 질량 [g] | 평가 횟수 |
|---|---|---|---|---|---|
| 기본 시작점 | 10.000 | 20.271 | 1.000 | 39.721 | 317 |
| 레퍼런스 시작점 | 10.000 | 20.271 | 1.000 | 39.721 | **288** |

설계 차이 최대 **3e-6 mm**, 질량 차이 **0.000 mg**. 바뀐 것은 반복 횟수뿐이다. 같은 설계를 프라이어 적용 전후로 평가했을 때 **제약 dict와 안전율도 동일**했다.

### 🔑 최적해가 모든 치수 프라이어 아래로 내려갔고, 그 간극이 정보였다
레퍼런스는 h 30~60, b 20~40, t 2~4 mm를 말했다. 최적해는 20.3 x 10.0 x 1.0 mm로 **셋 다 아래**였고 b와 t는 **설계공간 하한**에 붙었다.

프라이어가 틀린 것도 옵티마이저가 틀린 것도 아니다. **문제 서술이 실제 포먼암을 결정하는 것들을 담고 있지 않았다.** 그래서 그것들을 **프라이어 강제가 아니라 요건으로** 추가했다(둘 다 대표값 [ASSUMED]): 액추에이터·배선 관통 **clear bore 15 mm**, 손목 **토크 5 N·m**(굽힘과 동시 작용).

IR에 `Constraints.min_clear_bore_m`과 `LoadType.TORQUE`를 추가했다. 토크는 하중이지 제약이 아니므로 **하중으로** 넣었고, 그러려면 "정확히 하중 하나"를 요구하던 보 모델을 "정확히 점하중 하나"로 바꿔야 했다(가드의 의도는 보존). 비틀림은 Bredt 단일폐단면 `tau = T/(2 A_m t)`이고 **A_m은 외곽이 아니라 벽 중심선이 감싸는 면적**이며, 굽힘과는 `sqrt(sigma^2 + 3 tau^2)`로 합성한다. 새 제약 셋의 해석적 기울기는 옵티마이저가 신뢰하므로 **유한차분으로 검증**했다(상대오차 2e-4, 기존 `stress` 기울기의 5.5e-4와 같은 대역).

### 요건은 설계를 실제로 움직였다
| | b [mm] | h [mm] | t [mm] | 질량 [g] | 활성 제약 |
|---|---|---|---|---|---|
| baseline | 10.000 | 20.271 | 1.000 | 39.721 | deflection |
| packaged | **17.000** | 17.354 | 1.000 | 45.458 | **bore_b**, deflection |

폭이 **70 % 늘었다**. 프라이어가 같은 것을 요청했을 때는 아무것도 못 움직였다. **이 대비가 분리의 전부다.** 다만 정직하게: b는 레퍼런스 범위(20~40) 쪽으로 갔지만 여전히 밖이고, h는 오히려 **작아졌으며**(단면이 넓어져 같은 처짐에 필요한 높이가 줄었다), t는 그대로다.

### 어느 요건이 지배하는지 재봤다
토크를 5에서 400 N·m까지 훑었다.

| T [N·m] | b | h | t | 활성 |
|---|---|---|---|---|
| 5, 20 | 17.0 | 17.4 | 1.0 | bore_b, deflection |
| 50 | 20.0 | 20.1 | 1.0 | combined_stress |
| 100 | 27.9 | 27.9 | 1.0 | combined_stress |
| 200 | 39.0 | 39.0 | 1.0 | combined_stress |
| 400 | 54.7 | 54.7 | 1.0 | combined_stress |

**벽두께는 400 N·m에서도 하한 1.0 mm에 머문다.** 폐단면 비틀림은 전단류 `q = T/(2A)`로 전달되므로 **단면을 키우는 것이 벽을 두껍게 하는 것보다 훨씬 질량효율적**이고, 옵티마이저는 매번 단면을 키운다. 즉 레퍼런스의 2~4 mm 벽은 **하중에서 나오지 않는다.** 출처가 3D 프린팅 셸 논문인 데서 알 수 있듯 **제조가능성 제약**이며, 어떤 하중으로도 재현되지 않는다. 그것을 반영하려면 최소 제조 벽두께를 제약으로 넣어야 한다.

200 N·m에서는 b와 h가 둘 다 레퍼런스 범위 안에 든다. 레퍼런스의 비례는 **우리가 푼 것보다 훨씬 크게 하중받는 팔**에 대응한다.

### Brain 기록
`runs/brain.sqlite3`에 `humanoid-forearm-baseline`과 `humanoid-forearm-packaged`. 항목 8개 전부 출처·신뢰도·provenance 보존, `physically_validated: False`, 상한 0.6, 번들 신뢰도는 **최약항 0.20**(ASSUMED symmetry). 추가된 요건과 "최적해가 프라이어 아래"라는 사실도 함께 저장한다.

미학 가중치는 계산·기록되지만 **아직 소비되지 않는다**. 미학 목적함수가 없다. 안 쓰이는 것을 쓰인 것처럼 보고하지 않는다. 유기 서피싱 단계가 그 자리다.

### 최소 제조 벽두께를 넣자 레퍼런스가 완전히 분해됐다
토크 스윕이 "벽은 하중에서 안 나온다"를 확정했으므로, 올바른 반영 방법은 토크를 키우는 것이 아니라 **공정 제약**이다. `Constraints.min_manufacturing_wall_m`을 추가하고 2 mm를 넣었다([ASSUMED]이고 **공정 의존**이다. 프린팅 폴리머 셸 기준이며 절삭 알루미늄은 더 얇다. 요점은 이 숫자가 부품이 아니라 **공정**에 속한다는 것이다).

결과: b = h = **19.000 mm**, t = **2.000 mm**, 질량 95.540 g. 활성 제약은 `bore_b`, `bore_h`, `manufacturing_wall` **셋뿐**이다.

| 프라이어 | 범위 | 결과 | 판정 |
|---|---|---|---|
| t | 2~4 mm | **2.0000** | **INSIDE** |
| b | 20~40 mm | 19.0000 | 밖 (1 mm 차) |
| h | 30~60 mm | 19.0000 | 밖 |

**벽 프라이어가 충족됐다.** 공정을 명시하자마자, 강제하지 않고.

### 레퍼런스가 어떤 요건으로 분해되는지 측정으로 확정했다
- **t (2~4 mm) -> 제조 공정.** 공정 바닥에 정확히 앉는다(1e-18 이내). 하중과 무관하다.
- **b (20~40 mm) -> 관통 내경.** `b = bore + 2t`가 **정확히** 성립한다(1e-17). bore를 15/16/20/30/36/40 mm로 훑어 6점 전부에서 확인했다. **뒤집으면**: 레퍼런스의 20~40 mm 폭은 **16~36 mm 내경**을 뜻한다. 추측이 아니라 검사 가능한 주장이다.
- **h (30~60 mm) -> 하중 수준.** 여기서는 높이를 요구하는 것이 아무것도 없다. 토크 스윕에서 h가 39 mm에 닿는 것은 **200 N·m** 근처다.

### 🔑 이 하중에서 포먼암은 구조 문제가 아니다
구조 제약이 **하나도 활성이 아니다**: deflection +0.566, stress +0.942, combined_stress +0.914 전부 여유, 안전율 **71.7**. 단면을 정하는 것은 **패키징(내경)과 공정(벽)**이다.

이것이 완결된 실증이다. **어떤 프라이어도 강제하지 않았다.** 각각을 그것을 만들어내는 **요건의 종류**로 추적한 다음에만 문제에 넣었고, 물리는 한 번도 우회되지 않았다.

## SURROGATE 근거등급 게이트 (구현 완료)

생성설계 트랙(합성데이터, 분류, 임베딩, 서로게이트, 설계의도, 생성)에 들어가기 전에
막아 둔 것. 학습된 모델이 루프에 들어오면 "모델이 그렇게 말했다"가 "솔버가 그렇게
말했다"와 섞이는 순간이 오고, 그 순간 근거 사다리는 무너진다. 그래서 등급을 먼저
만들고 코드로 못 박았다.

**등급.** `EvidenceLevel.SURROGATE`를 `UNVERIFIED`와 `SIMULATED` 사이에 넣었다.
천장은 0.40으로, 정책값이지 측정값이 아니다. `SIMULATED` 천장 0.60보다 확실히
아래에 둔 이유는 confidence 숫자만 보고 서로게이트 진술을 솔버 진술로 착각할 수
없게 하기 위해서다. `EvidenceKind.SURROGATE`도 신설했다.

**유도 규칙.** `derive_level`은 세기 전에 서로게이트 근거를 먼저 떼어 낸다. 서로게이트
근거만 있으면 개수와 run 수가 얼마든 `SURROGATE`. 솔버 근거가 하나라도 있으면
서로게이트 항목은 run 수에도, 항목 수에도, confidence 의 n 에도 들어가지 않는다.
측정으로 확인한 것: sim 2 + surrogate 50 은 `SIMULATED`, sim 7 + surrogate 50 은
`REPEATED`(HIGH_CONFIDENCE 문턱 8에 한 개 모자람), sim 3 의 confidence 는 surrogate
40 을 더해도 그대로다. 반대로 물리시험 근거를 막지도 않는다: surrogate 5 + physical
1 은 여전히 `EXPERIMENTALLY_VALIDATED`. 게이트는 서로게이트가 못 하는 것을 정하는
것이지, 먼저 스크리닝됐다는 이유로 벌을 주는 것이 아니다.

**판정 가드.** `may_decide(level)`이 규칙이 적힌 유일한 자리다. `VERDICT_FLOOR =
SIMULATED`. `integration/checks.py`의 `CheckResult`에 `evidence_kind` 필드를 넣고,
`SURROGATE` 근거로 PASSED 나 FAILED 를 만들면 생성 시점에 `SurrogateVerdict`로
거부한다. FAILED 도 거부하는 이유: 예측으로 설계를 버리는 것도 판정이고, 틀리면
솔버가 받아들였을 설계를 버린다. 서로게이트 결과의 정직한 상태는 새 상태 `SCREENED`
이며, 어셈블리 판정에서 NOT_ASSESSED 와 같이 gap 으로 취급되어 PASSED 가 되지 못한다.
`SCREENED`는 모델 이름과 예측 내용을 요구하고, `safety_factor` 필드는 금지한다.
예측된 안전율이 그 자리에 들어가면 리뷰가 풀린 값으로 읽어 지배 조건으로 삼기
때문이다. 예측값은 detail 텍스트로만 남는다.

**Phase 6 연결.** `Prediction`은 항상 `SURROGATE`, `ScreeningResult`는 `verified`
일 때만 `SIMULATED`. 둘 다 `as_evidence`로 Brain 근거 항목이 되고, 예측이 될 수 있는
유일한 `CheckResult`는 `screened_check`다. 리뷰는 screened 목록을 unassessed 와
별도로 보이고 "솔버를 돌려라"를 권고한다.

**바뀌지 않은 것.** 기존 Brain/통합 테스트는 수정 없이 통과했다. rank 는 서로
비교만 하므로 등급 삽입으로 이동한 것이 없고, DB 는 문자열을 저장하므로 마이그레이션도
없다. 기본 `evidence_kind`는 SIMULATION 이어서 등록된 54개 능력의 동작은 그대로다.

**하지 못하는 것.** 이 게이트는 서로게이트가 판정하는 것을 막을 뿐, 서로게이트가
정확하다는 것을 말하지 않는다. 그리고 여전히 전부 SIMULATED 이하다.

## P5 합성데이터 엔진: 파라미터에서 검증된 라벨까지, 스크래핑 없이 (구현 완료)

생성설계 트랙의 첫 단계. 다섯 개 형상 패밀리(box, hollow_rect, l_bracket,
plate_with_holes, stepped_shaft)를 build123d 로 만들고, 이 프로젝트가 이미 검증한
경로(STEP 분석기 → Gmsh 2차 사면체 → CalculiX C3D10)로 라벨을 붙여 Apache-2.0
출처가 박힌 레코드로 저장한다. 새 물리는 없고, 새 능력으로 등록하지도 않았다. 부품을
만드는 것은 물리 질문에 답하는 것이 아니다.

**합성데이터의 값어치는 정답을 안다는 것이고, 그래서 매 레코드가 정답과 대조된다.**
패밀리마다 닫힌 형태 부피가 있고, 분석기가 STEP 을 읽어 낸 부피가 그것과 1e-6 안에서
맞지 않으면 그 부품은 저장되지 않고 거부 목록에 이유와 함께 오른다. 판 패밀리는
파라미터가 넣은 홀 개수·직경, 필렛 개수·반경을 인식기가 정확히 찾아야 한다. 이 두
검사를 일부러 어긋나게 한 패밀리(부피 2배 주장, 홀 99개 주장)로 거부가 실제로
일어나는지 테스트했다.

**라벨은 호출자가 등급을 고르지 않는다.** `label()` 은 근거 종류로 등급을 매긴다:
solve 와 유도는 SIMULATED, SURROGATE 종류는 SURROGATE, 물리시험만 그 위. 스키마는
계산된 라벨이 SIMULATED 위를 주장하면 레코드 자체를 거부한다(repeated,
high_confidence, experimentally_validated 각각 테스트). 하나의 solve 는 하나의
시뮬레이션이고, 그 위 등급은 독립 실행이 쌓인 진술의 것이다.

**측정한 것.** 200 x 40 x 30 mm 중공 상자, 두 메시 크기(10 mm, 6 mm): 최대 변위는
0.06 퍼센트 움직였고 피크 von Mises 는 14 퍼센트 움직였다. 완전 고정면 모서리는 응력
특이점이라 피크는 세분화해도 수렴하지 않는다. 그래서 모든 솔버 라벨은 거친 메시와
고운 메시 사이 상대 변화를 `mesh_sensitivity` 로 들고 다니고, 응력 라벨의 note 는
"수렴하지 않으며 인증에 쓰면 안 된다"고 말한다. 패밀리별 첫 표본에서 변위 민감도는
0.16 퍼센트(box)에서 8.3 퍼센트(짧고 뭉툭한 hollow_rect)까지 갔다. 짧은 부품의 "팁
처짐"은 국소 효과가 지배하므로 그 숫자는 라벨에 남아 소비자가 걸러 낼 수 있게 했다.

**교차검증.** box 패밀리는 두 메셔가 모두 덮는 유일한 형상이라 라벨 경로의 유일한
레퍼런스다. 엔진이 붙인 팁 처짐이 Warp hex 해와 2 퍼센트 안에서 맞는다(일반 형상
경로가 채택된 것과 같은 허용치). 질량은 분석기 부피 x 밀도로 1e-6 안에서 정확하다.

**비용.** 부품당 0.2초(샤프트)에서 5.7초(중공 상자, 고운 메시 26k 노드). 다섯 패밀리
한 벌 13초. 결정론적: 같은 시드는 같은 part_id 를 만든다(파라미터를 나노미터로 반올림한
sha1).

**결정 사항(사용자 확인 없이 기본값으로 정한 것).** 라벨 솔버는 CalculiX C3D10 하나.
Code_Aster 와 Elmer 는 각각 소성과 정자기 전용이어서 선형 정탄성 라벨에는 CalculiX 가
검증된 유일한 일반 형상 경로다. 하중 케이스는 캔틸레버 하나(x-min 면 고정, x-max 면에
-100 N). 재료 기본값 al_7075_t6. 저장은 JSONL 한 줄 한 레코드, 공개 파일에는
publishable 레코드만 쓰이고 proprietary 는 쓰기 전에 거부된다(부분 파일 안 남김).
데이터셋 규모는 아직 정하지 않았고 테스트는 패밀리당 하나만 만든다. 대량 생성은 P3 가
필요로 하는 만큼 그때 돌린다.

**하지 못하는 것.** 다섯 종의 프리즘이다. 나사, 공차, 챔퍼(파라미터로 준 것 외), 카탈로그
부품과의 닮음은 없다. 하중 케이스 하나뿐이어서 실제로 그렇게 하중 받지 않는 부품은
보지 않는 하중으로 라벨된 것이다. 전부 SIMULATED.

## P3 형상 기술자와 분류: 규칙이 답하고 모델은 대조한다 (구현 완료)

**기술자.** B-rep 에서 읽는 척도 무관 숫자 22개: log 컴팩트니스, 바운딩 박스 비율 둘,
채움률, 주관성모멘트 비율 둘, 질량중심 오프셋, 면·모서리·정점 수, 오일러 특성, 면
종류별 비율(평면·원기둥·구·토러스·원뿔·기타), 홀·필렛 개수와 상대 치수, 홀도 필렛도
아닌 원기둥 면 수. 같은 비율의 두 배 크기 판은 1e-6 안에서 동일 벡터를 낸다(테스트).
크기를 말하는 숫자는 하나도 없다. 그것은 솔버의 일이다.

**측정이 깨끔한 규칙을 반박했다.** 첫 버전의 오일러 특성 V - E + F 는 중공 튜브에
2 를 냈다. 관통 공동이 있으니 0 이어야 한다. 원인은 튜브 양 끝면이 원판이 아니라
고리라는 것: 안쪽 루프가 있는 면은 셀 하나로 세면 안 된다. 수정은 V - E + F - (안쪽
루프 수), 안쪽 루프는 와이어 수에서 면 수를 뺀 것. 측정: box·l_bracket·stepped_shaft
2, hollow_rect 0, 홀 n 개 판은 2 - 2n, Fusion 판(홀 4) -6. 이 하나가 규칙 셋(hollow_rect,
plate, shaft)을 한꺼번에 살렸다: 수정 전 규칙 정확도 0.40, 수정 후 1.00.

**규칙 분류기.** 다섯 패밀리 각각의 토폴로지 조건을 명시하고, 어느 것도 맞지 않으면
가장 가까운 것을 고르지 않고 UNKNOWN 과 이유 목록을 돌려준다. 생성 부품 150개(시드
둘)에서 1.00. Fusion 으로 저작된 판 픽스처 A, B 는 plate_with_holes 로 분류된다.
규칙은 Fusion 출력을 본 적이 없으니 커널 간 교차검증이다. 챔퍼·원뿔·오목 블렌드·슬롯이
있는 픽스처 C~G 는 전부 UNKNOWN 이고, 그것이 정답이다. 능력 `analysis.cad.classify`
로 등록(55번째, 노드 `shape.classifier`). 규칙 판정의 등급은 SIMULATED.

**학습 분류기는 SURROGATE 이고 제안만 한다.** 표준화 기술자 위 k=5 최근접이웃, 훈련
집합 자체의 leave-one-out 최근접 거리 99 백분위 x 계수 를 넘으면 UNKNOWN(개방 집합
거부). 의존성 추가 없음(numpy 만). 패밀리 다섯과 숫자 22개에 신경망은 필요 없고, 모든
판정을 이름 붙은 훈련 부품으로 추적할 수 있는 방법이 여기서는 정확도 몇 포인트보다
값어치가 있다.

계수를 측정으로 정했다: 1.25 이상에서는 원뿔이 든 픽스처 D 가 0.6 표로 box 로
수용됐다. 1.0 에서는 거부된다. 패밀리 구성원 오거부는 규칙을 한 번 더 보는 비용이고,
오수용은 틀린 답이므로 1.0. 또 하나: 컴팩트니스를 로그 없이 쓰면 얇은 튜브 하나가 다른
모든 것에서 7.8 표준화 단위 떨어져 홀로 거부됐다(정확도 0.96). 로그를 취하니 홀드아웃
1.00, 거부 0, 문턱 2.10. 그래도 테스트 하한은 0.9 로 느슨하게 두었다. 답은 규칙이고
이것은 규칙에 대한 대조다.

**하지 못하는 것.** 다섯 합성 패밀리 안에서만 의미가 있다. 실제 부품 대부분은 UNKNOWN
이고 그것이 옳다. 구와 토러스의 퇴화 모서리는 오일러 수를 흔들며 그 값은 보고되지만
genus 가 아니다. 학습 분류기는 규칙이 이미 아는 것을 배웠음을 보였을 뿐이다. 전부
SIMULATED 이하.

## P6 CAD 임베딩: 학습 공간은 히스토그램은 이기고 토폴로지는 못 이겼다 (구현 완료)

**점군.** OpenCASCADE 로 B-rep 을 삼각화(선형 편차 0.1 mm)하고 삼각형 면적에 비례해 표면
점 512개를 뽑는다. 평면 패밀리 셋은 삼각화 면적이 B-rep 면적과 라운드오프 안에서
같고, 판(홀·필렛)은 2e-5, 샤프트(원기둥)는 8e-4 낮다. 코드로 현을 그리니 그렇다.
정규화는 질량중심 이동 + RMS 반경 1. 그 다음 주축 정렬(PCA, 큰 분산 우선, 3차
모멘트로 부호 결정, 반사 제외).

**정렬을 학습에 맡기지 않은 이유(측정).** 무작위 회전 증강으로 인코더가 회전 불변을
배우게 하면 같은 부품을 돌렸을 때 자기 자신과의 코사인이 최소 0.19, 중앙값 0.86.
먼저 정렬하고 정렬이 못 정하는 네 가지 고유 부호 뒤집기만 증강하면 최소 1.00. 학습
시간도 12초에서 4초로.

**D2 히스토그램(무학습 기준선).** 무작위 점 쌍 거리의 히스토그램(64 구간, 평균 거리로
나눔). 이동·회전·척도 불변이 구조적으로 성립하고 테스트로 확인. 홀드아웃 최근접 이웃
패밀리 정확도 0.64.

**PointNet 임베딩(SURROGATE).** 점별 MLP → max 풀 → 32차원 단위 벡터. 패밀리 분류
헤드로 학습하고 헤드는 버린다. 훈련 150개·홀드아웃 50개(시드 분리): 정확도 0.88. 22개
기술자는 1.00. 즉 학습 임베딩은 학습 없는 히스토그램은 이기고 학습 없는 기술자는 못
이긴다. 다섯 프리즘 패밀리에서 답은 토폴로지이고, 학습 공간은 점만 보고 그 대부분을
복원할 수 있음을 보였을 뿐이다. 값어치가 생길 자리는 기술자가 갈라 내지 못하는
형상인데 이 훈련 집합에는 그런 것이 없다. 테스트 하한은 0.75 와 "D2 보다 높다"로
느슨하게 두었다. 학습 곡선의 가장 좋은 날에 핀을 박으면 보통 날에 깨진다.

**기대가 반박된 것.** Fusion 저작 판 픽스처 A, B 는 규칙으로는 plate_with_holes 이지만
임베딩 공간에서는 box 에 가장 가깝다. 100 x 60 x 10 판에 8 mm 홀 넷은 면적 비례
샘플링에서 거의 전부 슬래브이고, 패밀리를 결정하는 홀은 표면의 몇 퍼센트다. 처음
쓴 테스트는 "판 위에 떨어져야 한다"였고 측정이 아니라고 했다. 테스트는 측정된 대로
바꿨다: 슬래브 계열(box 또는 plate)에 떨어지고, plate 라고 말하는 것은 규칙이다.
임베딩에는 UNKNOWN 이 없다. 그래서 규칙이 있다.

**의존성.** torch 만(서로게이트가 이미 쓰는 것). 새 패키지 없음. GPU(RTX 3050)에서
훈련 4초, 저장·복원은 encoder.pt + meta.json(evidence: surrogate 명시).

**하지 못하는 것.** 다섯 패밀리 밖 부품에 대한 임베딩 거리는 아는 것들까지의 거리일
뿐이다. 유사성이지 측정이 아니다. 전부 SURROGATE, 히스토그램은 SIMULATED.

## P7 형상 서로게이트: 게이트가 만들어진 이유인 단계 (구현 완료)

**무엇을 근사하나.** P5 라벨러의 캔틸레버 케이스를 CalculiX C3D10 으로 푼 결과(팁 처짐,
최대 변위, 피크 von Mises)를 22개 기술자 + 부피·최장변·E·하중 로그 + 하중 방향에서
예측하는 작은 MLP. 예측은 전부 SURROGATE 등급, 홀드아웃 p95 오차를 달고 다니며,
integration 계층은 그 위에 PASSED/FAILED 를 세우기를 거부한다. 스크리닝 함수는 솔버가
실제로 돌린 부품만 돌려준다. Phase 6 서로게이트가 닫힌 형태 커널보다 느렸던 것과 달리
여기서는 평가기가 메시 + 솔브(부품당 0.2~6초)여서 비율이 뒤집혔다. surrogate/README
가 말한 조건이 처음 충족된 자리다.

**첫 측정은 쓸모없음이었다.** 라벨 40개(생성 100초)로 학습한 모델의 홀드아웃 처짐 R² 는
음수, 중앙 상대오차 0.8. 처짐이 3 데케이드에 걸치는데 표본 40개로 길이의 3제곱을 처음부터
배울 수는 없다. 스크리닝 파이프라인은 그래도 솔버 검증된 승자만 돌려줬다(예측 오차 92%
였어도). 설계대로다.

**물리 프록시 하나가 바꿨다.** 바운딩 박스를 채움률로 축소한 단면의 보 이론 처짐을 특징
하나로 추가했다. 일부러 조악하게: 좋은 프록시는 답 자체가 되고 조악한 프록시는 모델에
배울 잔차를 남긴다. 프록시 단독 R² 0.15. 측정(훈련 40, 홀드아웃 15, 로그 공간 R²):

        프록시 단독                      0.15
        프록시에 지수 피팅                0.53
        기술자 릿지                       0.68
        기술자 + 프록시 릿지               0.91
        기술자 + 프록시 MLP(32)            0.94    중앙 상대오차 0.07, p95 0.47

순위 매기기용이니 순위로도 재봤다: 40개에서 로그 R² 0.97, Spearman 0.99. 테스트 코퍼스
크기인 훈련 20 + 홀드아웃 8 로 무작위 추출 8회: Spearman 최소 0.79(중앙 0.90), 로그 R²
최소 0.41(중앙 0.81), 원값 R² 는 0.29 까지 떨어졌다. 3 데케이드에서 원값 R² 는 큰 부품
둘이 결정하므로 읽을 숫자가 아니고, 그래서 처음 쓴 테스트(원값 R² > 0.7)가 깨졌고 지표를
바꿨다. 하한은 Spearman 0.6, 로그 R² 0.3. 절제(프록시 없음) 결과는 핀 대신 출력으로
남겼다. 20개에서는 프록시 없는 운 좋은 추출도 잘 나올 수 있어 "항상 더 낫다"는 참이
아니다. 40개에서는 참이다(음수 → 0.97).

**p95 0.5 가 이것을 판정이 아니라 순위 매기기로 만든다.** `screen_and_verify_parts` 는
후보 전부를 라벨 없이 만들어(B-rep + STEP 읽기, 부품당 0.05초) 처짐을 예측하고, 예측
가능 후보를 가벼운 순으로, 예측 불가능 후보를 그 뒤에(예측도 근사라 하드 필터 금지) 세운
뒤 상위 k 개만 CalculiX 로 라벨한다. 승자는 솔버 숫자, SIMULATED. 통과한 것이 없으면
None 과 SURROGATE.

**메셔가 두 번 아무것도 안 냈다.** 반경 12.8 mm 샤프트를 9.9 mm 로 메시하면 곡면 위
2차 사면체의 야코비안이 음수가 되어 CalculiX 가 결과를 안 쓴다(6.7 mm 에서는 됨). 판
하나도 같은 증상. 두 치료를 측정했다. Gmsh 고차 최적화는 둘 다 고쳤고, 세 번째 부품에서
C++ 예외로 파이썬 프로세스를 죽였다. 잡을 수 없는 예외를 던지는 메셔는 기본값이 될 수
없다. 곡률 기반 크기(원 12점)는 샤프트를 고치고 판 노드 수를 두 배로 늘리며 0.6 퍼센트
바꿨고, 그 판에서 전에 없던 역전 요소를 만들었다. 채택: 둘 다 아님. 메셔는 준 크기로
메시하고, 라벨러가 솔버 거부 시 0.7배로 최대 두 번 재시도하며 실제 쓴 크기를 note 에
적는다. 세 번 다 실패하면 솔버 메시지와 함께 거부 목록. 재생성한 55개는 전부 라벨됐고
재시도 1건.

**결정 사항.** 테스트 코퍼스는 훈련 20 + 홀드아웃 8(약 90초, slow 마커). 실사용 규모는
부품당 2.5~7초 비용을 적고 사용자에게 남긴다. 새 의존성 없음(torch 재사용).

**하지 못하는 것.** 다섯 패밀리, 한 하중 케이스, 한 재료. 훈련 범위 밖 질의는 외삽이고
모델은 모른다. 피크 von Mises 예측(R² 0.96)은 애초에 수렴하지 않는 숫자의 예측이다. 전부
SURROGATE.

## P8 설계의도: 문장을 저장하지 않고 솔버로 검사한다 (구현 완료)

**무엇이 설계의도인가.** 설계자가 어떤 피처가 무엇을 위한 것인지 말한 것: 벽두께가 링크를
단단하게 만든다, 홀은 클리어런스라 아무것도 받지 않는다, 길이는 리치로 정해진다. 형상만으로
그것을 복원할 수 없다는 것은 로드맵이 이미 말했고, 그래서 피처 인식기는 무엇인지만 말하고
무엇을 위한지는 말하지 않는다. 이 모듈은 반대 길을 간다. 의도를 출처와 함께 **진술**하게
하고(`IntentClaim` 은 `DesignReference` 항목이라 출처·신뢰도·출처등급 없이는 생성 불가,
신뢰도 천장도 그대로 상속), 각 진술을 실제 솔버가 돌리는 **절제 실험**으로 바꾼다: 주장이
가리키는 파라미터 하나를 바꾸고, 두 부품을 Gmsh + CalculiX 로 라벨하고, 수치가 주장대로
움직였는지 본다.

**절제가 말할 수 있는 것.** SUPPORTED: 주장한 방향으로, 메시 잡음보다 크게, 주장한 크기가
있으면 그 크기로(없으면 주장이 "의미 있다"고 한 최소 크기 이상) 움직였다. REFUTED: 반대로
움직였거나 크기가 틀렸거나, 방향은 맞지만 주장이 의미 있다고 한 크기에 못 미쳤다. INCONCLUSIVE:
효과가 두 라벨의 메시 민감도 2배 안이다. 두 번의 solve 로는 잡음과 구분할 수 없고, 정직한 답은
"배운 것이 없다"이다. 잡음 바닥은 라벨러의 mesh_sensitivity(두 메시 추정치, 한계값 아님)라서
바닥 바로 위의 효과는 약한 증거이고 비율을 그대로 보고한다.

**측정 (전부 CalculiX, 부품당 두 메시).**
- hollow_rect 벽두께 2배 → 처짐 x0.625(37% 감소, 잡음 0.5%): SUPPORTED.
- 슬렌더 box 길이 2배, 주장 비율 8(보 이론) → 측정 7.98: SUPPORTED. 솔버가 3제곱을 확인했다.
- 판 홀 2 → 4 → 처짐 +1.8%(잡음 0.3%): 실재하는 효과. "홀은 구조다(20% 이상)"는 REFUTED,
  "홀은 클리어런스다(10% 이내)"는 SUPPORTED. **같은 측정 하나가 반대 주장 둘을 판정한다.**
- box 높이 0.1% 변화 → 처짐 -0.3%, 메시 민감도 0.3%: INCONCLUSIVE, Brain 에 기록 안 함.
- hollow_rect 벽두께 2배 → 피크 von Mises x0.638(잡음 10.4%): SUPPORTED. 피크 자체가
  수렴하지 않는 숫자라는 라벨러의 경고는 그대로 유효하다.

첫 판정 규칙은 방향만 맞으면 지지했고, 그러면 1.8% 로 "홀은 구조"가 지지됐다. 그래서 크기
없는 방향 주장에는 "의미 있는 최소 효과"(tolerance 재사용, 기본 25%)를 요구하게 바꿨다.
1% 움직이는 파라미터는 그 1% 가 실재해도 "링크를 단단하게 만드는 것"이 아니다.

**Brain 연결.** SUPPORTED 는 claim_key 로 통합되는 Knowledge 에 SIMULATION 근거로,
REFUTED 는 반례로, INCONCLUSIVE 는 아무것도. 등급은 사다리가 유도한다: 한 run 은 SIMULATED,
같은 run 반복은 통합만 되고 승격 없음, 반례만 있는 주장은 UNVERIFIED. 물리시험 없이는
EXPERIMENTALLY_VALIDATED 로 못 간다. 설계의도가 Brain 에 **주장이 아니라 근거와 반례를 단
진술**로 남는다.

**하지 못하는 것.** 패밀리 하나·파라미터 하나·하중 케이스 하나씩. 패밀리에 없는 파라미터나
허용 영역을 벗어나는 절제는 근사하지 않고 거부. 의도를 **발견**하지는 못한다. 진술된 의도가
솔버와의 접촉에서 살아남는지 잰다. 전부 SIMULATED.

## P9 생성 CAD 루프: 루프가 STEP 을 내보낸다, 그리고 그것이 전부다 (구현 완료)

**무엇을 했나.** 에이전트 루프에 네 번째 실행기 `generative_cad` 를 붙였다(레지스트리
56번째 방법, DESIGN_GENERATION, FEM3D, HEAVY, 조건은 `cad_family` 표현). 파라메트릭
전략은 한 단면 안을 움직이고 토폴로지 전략은 CAD 커널이 읽지 못하는 밀도장을 키운다. 이
전략은 `core.part_dataset` 의 합성 패밀리(box, hollow_rect, plate_with_holes, 문제 길이
고정, 문제의 단면 포락선 안)를 탐색한다. 후보 전부를 B-rep 으로 만들고 분석기로 읽어 닫힌
형태 부피와 대조한 뒤(부품당 0.05초), **랭커**가 처짐을 예측해 순서만 정하고(기본은 보 이론
프록시, 학습된 형상 서로게이트를 주입 가능, 둘 다 SIMULATED 미만), 상위 k 개만 CalculiX
로 라벨하고, **솔버가** 처짐 한계 안에서 찾은 가장 가벼운 부품이 승자다. 승자의 STEP
경로·패밀리·파라미터·근거등급이 에피소드 genome 에 기록된다.

**랭커는 결정하지 않는다는 것을 테스트로.** 모든 후보에 대해 거짓말하는 랭커(순서 반전,
1000배 낙관)를 주입해도 결과는 솔버가 라벨한 부품이고 그 예측값은 솔버값 옆에 남아 틀렸음이
보인다. 틀린 랭커의 비용은 솔버 시간이 잘못된 부품에 쓰이고 더 좋은 부품을 놓치는 것이지,
검증 안 된 부품이 로그에 들어가는 것이 아니다. `DesignOutcome` 은 세 표현(설계 벡터,
밀도장, 라벨된 부품 레코드) 중 정확히 하나를 들며, CAD 레코드에 솔버 라벨이 없으면 생성이
거부된다. 형상은 설계가 아니다.

**루프 통합.** `cad_family` 표현만 가진 컨텍스트로 라우팅하면 적용 가능한 방법이 하나라
루프가 그것을 실행해야 하고, 에피소드의 `strategy_used` 가 generative_cad 로 시작하며
STEP 파일이 존재하고 미충족 권고가 0 임을 테스트. 실행기 하나 추가로 기존 파라메트릭·
토폴로지 루프 테스트는 그대로 통과(실행 가능 집합을 세는 테스트 하나만 3 → 4 로).

**"생성"이 여기서 뜻하지 않는 것.** 자유형 아님. 학습된 무엇도 형상을 제안하지 않음. 세
패밀리가 펼치는 것 이상의 토폴로지 변화 없음. 형상을 발명할 수 있는 방법은 다른 이름의 다른
방법이고, 이 문단 없이 이것을 생성설계라 부르는 것이 이 프로젝트가 피하려는 과대주장이다.
registry notes 에도 같은 말이 있다. 응력은 제약이 아니다: 라벨러의 피크 von Mises 는 고정
모서리 특이점에 있어 수렴하지 않으므로 detail 에 보고만 하고 판정하지 않는다(토폴로지
실행기와 같은 처짐 제약).

**비용.** 후보 6, 검증 2 로 실행기 한 번에 약 3~5초, 루프 1회 반복 포함 테스트 파일 6개
10초.

**하지 못하는 것.** 세 패밀리, 캔틸레버 하나, 처짐 제약 하나, 재료는 문제의 것. 다음 확장은
패밀리·하중 케이스 확대이고 그 뒤는 하드웨어와 실측이다. 전부 SIMULATED.

## Code_Aster 접촉: xfail 의 원인은 부등식의 부호였다 (구현 완료)

오래 strict xfail 로 남아 있던 Hertz 접촉 테스트. 기록된 증상: 접촉 영역이 평면 아래
9.6 마이크로미터에 앉아 있고 두 물체가 자유롭게 관통, 접촉 반경 0, 피크 응력 386 GPa
(고정 꼭짓점의 점하중 특이점). 갭 조건을 DY >= 0 에서 DY >= -Y 로 고쳐도 열두 자리까지
아무것도 안 바뀌었다는 것이 "조건이 시스템에 들어가지 않는다"는 가장 분명한 증거였다.

**원인.** U4.44.11: LIAISON_UNIL 은 sum(COEF_MULT x ddl) < COEF_IMPO 를 강제하고, 음의
COEF_MULT 가 부등식 방향을 뒤집는 방법이다. 스터디는 COEF_MULT = +1, COEF_IMPO = -Y 로
썼다. 그것은 DY < -Y, 즉 노드가 평면 **아래로** 지나가야 한다는 반대 반공간이다. 그래서
"갭을 고쳐도" 안 바뀌었고, 영역이 일정한 양만큼 평면 아래에 앉았다. Y + DY >= 0 은
-DY < Y: COEF_MULT = -1, COEF_IMPO = Y.

**측정.** 부호를 바로잡자 첫 실행에서 접촉 반경 오차 2.0 퍼센트, 피크 압력 오차 1.3
퍼센트(xfail 이 들고 있던 기준은 10·15 퍼센트). 반공간 비율 1~2 퍼센트. 3초.
`reverse_half_space=True` 로 옛 조건을 재현할 수 있게 남겨서 실패가 기억이 아니라
측정으로 남는다: 그 경우 반경 0, 최저점 음수임을 테스트가 확인한다.

**등록.** 접촉이 검증됐으므로 `analysis.contact.code_aster` 로 등록(57번째 능력).
프로세스 내 hertz_contact 는 같은 닫힌 형태의 산술이라 이것이 첫 solve 기반
교차검증이고, 시스템에서 접촉을 푸는 유일한 방법이다. notes 에 측정 오차와 부호 규약을
적었다. 검증 안 된 것도 적었다: 마찰, 두 변형체, 큰 미끄럼, 비축대칭.

## 물성표: 모든 숫자에 문서를 붙이다 (구현 완료)

**요구.** 산업·로봇·항공우주 부품 재료 세트, 값마다 출처와 온도 범위, 출처 없는 값은
넣지 말고 없음으로.

**스키마.** `MaterialSpec` 에 `sources`(읽은 문서: id, 제목, 발행자, 등급, URL, 문서 날짜,
읽은 날짜)와 `value_sources`(필드별: 어느 문서, 어느 표, 인쇄된 원값과 단위, 조건)를
추가했다. 등급 셋: primary(제조사 데이터시트나 규격을 그날 끝까지 읽음), secondary
(MatWeb·AZoM·MakeItFrom 처럼 출처 없이 숫자를 옮기는 곳, 확인용으로 이름만),
derived(저장값에서 정확히 계산). 검증기가 인용된 문서가 실제로 목록에 있는지, 필드명이
실재하는지, derived 등급이 derived_fields 에도 있는지, 온도 곡선에 출처가 있고 온도가
오름차순인지 확인한다. `fatigue_strength_pa` 는 None 허용으로 바꿨다. None 은 0 이 아니라
"출처 있는 값 없음"이고, 피로 방법 셋(S-N, Miner, 샤프트)은 `require_fatigue_strength_pa()`
를 통해 그런 재료를 거부한다. `service_temperature_k` 와 `temperature_dependence`(출처 있는
곡선만)도 추가. `unsourced_fields()` 가 문서 없는 숫자를 세고 테스트가 0 임을 확인한다.

**읽은 문서(primary).** Kaiser 7075 판재·6061 봉재, Outokumpu 304/304L·316/316L 북미판,
Special Metals SMC-045(INCONEL 718: Table 2·4·5, AMS 최소값), Carpenter·ATI Ti-6Al-4V,
Victrex 450G, EOS PA 2200, Stratasys ABS-M30, NatureWorks 4043D, Hexcel HexPly 8552/AS4.
MatWeb 은 자동 조회에 403 을 돌려줘 다시 읽지 못했다. 그 위에 서 있던 기존 값은 그대로
두되 secondary 로 등급을 낮추고 "이 세션에서 재확인 안 됨"이라고 적었다.

**옮기지 않은 값.** al_7075_t6 의 E 71.7 GPa·밀도 2810·피로 159 MPa 는 이 저장소의 모든
고정 결과(MVP 0.250 kg 등)가 의존한다. Kaiser 판재는 71.0·2800·158 을 준다. 1 퍼센트
미만 차이지만 바꾸면 수십 개 테스트가 움직이므로 기존 값을 두고 Kaiser 숫자를 notes 에
적었다. 반대로 ss_316 은 아무 고정 결과도 의존하지 않아 Outokumpu 값으로 옮겼다(밀도 8000
→ 7889, E 193 → 200 GPa, 항복 290 → 303 MPa).

**추가.** ss_304, inconel_718(E-온도 곡선 294~1366 K, 사용 범위 20~977 K), peek,
steel_4140_annealed. cfrp_ud 는 Hexcel 라미나로 갱신(E1 141, E2 10 GPa, 인장 2207/81 MPa,
전단 114, 압축 1531, 밀도 1580); G12·nu·팽창계수는 Hexcel 에 없어 typical 로 남고 secondary.

**없음으로 둔 것.** 피로강도: 4140 annealed, 718, ABS, PLA, PA12, PEEK. 팽창계수: PLA, PA12.
사용 온도 범위: 알루미늄·스테인리스(출처가 용융 범위나 어닐링 온도만 줌). 등방성 CFRP
항목: 강성은 `quasi_isotropic_modulus_estimate_pa()` 로 3/8 E1 + 5/8 E2 = 59 GPa 을 derived
로 내주지만 강도는 정직하게 유도할 수 없어 항목을 만들지 않았다.

**스키마와 데이터시트가 맞지 않는 자리.** ABS-M30 과 PLA 4043D 는 항복이 파단보다 높다(연성
플라스틱의 네킹). 스키마의 "항복 < 극한" 규칙 때문에 파단값을 yield, 항복값을 ultimate 로
저장하고 value_sources 의 note 에 뒤집었다고 적었다. PEEK 은 항복만 주어 극한을 0.1 퍼센트
위에 두고 측정값이 아니라고 적었다. 이 세 곳은 스키마가 바뀌어야 할 자리이고 숨기지 않았다.

**Poisson 비.** 제조사 시트 어느 것도 주지 않는다(718 만 Table 4 에 0.294). 나머지는
secondary(AZoM, MakeItFrom, 유통사 페이지) 또는 기존 MatWeb 값이며 전부 그렇게 표기했다.

## 데이터셋 인프라: 하중 케이스 5, 패밀리 13, 재료 스케일링, 재개 가능 배치 (구현 완료)

**사양 먼저.** `docs/dataset_spec.md` 에 규모와 비용을 측정값으로 썼다. 부품당 3.2초(두 메시, 두 solve, 55개 평균).
첫 초안은 Poisson 그룹 4개 x 10,400 solve 였다. 측정이 그것을 지웠다: E 2배 → 변위 정확히 0.5배, 응력
1.000; Poisson 0.22~0.40 전 범위에서 굽힘 처짐 변화 0.5 퍼센트 미만, 피크 응력 0.7 퍼센트 미만. 그래서
형상 x 하중 케이스당 solve 하나, 나머지 18 등방성 재료는 정확한 스케일링(질량 ∝ 밀도, 변위 ∝ 1/E,
비틀림 ∝ 1/G, 열 변위 ∝ α, 응력 불변). 13 x 5 x 100 = 6,500 solve ≈ 8 워커 45분 → 약 117,000 레코드.
하루 문턱 아래라 사용자 결정 불필요.

**하중 케이스.** 굽힘(있음) + 축·비틀림·굽힘+비틀림 조합·열 구배. CalculiX 덱 작성기에 노드별 힘 벡터와
온도장(*EXPANSION, *TEMPERATURE)을 추가했다. 각 케이스는 라벨을 붙이기 전에 슬렌더 정사각 바에서
닫힌 형태와 대조: 축 신장 F L/(E A) 오차 0.36%, 원기둥 비틀림 T L/(G J) 0.04~0.10%, 정사각 비틀림
(Roark 상수 0.1406) 1.5%, 열 구배 α g L²/2 2.2%(고정면이 곡률을 방해), 조합은 굽힘·비틀림 단독과 각각
0.1%·1.6% 일치. 첫 조합 구현은 굽힘 병진을 비틀림으로 읽어 0.12 rad 을 냈다. 면 평균 변위를 뺀 뒤
투영하도록 고쳤다. 열 케이스는 팽창계수 없는 재료(PLA, PA12)를 거부한다.

**스케일링의 잔차.** 케이스마다 alumina·PEEK·304 를 직접 풀어 스케일링과 비교(scaling.py 에 표):
변위 잔차 0.2~0.8%, 응력 잔차 0.3~1.7%, 열 응력만 5.85%(고정면의 측방 팽창 구속이 Poisson 에 직접
의존). 케이스별 (변위, 응력) 한계를 측정값 위에 핀했고 스케일된 라벨은 derived 등급, 참조 재료와 잔차
한계를 note 에 든다. 측정 안 된 케이스는 스케일 불가로 거부.

**패밀리 8 추가**(bracket, flange, housing, keyed_shaft, gear_blank, link, mount, ribbed_plate), 각각 닫힌
형태 부피와 피처 기대값. 둘이 첫 시도에서 엔진에 거부됐다: housing 은 공동 위치가 틀렸고(부피 불일치),
link 의 둥근 끝은 인식기에게 필렛(양쪽 긴 면에 접함)이라 기대값을 측정대로 바꿨다. 규칙 분류기는 이
여덟을 UNKNOWN 이라 하고 그것이 옳다(규칙은 다섯에 대해 측정됐다).

**배치.** 셀(패밀리 x 하중) 단위 JSONL + done 파일 + refused.jsonl. 중단 시 최대 부품 하나 손실, 재실행은
done 집합을 건너뜀(테스트: 2개 → 4개 실행 시 새 2개만 라벨). 전체 스위트가 하나 더 찾았다: seed 0 의 첫 mount 는 coarse 크기에서 표면 메시가
실패(Gmsh overlapping facets)했고 라벨러는 solver 거부만 재시도했다. 이제 메셔 실패도 같은 방식으로
재시도한다(12.57 mm 실패, 8.80 mm 성공). mount 20개(seed 1)에서 거부 0, 재시도 5. 같은 부품을 소수
다섯 자리로 반올림하면 세 크기 모두 메시는 되지만 CalculiX 가 2차 사면체 8개의 nonpositive Jacobian 으로
거부한다. 곡면 위 2차 노드의 알려진 한계이고, 그런 부품은 숨기지 않고 거부 목록에 남는다. `expand_materials` 가 등방성 재료 전부로
스케일 레코드를 만든다. 실사용 규모 생성은 돌리지 않았다. 사양대로 45분 남짓이고 사용자가 정한다.

## 형상미학 B, 제조성 C (구현 완료)

**B, 형태 선호의 소비.** `DesignReference` 의 FormTarget 은 계산·기록만 되고 소비되지 않았다(이 문서 앞
절에 그렇게 적혀 있다). `geometry/aesthetics/form.py` 가 어휘 넷(compact, smooth, symmetric, slender)을
ShapeMetrics 의 한 숫자씩에 대응시켜 [0,1] 점수로 만들고, 가중치는 기존 `aesthetic_weights`(가중치 x
신뢰도)를 쓴다. 결과는 `DesignEntry.form_score` 로 들어가 허용 가능한 설계끼리만 순위를 매긴다. 어휘 밖
목표는 버리지 않고 unmapped 로 나열. 사다리에 칸이 없고, 실패한 설계를 구할 수 없음을 리뷰 통과
테스트로 확인.

**C, 공정별 DFM 규칙.** `geometry/manufacturability/`. 규칙은 읽은 설계 가이드(Protolabs Network 의 CNC·
FDM·SLS·금속 3D 프린팅·사출 가이드, SheetMetal.Me, AMN Engineering 다이캐스팅)에서 인쇄된 문장 그대로
인용하고 URL·읽은 날짜를 단다. 측정은 삼각화된 B-rep 위의 numpy 레이캐스팅: 벽두께(안쪽 법선 레이의
첫 교차, 3 mm 튜브에서 3.000 mm), 오버행 면적 비율(빌드 방향 기준, T 형에서 정확), 6축 공구 접근
(레이가 공구 쪽으로 탈출하고 면이 공구를 등지지 않으면 도달; 밀폐 공동은 정확히 내부 면적 비율만큼
미도달), 기존 draft 로 드래그 면적. 등급은 `rule_based_dfm_guideline` 이고 EvidenceLevel 이 아니다.
측정 못 하는 규칙(굽힘 반경, 홀-굽힘 거리, 리브, 축대칭)은 unassessed 로 나열되고 통과로 세지 않는다.

**측정이 고친 것.** (1) 삼각화 법선의 절반이 안쪽을 향했다(REVERSED 면의 와인딩): 오버행이 0, 두께
표본이 절반만 나옴 → `tessellate` 가 면 방향에 따라 와인딩을 뒤집는다. (2) 첫 공구 접근 기준(레이가 그
면을 먼저 맞혀야)은 포켓 벽을 전부 미도달로 판정했다. 엔드밀은 측면으로 벽을 깎으므로 기준을
"레이 탈출 + 등지지 않음"으로 바꿨다. (3) trimesh 의 thickness 는 rtree 가 필요해 쓰지 않았다.

**하지 못하는 것.** 규칙은 규칙이다. 전부 통과해도 가공 가능하다는 증명이 아니고, 하나 실패해도 어느
공장은 만든다. 언더컷 측정은 축 정렬 패밀리에서 항상 0 이라 증명하는 바가 없어 넣지 않았다.

## 카탈로그 표준 부품: 치수에 출처를 달다 (구현 완료)

`geometry/cad_export/standard_parts.py`. ISO 4762 소켓 볼트(dk, k, s: Engineers Edge 표), ISO 4032 육각
너트(s, m: AmesWeb 표), GT2 2 mm 풀리(PD = 2N/π, OD = PD − 0.508 mm; PowerDrive 카탈로그 G-85/86 의 모든
행에서 PD − OD = 0.020 in 이라 PLD 0.254 mm 를 표에서 유도), GT2 벨트 길이(피치 x 이 수, 정확),
히트셋 인서트(Ruthex/CNC Kitchen 계열 OD·길이, Creative3DP 표). 볼트·너트는 나사 없는 외피(외형 치수는
규격), 풀리는 이 없는 외피(`geometry_is_real=False`), 인서트는 널링 없는 원통. 부피는 전부 닫힌 형태와
1e-6 대조. 재료 연결: 볼트·너트 → steel_scm440(가장 가까운 항목이라고 적음), 풀리 → al_6061_t6(합금
미표기, 가정 표기), 인서트 → 황동은 DB 에 없어 None. 모터·감속기는 기존 placeholder 그대로.

## Gazebo 교차검증: 두 정역학이 0.03 퍼센트로 만나다 (구현 완료)

**머신 측정.** Gazebo Fortress 6.18.0 + ROS 2 Humble ros_gz 설치됨, `ign sdf -p` 로 URDF→SDF 가능, 헤드리스
서버 200 스텝 2.3초. Isaac Sim 없음. 요구사양(VRAM 16 GB, RAM 32 GB) 대 이 머신(4 GB, 15 GB)이라 설치를
권하지 않고 Gazebo 경로만 구현.

**방법.** URDF 에 외피 상자(단면 외형 x 길이, 파일 안에 "외피" 주석)를 추가하고, 자세 q 를 조인트 원점에
접어 넣어 모든 조인트가 0 에서 시작하게 한 뒤, 조인트마다 비틀림 스프링(rest = τ/k)을 SDF 에 써서 시작
자세에서 정확히 τ 를 가한다. τ 가 자세를 잡는 토크면 조인트는 0 근처에 머물고, 틀리면 스프링이 부족분을
채우는 각도까지 처진다. 정지 상태에서 스프링 토크 = 엔진이 계산한 그 자세의 중력 토크이므로, 처진
자세에서 이 프로젝트의 Jacobian 정역학과 비교하면 두 독립 정역학(우리 Jacobian, DART 구속 솔버)의
비교가 된다.

**측정(2링크 평면 팔, k = 2 N m/rad, 4초).** 정역학 토크: 처짐 −0.0002 rad, 스프링 대 정역학 0.03%. 영
토크(대조군): 어깨 0.44 rad 처짐, 처진 자세에서 0.03% 일치. 절반 토크: 0.04%. 대조군이 있어 이 검사는
실패할 수 있는 검사다.

**시도했고 남기지 않은 것.** JointPositionController 로 잡고 비례 오차에서 토크를 읽는 길: 목표에서 0.3
rad 떨어진 곳에 앉고 토크 추정이 정역학의 16배. 이 빌드의 플러그인 의미를 확정하지 못해 제거.
ApplyJointForce 에 `ign topic -p` 단발 발행: 메시지가 도착하지 않음(정역학·영·음의 토크에서 처짐 동일).
캡처 결함 둘도 찾았다: 메시지 N 개 요청은 처음 N 개를 주고(시각 0), 파이프는 64 KB(약 65 메시지)에서
막힌다. 둘 다 코드 주석에 남겼다. 실시간 계수 1 로 돌려야 스트림이 따라온다.

**간섭.** 엔진이 아니라 기하로: 외피 상자를 우리 순기구학으로 놓고 CAD 커널로 쌍마다 교집합 부피.
접힌 팔에서 link1·link2 외피가 겹침을 검출하고, 인접 링크는 기본 제외. 외피의 간섭이지 부품의 간섭이
아님을 요약문이 말한다.

**등급.** 시뮬레이션 둘의 일치, SIMULATED. 새 능력으로 등록하지 않았다. 교차검증이다.

## 실측·평가 가이드라인 (문서)

`docs/measurement_guideline.md`. 측정 항목(치수·질량·처짐·비틀림·고유진동수·응력·모터 토크·온도)마다
장비·분해능·일치 허용오차와 주의(고정부 컴플라이언스가 캔틸레버 시험의 최대 계통오차, 피크 응력은
특이점이라 측정 불가라 게이지 위치에서 비교), 순서(부품 치수·질량 먼저, 고정구 측정, 하중 단계·이력,
장착 3회, 비교 전 기록), 기록 JSON 형식과 로더 검증 규칙(예측값은 저장 라벨과 같아야 하고 손으로 친
예측은 거부, 공정 허용오차 밖 부품은 설계 시험이 아님), Brain 연결(통과 → 그 진술 하나에
PHYSICAL_TEST 근거, 실패 → 반례). 이 문서는 절차이고 근거가 아니며 실측 데이터가 들어오기 전엔 어떤
등급도 오르지 않는다고 명시.

## 산업 데이터셋 위의 서로게이트와 임베딩 재측정 (구현 완료)

**무엇이 달라졌나.** P7 서로게이트는 40개 부품, 한 하중 케이스, 한 재료였다. `core/part_dataset/
industrial_surrogate.py` 는 생성 run 을 그대로 먹는다: 해석 6,430 부품과 그 스케일 복제본, 13 패밀리,
5 하중, 18 재료, 총 113,152 행. 피처 43개(기술자 22 + 치수·재료·하중 크기 로그 + 하중 종류 원핫 +
하중별 닫힌 형태 프록시 4종), 타깃 3개(하중별 primary_response, 최대 변위, 피크 von Mises).

**분할.** 셀별 뽑기 순서의 뒤 20%. 이렇게 하면 모든 패밀리와 모든 하중이 홀드아웃에 들어가고, 스케일
복제본은 원본과 같은 쪽에 남는다. 복제본은 원본과 정확한 재료 배수만큼만 다르므로 반대쪽에 놓이면
누출이고, 테스트가 이 불변식을 검사한다.

**측정(홀드아웃 22,578행).** primary_response Spearman 0.997, log R² 0.994, 중앙 오차 0.068, p95 0.375.
하중별 Spearman 0.99~1.00, 패밀리별로는 link 0.955(p95 1.17)와 gear_blank 0.993 이 최악, keyed_shaft 가
최고(p95 0.10). 재료별 편차는 0.001 이내인데, 이는 재료에 대한 결과가 아니라 정확한 스케일링이
그래야 한다는 확인이다.

**정직하게 만드는 기준선.** 한 패밀리·한 하중 안에서 부품은 대체로 크기만 다르고 보 공식이 이미 그
순서를 안다. 그래서 프록시 단독을 같은 행에 대해 재서 나란히 둔다: Spearman 0.878, log R² 0.652,
중앙 오차 0.358, p95 0.995. 패밀리별로 bracket 0.47 부터 0.99 까지. 모델이 버는 것은 순위 상관 약 0.12
와 중앙 오차 5배 감소다.

**그리고 프록시는 더 이상 필수가 아니다.** 40개일 때는 프록시를 빼면 log R² 가 음수였다. 90,574 행에서
같은 제거의 대가는 Spearman 0.997, log R² 0.994, 중앙 오차 0.073(있을 때 0.068)로 거의 없다. 데이터가
3차식을 대신 가르쳤다. 프록시는 닫힌 형태 하나씩의 비용이고 작은 run 을 쓸 수 있게 하므로 남긴다.

**피크 응력은 여전히 라벨의 문제다.** 두 메시 사이 mesh_sensitivity 가 구멍 있는 부품에서 0.5 에
이른다. 완벽히 재현해도 메시 의존 숫자를 재현하는 것이므로, 순위를 요구받는 양은 primary_response 다.

**임베딩(P6) 재측정.** 13 패밀리 x 120 부품, 같은 분할: 기술자 0.99, PointNet 0.95, D2 히스토그램 0.64.
5 패밀리일 때는 1.00 / 0.88 / 0.64 였다. 데이터가 학습 공간을 0.88 에서 0.95 로 올렸지만 순서는 그대로다.
기술자가 이긴다는 것은 이 패밀리들이 개수와 모멘트로 갈린다는 뜻이지 기술자 일반에 대한 진술이 아니다.

**등급.** 예측은 전부 SURROGATE 이고 `screened_check` 만 낼 수 있다(SCREENED, 판정 아님). 유효 범위는
run 이 뽑은 파라미터 범위, 그 하중 크기, 그 재료들이다. 밖은 외삽이고 모델은 그것을 알지 못한다.

## Method Registry와 향후 방법 라이브러리 확장
registry에 등록된 방법이 시스템의 능력 상한이다. 지금 **없는** 것을 적어 둔다. 이후 단계는 이것들을 하나씩 **검증하고 등록하는** 일이며, 검증 없이 등록하는 것은 금지된다.

**구조 해석**
- **좌굴**: (Phase 16에서 **해석적 오일러 완료**) 남은 것은 **3D FEM 선형좌굴 고유치**(기하강성 Kσ)와 **국부 좌굴**(박벽 웹·플랜지). 해석적 오일러는 전체 기둥 좌굴만 보고 얇은 벽이 국부적으로 접히는 것은 못 본다.
- **피로**: (Phase 16에서 **S-N + Goodman/Soderberg 완료**) 남은 것은 **Miner 누적손상**(가변진폭 스펙트럼), **저주기/변형률-수명**, **노치·표면·치수 계수**.
- **열**: (Phase 18·25에서 **완료**, 모터 정상상태 + 구속 열응력 + CTE 15종 + 전도/대류/복사 + 직렬병렬 저항망 + Biot 검사가 붙은 집중 과도) 남은 것은 **분포(PDE) 과도 해석**, **온도장/구배로 인한 열응력**(구속이 없어도 생긴다), CFD, **접촉 열저항**(빼면 온도를 과소예측), **확산 저항**, **온도의존 CTE·물성**.
- **접촉**: (Phase 19·23·24에서 **완료**, 볼트 이음 + 압입 + 헤르츠 점·선 접촉 + Kt) 남은 것은 **마찰·프레팅**, **일반 3D 접촉**(비헤르츠 형상), 소성 접촉. `physics/collision.py`는 여전히 스텁이다.
- **비선형**: 대변형, 소성, 좌굴 후 거동. 현재 FEM은 선형탄성·미소변형 전용이다.
- **모달·다중모드**: 고유진동수, 모드형상, 공진 회피.

**재료**
- **완전 이방성**: 지금은 등방과 직교이방까지다. 21개 독립상수의 일반 이방성은 없다.
- **온도의존 물성**: E(T), 항복(T).
- **CLT(고전 적층판 이론)**: (Phase 21에서 **완료**, ABD + ply 응력 + Tsai-Wu first-ply) 남은 것은 **진행성 손상**(첫 ply 이후 하중 재분배), **모드 기반 기준**(Hashin·Puck), **자유단 층간응력**(실제 층간분리 지점), **습열 항**(경화 잔류응력·수분), 좌굴·후좌굴.

**형상·구조 생성**
- **형상 최적화**: 경계를 직접 움직이는 방식. 지금은 밀도장(SIMP)뿐이고, 그래서 Phase 14a가 깨끗한 필렛을 못 만든다.
- **Lattice·주기적 미시구조**: 균질화 기반.
- **Implicit·레벨셋 표현**: 밀도 임계처리 없이 경계를 직접 다룬다.

**최적화**
- **MMA**: 응력제약 토폴로지의 표준 최적화기. 지금은 벌점 투영법이고 제약 경계에서 진동한다.
- **베이지안 최적화**: `optimization/bayesian.py`는 여전히 스텁이다. 비싼 평가에서 필요하다.
- **다목적 최적화**: (Phase 20에서 **완료**, NSGA-II + Pareto front + 재료가격) 남은 것은 **이산·범주형 변수 연산자**(현재는 재료마다 따로 돌려 front를 합침), **제조성 목적**(단순 프록시가 아닌 것), 참조점 기반 다수목적(4개 초과), 대리모델 결합 NSGA-II.

**Phase 22~28에서 추가된 것과 그 뒤에 남은 것**
- **통합 캐스톤**(22): 결합 판정 + 커버리지 + §29 리뷰 완료. 남은 것은 **순차 사이징 루프를 닫는 것**(더 무거운 링크 → 더 큰 모터 → 더 무거운 관절), 검사 간 결합(베어링 마모가 정렬을 바꾸는 등).
- **기계요소**(23): 키·스플라인·압입·용접·ISO 286 완료. 남은 것은 **용접 피로**(모재와 전혀 다르다), **GD&T 공차누적**, 나사 스트리핑, ISO 286의 **표로만 주어지는 편차**(p·r·s·u).
- **재료역학**(24): 응력변환·비원형 비틀림·용기·헤르츠·Kt 완료. 남은 것은 **뒤틀림 구속 비틀림**, **외압 용기 좌굴**, 측정된 **Kf**(노치 민감도), 3D 응력집중.
- **유체**(26): 내부유동·항력·유공압·냉각 완료. 남은 것은 **CFD**, **압축성 유동**, **펌프 곡선 정합**, **캐비테이션**, 전이영역의 실제 물리.
- **규격 준수**(27): 선호수·스냅·치수 확인 완료. 남은 것은 **GD&T(ISO 1101) 실질 구현**, **ISO 6336 기어 등급 실계산**, 재고 가용성 데이터.
- **설계검토**(28): 다중검토·최소치수·단방향 연성 완료. 남은 것은 **양방향(수렴) 연성**, 요소 간 결합을 닫는 전역 최소화.

**기계요소** (Phase 17·19·23에서 상당 부분 완료)
- **베어링 L10 수명**: (Phase 17에서 **완료**) 남은 것은 **ISO 281 보정계수**(신뢰도 a1, 윤활·오염 a_ISO), 상세 접촉피로, 앵귤러·테이퍼 형식.
- **샤프트 설계**: (Phase 17에서 **완료**, DE-Goodman 복합하중) 남은 것은 **중공·단차 단면**, 측정된 Kf(현재 [ASSUMED]), **부착질량 포함 위험속도**, 3D 샤프트 FEA.
- **볼트 이음**: (Phase 19에서 **완료**, 프리로드·하중계수·분리·볼트피로) 남은 것은 **볼트군과 편심하중**, **전단 이음**, **풀림**(진동에 의한 프리로드 상실), 각도법 조임.
- **기어**: (Phase 19에서 **완료**, Lewis 굽힘 + Hertz 접촉) 남은 것은 **전체 AGMA 계수**(동하중 Kv·하중분포 Km·용도 Ko·치수 Ks를 계산으로), **유성·하모닉 내부**, 치형 수정, 윤활막.

## Post-Phase-7 업그레이드 로드맵 (Phase 8~10)
확정된 방향의 기록이다. 지금 착수하지 않는다.

### Phase 8: 재료 전면 확장 + 이방성
- 재료 DB를 전 물질군으로 확장: 금속에 더해 비금속(고분자, 세라믹, 복합재, 엘라스토머).
- **물성 추론층**: DB에 없는 재료의 값을 **불확실성과 출처를 함께** 추정한다. 기존 evidence-level 규율을 그대로 따른다. 추정값을 데이터시트 값처럼 표기하는 것은 금지다. 비금속은 온도·변형률·습도 의존이고 복합재는 공정 의존이므로 단일값이 아니라 조건과 범위로 다룬다.
- **물리 확장**: 등방성 E/ν에서 직교이방성·이방성 강성텐서로. 재료 스키마에 방향별 물성(E1/E2/E3, G12/G13/G23, ν12/ν13/ν23)과 복합재 적층·배향을 추가한다. FEM 요소는 직교이방성 벤치마크로 재검증한다.
- 기존 "재료 확장 로드맵"의 3단계(이방성)를 구체화한 것이 이 Phase다.

### Phase 9: CAD 출력 (STEP)
- OpenCascade 계열(pythonocc, CadQuery, build123d)로 B-rep을 만들고 STEP으로 내보낸다.
- **난이도를 정직하게 구분한다**: 파라메트릭 솔리드에서 STEP 출력은 간단하고 보장할 수 있다. 반면 토폴로지 최적화 결과나 유기적 형상은 깨끗한 STEP이 어렵다. 그 경우 메시·STL 경로나 표면 재구성이 필요하다. "항상 STEP"은 파라메트릭 경로에서만 보장되며, 유기 형상은 별도 경로임을 명시한다.

### Phase 10: 어셈블리와 기구학
- 단일 부품에서 다물체 조립으로: 링크와 조인트(회전, 직동 등), 구속조건 데이터 모델, 운동학 표현(DH 또는 URDF 유사), 다물체 하중 전달.
- 최종 비전(링크 → 조인트 → 감속기 → 다리 → 휴머노이드)의 축이며 기구학·동역학 원칙을 도입한다.

**순서와 의존성**: Phase 8과 9는 서로 비교적 독립이라 병행 가능하다. Phase 10이 가장 크고 앞의 둘 위에 얹힌다(이방성 재료로 만든 부품을, CAD로 내보낼 수 있어야 조립이 의미를 갖는다).

## 7. 현재 상태
**Phase 1 완료.** Phase 0(venv+스켈레톤+프로파일+Warp/torch 새너티) 위에 Engineering IR(문제 표현), 재료 DB, Design Genome(중공 사각단면 MVP)을 구현. 역할 분리: IR=문제(고정: 길이·하중·재료·제약·목표), Genome=설계변수(가변: 단면 치수).
- 테스트 100개 통과. 단면2차모멘트 공식은 해석식과 별개의 수치적분(격자 리만합)으로 독립 검증(최악 상대오차 ~1e-5, 허용 1e-4).
- 재료는 MVP 기준 등방성 Al 2종(al_7075_t6, al_6061_t6)만. 확장은 위 "재료 확장 로드맵" 순서.
- Warp 1.16.0 + torch 2.13.0+cu130, RTX 3050(sm_86) 인식·커널 검증. 자동감지 → laptop_4gb.

**다음 단계는 Phase 2, Warp GPU 물리 코어** (보/링크 응력·처짐·질량, 미분가능, 해석해 대조 검증).
