# Daedalus Engineering AI: 개념설계 (v0)

> 이 문서는 자율 엔지니어링 AI 비전을 개발 머신(RTX 3050 4GB, 확장 예정)의 실제 실행 계획으로 내려앉힌 것이다. 에이전트/CLI 브랜드는 **Daedalus**.

## 0. 설계 원칙
1. **비전은 그대로, 하드웨어는 현실**: 3050 4GB에서 "지금 돌아가는 최대치"로 시작하되, 큰 GPU로 옮기면 코드 수정 없이 규모만 커지도록 **프로파일 옵션**으로 스케일을 분리.
2. **시스템 불가침**: 모든 파이썬·CUDA 의존성은 `.venv` 안에만. 시스템 파이썬(3.10.12)·드라이버 건드리지 않음.
3. **CLI 우선**: 자율 루프·모니터링은 터미널(rich TUI + JSONL 로그). 웹 GUI는 `interfaces/api/` 자리만 비워두고 후순위.

## 1. 기술 스택 (하드웨어 반영)
| 계층 | 선택 | 이유 |
|---|---|---|
| 물리 코어 | **NVIDIA Warp (warp-lang)** | pip·venv 설치, 자체 JIT(시스템 CUDA 툴킷 불필요), 미분가능, sm_86 지원, 확장성 |
| ML surrogate | **PyTorch (cu13x)** | CUDA 동작 확인, Warp↔torch 텐서 연동 |
| 최적화 | Warp autodiff + 파이썬 진화/베이지안 | 4GB에선 GPU 배치평가 + CPU 메타옵티마이저 |
| Geometry | implicit/SDF(파이썬+Warp) 우선, CAD 후순위 | 초기 topology엔 voxel/level-set이 적합 |
| Orchestration | Python + Typer CLI | |
| 모니터링 | rich/textual TUI + JSONL | CLI-only, tmux attach로 관찰 |
| 추론(LLM) | 외부 세션 오케스트레이터 | 상위 오케스트레이션은 엔진 바깥의 LLM 세션이 담당 |

## 2. GPU 프로파일 시스템 (핵심)
스케일을 코드에서 분리 → `configs/profiles/*.yaml`. 선택 우선순위: `--profile` → `ENG_PROFILE` → VRAM 자동감지 → default fallback. 로더: `core/profile.py`.

| 프로파일 | VRAM | 성격 |
|---|---|---|
| laptop_4gb | 4GB | 현재. 후보풀 작게, 저해상, AMP+grad checkpoint |
| desktop_16gb | 16GB | 중형 확장 |
| rtx5090_32gb | 32GB | 고대역폭·빠른 solve. 이 워크로드 속도 최적 |
| dgx_spark_128gb | 128GB | 대용량·저대역폭. 큰 문제/로컬LLM용, solve 속도는 낮음 |
| cloud_a100 | 80GB | 대량 병렬 |

## 3. 하드웨어 사이징 가이드 (참고)
VRAM을 잡아먹는 3요인: ①3D 토폴로지 해상도(res³×필드×4B) ②동시 후보 수 ③surrogate 모델. 병목은 항상 "후보수×해상도"이며 프로파일이 상한을 건다.
- **4GB(현재)**: 전 단계 개발 + MVP 가능. ~256³, 후보 ~수백~1천.
- **32GB(5090)**: 이 프로젝트 대부분을 빠르게. 대역폭 1.8TB/s로 solve가 빠름.
- **128GB(DGX Spark)**: 용량은 크나 대역폭 273GB/s로 solve 느림. 로컬 대형 LLM·초대형 단일문제에 유리.
- **멀티GPU**: 독립 후보 병렬은 선형 확장(주 워크로드에 최적). 단일 거대문제 분산은 NVLink 없는 5090에선 PCIe 병목. → 설계에 device-pool(후보 배치 샤딩) 개념 예정.

**판단 기준**: "solve를 빨리·많이" = 5090. "거대한 걸 통째로 담기/로컬 LLM 두뇌" = DGX Spark. 지금은 하드웨어 구매 불필요: 3050으로 전 파이프라인 구축 후 천장에 닿을 때 선택. 어느 쪽이든 `--profile`로 흡수.

## 4. 자율 루프 매핑
MD의 REASON→PLAN→DESIGN→ACT→SIMULATE→EVALUATE→LEARN→UPDATE_BRAIN을 현 구조에 매핑:
- 추론/계획/관찰/학습(LLM층) = 외부 LLM 세션 오케스트레이터
- 설계생성/물리/최적화/평가(엔진층) = venv 파이썬+Warp 프로세스
- Brain = 레포 내 구조화 저장소(초기 SQLite+JSONL, 후에 벡터+그래프)

## 5. 단계 계획 (물리엔진 조기투자 반영)
- **Phase 0 (완료)**: venv + 스켈레톤 + 프로파일 + Warp/torch GPU 새너티.
- **Phase 1**: Engineering IR + Design Genome (단일 링크 파라메트릭).
- **Phase 2**: Warp GPU 물리 코어: 보/링크 응력·처짐·질량, 미분가능. 해석해 대조 검증.
- **Phase 3**: 최적화 루프: Warp autodiff 경사 + 진화탐색, 4GB 내 다후보 배치.
- **Phase 4**: 자율 루프: 세션이 엔진 구동·관찰·재계획.
- **Phase 5**: Brain: 에피소드/전략/지식.
- **Phase 6**: Surrogate(torch): 데이터 축적 후.
- **Phase 7**: 고정밀 검증 + 확장 프로파일.

MVP 목표(MD §34): "하중 받는 단일 로봇 링크의 최소 질량 설계" 루프 1회 완주.

## 재료 확장 로드맵
재료 확장은 **물리가 감당할 수 있는 순서**로 진행한다. 스키마와 솔버가 못 받는 재료를 먼저 넣으면 값은 들어가도 계산이 틀린다.

- **1단계 (현재/MVP)**: 등방성 Al 2종만: `al_7075_t6`, `al_6061_t6`.
- **2단계: 등방성 금속 세트 확장**: 6061 / 7075 / 2024, S45C / SCM440, SS316, Ti-6Al-4V, AZ31 등. **현재 스키마·선형탄성 물리를 그대로 수용**하므로 코드 변경 없이 데이터만 추가하면 됨. 각 항목은 출처 있는 값 + `status` 태그 필수.
- **3단계: 이방성 재료 추가**: CFRP(방향별 물성·적층 구성), 3D프린팅 플라스틱(공정 의존·직교이방성).
  **선행 조건: 스키마 확장(방향별 물성 필드) + 이방성 솔버 지원.** 단일 E/항복/포아송 스키마에 이방성 재료를 밀어넣으면 물리가 틀리므로, 선행 조건이 갖춰지기 전에는 추가 금지.

## 모델 충실도 (Phase 2 시점)
Phase 2의 물리는 **Euler-Bernoulli 보 이론**이다. 미분가능하고 배치 평가가 싸다는 게 장점이지만, 다음을 **반영하지 않는다**:
- **근부 응력집중 미반영**: 필렛·볼트홀·체결부 형상을 못 봄. 실제 근부 최대응력은 여기서 보고하는 값보다 **높다**. 보고된 응력은 실제 peak의 하한으로 취급할 것.
- **전단변형 미반영**: 처짐은 굽힘 성분만. 세장비가 큰 링크에선 타당하나 뭉툭해질수록 틀림.
- **좌굴 미검사**: 얇은 벽은 항복 전에 국부 좌굴로 먼저 망가질 수 있는데 감지 못 함.
- 균일 프리즘 단면·완전 재료 가정(테이퍼·조인트·엔드캡·용접부·잔류응력 없음), 정적 해석만.

따라서 이 단계를 통과한 설계는 **후보**이지 검증된 부품이 아니다. 응력집중·전단·좌굴은 후속 **3D FEM 고정밀 단계**에서 해소하며, 설계는 그 관문을 통과해야 실물로 취급한다. (값싼 미분가능 모델로 공간을 탐색하고, 비싼 고정밀 모델로 승자를 확정하는 해석↔FEA 분업.)

## 6. 운영 규칙 (필독)
- **venv는 깨끗한 PYTHONPATH에서 실행할 것(`env -u PYTHONPATH`).** 셸에 source된 다른 환경이 PYTHONPATH를 내보내면 그쪽 패키지가 venv 패키지를 가려서, 버전이 다른 numpy 같은 게 조용히 먼저 잡힌다. 부트스트랩 설치 스크립트가 이 래핑을 자동으로 처리하는 것을 목표로 한다.
- venv는 sudo 없이 `--without-pip` + get-pip 부트스트랩으로 구성됨(시스템 미변경).
- git init만 됨, 커밋은 사용자 확인 후.

## 최적화 결과의 전제 (Phase 3)
MVP 링크의 최적해는 **처짐 지배(deflection-limited)**다. 응력 제약은 70% 넘게 여유가 남고(안전율 ~14, 요구 2) 팁 처짐만 1 mm 한계에 붙는다. 즉 이 링크가 지불하는 비용은 강도가 아니라 **강성**이다.

주의할 점: 설계변수 3개 중 **2개가 bound에 붙어서** 끝난다: b는 하한 10 mm, t는 하한 1 mm. 따라서 최적 질량은 물리만큼이나 **bound 설정이 좌우한다.** 특히 `t_min = 1 mm`는 CNC 알루미늄 가정의 **[ASSUMED]** 제조성 한계이지 유도된 값이 아니다(`core/design_genome/bounds.py`). 공정이 바뀌면(적층 제조, 압출, 판재 성형) 이 값이 바뀌고 최적 질량도 함께 바뀐다. 최적화 결과를 "달성 가능한 질량"으로 취급하기 전에 이 가정부터 확인할 것.

## 자율 루프의 "추론"에 대한 정직한 범위 (Phase 4)
Phase 4의 reasoner는 **결정론적 규칙기반 휴리스틱**이다. **LLM이 아니다.** 탐색/활용 스케줄을 따를 뿐이며, 이걸 "AI 추론"이라고 부르면 과대주장이다. 코드·README·이 문서 모두 휴리스틱이라고 명시한다.

의미가 있는 부분은 **확장점**이다. `Reasoner`는 `decide(state, history) -> Action` 하나짜리 추상 인터페이스이고, 루프 엔진은 그 너머를 보지 않는다. 따라서 LLM 기반 정책을 나중에 엔진 수정 없이 끼워넣을 수 있다. 전체 시스템에서 LLM은 이 클래스가 아니라 **바깥 오케스트레이터**(이 엔진을 구동하는 세션)에 해당한다.

루프가 단발 최적화보다 나은 이유: SLSQP는 가장 가까운 KKT 점으로만 수렴하는데, 설계변수 3개 중 2개가 bound에 붙는 이 문제에서는 "다른 basin이 더 나은가"가 실제 질문이다. 루프는 그래서 **multi-start 오케스트레이터**다: exploit은 현 best 주변 정제, explore는 무작위 재시작, 정체 시 강제 탐색. 국소최적·bound 견고성이 여기서 나온다.

## Brain의 정직한 범위 (Phase 5)
Brain은 **"실험 검증된 사실 저장소"가 아니라 "증거수준이 표시된 경험 저장소"**다. 세 가지를 명확히 한다.

1. **모든 지식은 시뮬레이션 유래**다. Phase 2 충실도(Euler-Bernoulli, 응력집중·전단·좌굴 미반영)를 그대로 상속하며, 각 지식항목의 `assumptions`에 그 사실이 기록된다.
2. **검색은 수치 특징 유사도이지 의미검색이 아니다.** 임베딩 모델이 없고 "의미" 개념도 없다. 설계의 공학량(문제 파라미터·설계변수·metric)을 공통 스케일에 올려 비교할 뿐이다. 그래서 API 이름도 `semantic_search`가 아니라 `retrieve_similar`다. 텍스트 임베딩은 모델 의존성이 필요해 이번 단계에서 의도적으로 배제했고, 확장점으로만 문서화했다.
3. **EXPERIMENTALLY_VALIDATED는 시뮬레이션으로 도달 불가능하다.** 물리시험 증거(`PHYSICAL_TEST`)만 그 관문을 연다. 시뮬 1000회도, 통과한 테스트 스위트도, 해석적 유도도 열지 못한다. 이것이 Brain이 스스로를 과신하지 않게 하는 핵심 규칙이며 전용 테스트로 고정돼 있다.

**독립성은 에피소드가 아니라 run 단위로 센다.** 한 번의 최적화 run 안의 20개 이터레이션은 하나의 탐색을 20번 샘플링한 것이지 독립 관측 20개가 아니다. 이를 독립으로 세는 것이 바로 기억이 근거 없는 확신을 쌓는 경로라서, 단일 run은 아무리 길어도 최대 SIMULATED에 머문다. 미해결 반례가 있으면 레벨은 REPEATED로 상한이 걸린다.

confidence는 발명하지 않는다. `min(n/(n+k) × 1/(1+미해결반례), 레벨 상한)` 이며, 증거에 단조증가, 반례에 감소, [0,1] 유계.

## Surrogate의 정직한 범위 (Phase 6)
- **근사 대상은 Phase 2 보 이론 평가기이지 3D FEM이 아니다.** 아직 고정밀 모델이 없으므로 surrogate 오차는 보 이론 오차 **위에 더해진다**. 근사의 근사다.
- **현재 속도 이득은 없다.** 20,000 후보 실측: surrogate 약 850만/s vs 배치 솔버 약 2,200만/s → **0.38배, 즉 surrogate가 약 2.6배 느리다.** 보 커널이 닫힌 형태 산술이라 MLP 순전파보다 싸기 때문이며 당연한 결과다. 이 인프라의 가치는 base가 비싼 3D FEM이 되는 **Phase 7에서 발현**된다. (프로파일 청크 경로(batch 4, 5,000회 launch)와 비교하면 약 5,000배 "speedup"이 나오는데 그건 launch 오버헤드지 물리가 아니다. 공정한 비교는 단일 배치 launch 기준이다.)
- **2단계 규율**: surrogate는 순위만 매기고 **결정은 솔버가 한다.** `screen_and_verify`가 반환하는 설계는 항상 솔버가 실제로 평가한 것이며(`verified=True`), 보고되는 질량·응력·처짐은 전부 솔버 값이다. 스크리닝이 좋은 후보를 잘못 버릴 수는 있다(recall 위험, 단축의 대가). 하지만 **검증 안 된 설계를 결과로 내놓지는 못한다.**
- 예측은 절대 맨몸으로 반환되지 않고 metric별 p95 held-out 오차를 동반한다. 안전율은 예측하지 않고 예측 응력에서 **유도**한다(모델이 낸 응력과 어긋날 수 없게).
- **정확도 측정의 함정 1건**: 데이터는 컨텍스트 단위로 묶여 생성되므로(컨텍스트 1개 = launch 1회, 설계 다수) 무작위 **행 분할**은 같은 문제의 설계를 train/test 양쪽에 넣는다. 그러면 "본 적 있는 문제 안에서의 보간"을 재는 셈이라 점수가 부풀려진다. 그래서 **컨텍스트 단위 분할**이 기본이며, held-out은 학습 때 본 적 없는 문제들로만 구성된다.
- **단조성은 보장되지 않는다.** 학습 모델은 물리 경향을 지킬 의무가 없어서, 벽 두께를 늘려도 예측 응력이 안 줄어드는 위반이 약 0.1% 표본에서 발생한다. 작지만 0은 아니며, 이것도 솔버가 최종 결정을 하는 이유다.

## 3D FEM의 정직한 범위 (Phase 7)
- **고정 경계는 응력 특이점이다.** 이상적인 완전고정 면의 재진입 모서리에서 선형탄성 해는 응력이 발산한다. 따라서 **peak 응력은 메시에 수렴하지 않는다.** 실측: 고체 캔틸레버를 4단계 정제하며 peak von Mises가 3.485 → 3.842 → 4.043 → 4.314 MPa로 계속 상승했고 멈추지 않았다. 그러므로 두 가지를 구분해 보고한다.
  - `peak_von_mises_pa`: 원시 최대값. **메시 의존, 수렴하지 않음.** 같은 방식으로 메시한 설계들 사이의 상대 비교에만 쓸 것. 부품을 인증하는 숫자가 아니다.
  - `gauge_von_mises_pa`: 지지부에서 단면 높이만큼 떨어진 게이지 스테이션의 값. 매끄러운 굽힘장 안이라 **수렴한다.**
- **처짐은 이 문제가 없고 깨끗하게 수렴**하므로 신뢰할 수 있는 강성 지표다.
- 응력 비교는 **같은 station·같은 fibre**에서 한다. 요소 중심 응력을 최외곽 fibre 값과 비교하면 20~30% "오차"처럼 보이지만 그건 샘플링 위치 차이일 뿐이다. fibre를 맞추면 FEM/보 이론 비 = 1.0015 → 1.0027로 수렴한다.
- 3D FEM도 **여전히 시뮬레이션**이다: 선형탄성, 소변형, 등방성, 이상화된 완전고정 경계, 필렛·체결부·접촉 없음. 실제 지지부는 완전 강체가 아니고 실제 부품에는 필렛이 있다. 진짜 peak 응력을 알려면 실제 조인트 형상이 필요하며 그건 이 모델 밖이다.
- Brain 기록도 **SIMULATED에 머문다.** 충실도가 높아졌다는 사실은 note에 남기지만 증거 종류는 여전히 시뮬레이션이며, **EXPERIMENTALLY_VALIDATED는 물리시험 없이는 여전히 도달 불가**다. 전용 테스트로 고정했다.

## Phase 7.5: 다충실도 루프를 닫다 (실패에서 배워 재설계)
Phase 7의 3D FEM 관문이 Phase 3 최적해를 탈락시켰다. 원인은 값싼 모델(Euler-Bernoulli)이 **전단 변형을 빼먹기** 때문이고, MVP 링크는 세장하지 않다(L/h 약 6). 최적화기는 값싼 모델의 맹점 위에 정확히 올라앉은 해를 찾아냈던 것이다. Phase 7.5는 그 모델을 고치고 다시 설계해 관문을 통과시킨다.

**1. 보 모델 업그레이드: Euler-Bernoulli → Timoshenko**
δ = PL³/(3EI) + PL/(G·A_s), G = E/(2(1+ν)), A_s = 웹 면적.
중공 박스는 수직 벽(웹)이 전단을 나른다고 보아 **A_s = 2·t·h**로 둔다. 이 계수는 **[ASSUMED]** 박벽 박스 근사이지 유도한 값이 아니며, **3D FEM으로 검증**했다. 기존 Euler-Bernoulli 경로는 `shear_deformation=False`로 남겨 두 모델의 차이(= 전단 기여)를 직접 볼 수 있게 했다. 미분가능성은 유지된다(autodiff가 전단 항을 통과하며, 벽 두께 민감도가 실제로 달라지는 것을 테스트로 확인).

**2. 3D FEM 대조 (계수 검증)**  단면 10×40×1 mm, L/h를 4에서 20까지:
| L/h | 3D FEM | Euler-Bernoulli | EB/FEM | Timoshenko | Timo/FEM |
|---|---|---|---|---|---|
| 4 | 0.23771 mm | 0.22302 | 0.9382 | 0.23758 | **0.9995** |
| 6 | 0.77202 | 0.75271 | 0.9750 | 0.77454 | 1.0033 |
| 8 | 1.80565 | 1.78419 | 0.9881 | 1.81331 | 1.0042 |
| 12 | 6.03701 | 6.02165 | 0.9975 | 6.06532 | 1.0047 |
| 20 | 27.81803 | 27.87802 | 1.0022 | 27.95081 | 1.0048 |

평균 절대오차: Euler-Bernoulli **2.067%** → Timoshenko **0.350%**, 약 **5.9배 개선**. 전단이 가장 중요한 L/h=4에서 0.9382 → 0.9995로 거의 정확해진다.
참고로 잔차에 최소자승으로 전단계수를 맞추면 음수가 나오는데, 이는 큰 L/h에서 두 1D 모델이 **모두** FEM을 약간 넘어서기 때문이다(FEM 메시 이산화와 고정 경계의 영향). 즉 잔차가 순수한 전단 성분이 아니므로 그 피팅은 의미가 없고, A_s = 2th(계수 1.0)를 유지한다.

**3. 재최적화와 고정밀 재검증**
| | 질량 | 모델 처짐 | 3D FEM 처짐 | 판정 |
|---|---|---|---|---|
| Phase 3 최적해 (EB) | 0.249977 kg | 1.02248 mm | 1.01898 mm | **FAIL** |
| Phase 7.5 최적해 (Timoshenko) | **0.251828 kg** | 1.00000 mm | **0.99749 mm** | **PASS** |

새 최적해는 b/h/t = 10.000 / 81.618 / 1.000 mm로 높이가 0.66 mm 자랐고 질량은 **+0.74%** 늘었다. 강성을 공짜로 얻고 있던 만큼을 실제로 지불한 셈이다. SLSQP와 DE가 다시 일치했고 활성제약은 여전히 처짐이다.
**여유는 +2.51 μm(0.25%)로 얇다.** 통과하는 이유는 보정된 모델이 FEM보다 처짐을 약간 **크게** 예측하기 때문이다(FEM/모델 = 0.9975). 즉 안전한 방향으로 보수적이라 한계에 붙여 최적화해도 관문을 통과한다. 반대 방향이었다면 마진을 명시적으로 넣어야 했을 것이다.

**4. 정직성**  Timoshenko도 여전히 1D 보 근사다. 3D FEM이 관문으로 남고, 그 FEM조차 시뮬레이션이다. Brain에 기록되는 이 교훈의 증거수준도 **SIMULATED**에 머문다. 시뮬레이션 두 개를 대조한 것은 아무리 한쪽이 충실해도 물리시험이 아니다.

## Phase 8: 재료 전면 확장과 이방성 (구현 완료)
재료 15종(기존 2종 + 신규 13종)과 직교이방성 탄성을 지원한다.

**등방성을 특수경우로 통일**: 등방 재료도 Ei=E, Gij=E/(2(1+ν)), νij=ν로 9개 직교이방 상수로 전개한 뒤 **같은 compliance 역행렬 경로**를 탄다. 등방에만 별도 지름길을 두면 두 경로가 조용히 어긋날 수 있고, 그러면 Phase 7의 등방 결과가 일반 코드의 회귀 검사 역할을 못 하게 된다. 실측 일치: 일반 경로와 닫힌 형태 등방 행렬의 최대 차이 **3.05e-05 Pa**(1e11 스케일 대비 상대 3e-16), 전체 FEM 해까지 상대 1e-9 이내 동일.

**Constitutive 검증**: 모든 재료의 6×6 강성 C에 대해 **대칭성과 양정치**를 단언한다. 양정치가 아닌 C는 변형만으로 에너지를 방출하는 물리적으로 불가능한 재료를 뜻하므로 **거부**한다. 상반성 νij/Ei = νji/Ej는 저장하지 않고 항상 유도해서 compliance가 데이터 파일의 오타로 비대칭이 될 수 없게 했다.

**직교이방 FEM 검증**: 상수변형 패치 테스트를 CFRP 강성으로 통과(변위·응력 모두 1e-12 이내). 축정렬 CFRP 봉의 신장을 해석값 E1·A/L과 대조하니 비 **1.057**인데, 이는 완전고정 근부가 근처의 횡수축을 막아 봉이 약간 뻣뻣해지기 때문이며 자유수축 공식보다 큰 강성이 나오는 것이 정상이다. 등방 재료로 같은 시험을 하면 방향 선호가 전혀 없음(상대 1e-6 이내)을 대조군으로 확인했다.

**재료 DB (전부 reference_typical, 출처 명시, 생산 전 데이터시트 대조 필요)**
- 등방 금속 6종 추가: al_2024_t3, ti_6al_4v, steel_s45c, steel_scm440, ss_316, mg_az31b.
- 고분자 5종: abs, pla, pa12, petg, pc. **온도·변형률·공정 의존이 크다**고 각 항목 notes에 명시했다. 특히 프린팅 부품은 등방이 아니며, 여기 값은 벌크 물성이라 프린팅 부품의 **상한**이다.
- 세라믹 alumina_al2o3: **연성 항복점이 없다.** 스키마 유지를 위해 yield 필드에 굴곡강도(~350 MPa)를 넣되, **항복 기준 안전율은 이 재료에 틀린 파괴 기준**임을 notes에 적었다. 세라믹 강도는 산포 지배·크기 의존이라 Weibull 처리가 필요하고 이 DB는 그걸 제공하지 않는다.
- 직교이방 복합재 cfrp_ud: E1/E2 = **13.5배**, 섬유방향/횡방향 강도비 **30배**. 단일 항복값이 무의미하므로 방향별 강도(long/trans/shear)를 필수 필드로 두고, 없으면 스키마가 거부한다.

**G는 유도값이다**: 등방 재료의 전단탄성계수는 추정이 아니라 정의(G = E/2(1+ν))다. `derived_fields`에 기록하고, **저장된 값이 실제로 그 유도와 일치하는지 검사**한다. 처음에 유효숫자 6자리로 써서 12종이 이 검사에 걸렸고, 값을 완전 정밀도로 다시 써서 해결했다. 검사가 없었다면 "유도했다"고 적힌 채 어긋난 값이 남았을 것이다.

**물성 추론층 (규칙기반, ML 아님)**
- **유도(DERIVED)**: 정확한 항등식만. G, 체적탄성계수, 상반 포아송비. 불확실성 없음.
- **추정(ESTIMATED)**: 상관식·경험칙. **반드시 불확실성과 근거를 동반**하고, 적용하면 재료 status가 **ASSUMED로 강등**되며 notes에 "NOT a datasheet value"가 붙는다. 예: 강의 Se ≈ 0.5·Su(±25%), 알루미늄·마그네슘은 **진짜 내구한도가 없어서** 같은 형태의 규칙이라도 ±40%로 신뢰도가 낮다.
- ML 예측기는 일부러 만들지 않았다. 학습 데이터가 없고, 규칙보다 감사하기 어렵다. 나중에 넣더라도 같은 Estimate 형태(불확실성 포함)를 내야 한다.

**보 모델**: 축방향 모듈러스로 E1을 쓴다. 등방은 값이 같아 불변. 직교이방은 **재료 1축이 보 축과 정렬됐다는 가정**이며, off-axis 하중은 인장과 전단을 커플링시켜 1D 보 모델이 표현할 수 없다는 한계를 명시했다.

**여전히 SIMULATED**: 재료가 늘고 이방성을 다뤄도 증거수준 규율은 그대로다. 물리시험 없이는 EXPERIMENTALLY_VALIDATED에 도달하지 못한다.

## Phase 9: CAD 출력, STEP (구현 완료)
파라메트릭 설계를 B-rep으로 만들고 STEP으로 내보낸다. 커널은 **build123d 0.11.1**(OCCT 래핑), 대안으로 cadquery를 시도한다.

**선택 의존성**: CAD는 `requirements-cad.txt`로 분리했다. 해석만 쓰는 사용자가 수백 MB짜리 OpenCascade를 받을 이유가 없다. 커널이 없으면 CAD 테스트는 **skip**되고 `dae export`는 트레이스백 대신 설치 안내를 낸다. 커널을 차단한 환경에서 실제로 확인함: CAD 테스트 10개 skip, 6개 통과, 나머지 스위트는 영향 없음.

**정합 관문 (핵심)**: 내보낸 STEP이 **해석한 그 부품인지** 검사한 뒤에만 파일을 쓴다.
1. B-rep 부피 vs 해석 단면적×길이
2. 그 부피×밀도 vs 물리가 사용한 질량
어긋나면 예외를 던지고 **파일을 남기지 않는다**(테스트로 확인). 어긋난 채 내보내면 아무도 시뮬레이션하지 않은 부품을 제조에 넘기는 셈이기 때문이다.

Phase 7.5 최적해 실측:
- 부피 8.961850000e-05 m³, 해석값과 **상대 6.0e-16** 일치
- 질량 0.251827985 kg, 해석 질량과 **상대 2.4e-07** 일치
- 단일 솔리드, bbox 500.0000 × 81.6185 × 10.0000 mm로 설계 치수와 일치
- STEP 왕복: 다시 읽어 부피 1e-9 이내 동일, ISO-10303 헤더 확인

**질량 잔차 2.4e-07의 출처를 분명히 해둔다**: CAD 부피는 float64이고 박스에 대해 정확하다. 이 잔차는 **fp32 물리 커널의 정밀도**에서 온다. 따라서 허용오차를 fp32보다 조이면 엉뚱한 것을 재게 된다.

**"항상 STEP"의 경계를 정직하게 그었다**
- **파라메트릭 솔리드: 보장.** 중공 박스는 평면 몇 개라 B-rep이 정확하고 STEP이 깨끗하다.
- **유기·토폴로지 형상: 보장 안 됨.** 밀도장이나 음함수 표면으로 나오지 면과 모서리로 나오지 않는다. 깨끗한 B-rep을 얻으려면 표면 재구성과 재피팅이 필요하고, 대충 하면 downstream CAD가 받아주지 않는 NURBS 조각모음이거나 솔리드인 척하는 수천 개 패싯이 된다. 그래서 `mesh_from_density_field`는 **의도적으로 NotImplementedError**다. 제조 가능해 보이지만 아닌 지오메트리를 뱉는 것보다 낫다.
- `export_stl`은 기존 B-rep을 테셀레이션한다. 시각화·프린팅용이며 허용오차가 명시된 근사이지 치수가 중요한 곳에서 STEP의 대체가 아니다. 실측: watertight, 부피 상대 2.7e-09.

**이것은 해석 형상이지 제조 준비 형상이 아니다**: 필렛도, 체결 피처도, 공차도 없다. 특히 날카로운 근부 모서리는 **Phase 7이 응력집중을 찾아낸 바로 그 위치**이며 실제 부품이라면 거기에 필렛이 필요하다. DfM 피처는 후속 단계이고, 다부품 조립 STEP은 Phase 10이다. CLI 출력에도 이 경고를 상시 표시한다.

## Phase 10: 어셈블리와 기구학 (구현 완료)
단일 부품에서 다물체 조립으로 확장했다. 링크·조인트·구속을 정의하고, 기구학으로 자세를, 정역학으로 링크별 하중을 구해 기존 구조해석에 연결한다.

**좌표계를 먼저 못박았다**: 오른손 좌표계, SI. 월드는 x 전방, **y 상방**, z 지면 밖. 중력은 **-y**. 이건 `configs/default.yaml`과 보 모델(단면 높이 h와 팁 하중이 모두 y축)에 맞춘 것이다. 로보틱스 관행대로 z-up을 골랐다면 **기존 구조 결과 전부에 대해 하중 방향이 조용히 뒤집혔을 것**이다. `joint_torques`는 **자세를 유지하는 데 필요한 구동 토크**를 반환한다(수평 팔이 아래로 당기는 페이로드를 들려면 양의 토크).

**검증 (전부 독립 대조)**
- **FK vs 2링크 닫힌형식**: 6개 자세에서 **1e-12 이내** 일치. 구현은 축·원점 기반 일반식이라 평면 팔을 전혀 모른다. 따라서 닫힌형식은 재진술이 아니라 외부 검증이다.
- **Jacobian vs 중앙차분**: 4개 자세에서 최대 오차 **1e-6 미만**(실측 7.3e-11). 각속도 행이 조인트 축과 일치하는지도 확인.
- **τ = J^T F 일관성**: 별도 테스트로 고정.
- **IK 왕복**: 4개 목표에서 수렴, 해를 FK에 넣으면 목표 복원(1e-6). 도달 불가 목표는 **수렴 실패로 보고**(자신 있는 오답을 내지 않음). 조인트 한계 준수, **특이점(완전 신전) 시작에서도 발산하지 않음**(damped least squares. 유사역행렬이면 터진다).
- **정적 토크 손검증**: 자중 포함해 모멘트 합과 **1e-12 이내** 일치.
  τ1 = P(L1+L2) + m1·g·(L1/2) + m2·g·(L1+L2/2), τ2 = P·L2 + m2·g·(L2/2)
- **단일링크 브리지**: 1링크 고정 어셈블리가 이전 단계의 캔틸레버 근부 모멘트를 정확히 재현(프로젝트의 두 갈래가 어긋나지 않았음을 확인).

**🐛 손검증이 잡은 버그**: 기하 Jacobian 공식 `Jv = z × (p − p_joint)`는 **그 조인트가 실제로 움직이는 점**에만 유효하다. 모든 조인트에 무조건 적용하면 안쪽 링크의 질량중심이 바깥 조인트에 대한 민감도를 갖게 된다(팔꿈치가 위팔 무게중심을 움직일 수 없는데도). 결과 오차는 1% 미만이라 **토크 표에서 그럴듯해 보였고**, 손으로 계산한 모멘트 합이 잡아냈다. `supporting_joints`로 열을 제한하고 테스트로 고정했다.

**하중 전달**: 각 링크는 자기 **근부 굽힘모멘트**를 재현하는 `equivalent_tip_load_n`을 받는 캔틸레버로 검사한다. 근부 모멘트가 굽힘응력을 지배하고 구조가 임계가 되는 지점이므로 사이징에는 이 등가가 맞다. 다만 자중의 분포 형상은 재현하지 않으므로, **자중이 지배적인 링크는 분포하중 모델이 필요**하다는 한계를 명시했다.

**어셈블리 STEP**: FK로 배치한 다부품을 compound로 출력. Phase 9 정합 관문을 **부품마다 재사용**하고, 어셈블리 질량이 링크 질량 합과 일치하는지 추가 검사(실측 상대 **1.8e-16**). 왕복 시 솔리드 2개와 총 부피 보존 확인.

**캡스톤 데모** (`dae assemble --payload 2 --step`): 2링크 평면 팔, 최악 중력 자세 자동 탐색 → 관절토크(shoulder −11.5804, elbow −5.0549 N·m) → 링크별 구조검증(link1 σ 5.210 MPa / SF 96.55 PASS, link2 σ 3.720 MPa / SF 135.20 PASS) → 어셈블리 STEP 출력. 페이로드를 400 kg으로 올리면 구조검사가 **실패**하는 것도 테스트로 확인했다(실패할 수 없는 검사는 검사가 아니므로).

**정직성: 정역학까지만이다.** 관성·코리올리·가속 토크가 없고, 마찰·백래시·관절 컴플라이언스도 없다. 강체와 이상 관절 가정이다. 이 범위는 "이 자세를 중력과 페이로드에 맞서 유지하라"를 다루며 그것이 **링크**를 사이징한다. **모터와 감속기 사이징은 하지 못한다** 이것은 동역학 항이 필요하며 **Phase 11 이후**다. 폐루프 기구도 구속 해석이 필요해 후속이다. 여전히 SIMULATED.

## Phase 11: 강체 동역학 (구현 완료)
정역학에서 동역학으로. τ = M(q)q̈ + C(q,q̇)q̇ + G(q) + F_friction.

**링크 관성을 기하에서 유도**: 중공 프리즘 = 솔리드 박스 − 동심 공동이므로 관성텐서도 두 닫힌형식의 차. 동심이라 평행축 항이 필요 없다. **CAD 커널(실제 B-rep 적분)과 대조해 상대 7.6e-16 일치.** `is_valid_inertia`는 대칭·양정치에 더해 **주관성모멘트의 삼각부등식**까지 검사한다. 임의의 양정치 행렬이 곧 강체의 관성은 아니기 때문이다.

**M은 운동에너지에서 조립**: M(q) = Σ_k [m_k Jv_k^T Jv_k + Jw_k^T R_k I_k R_k^T Jw_k]. Phase 10의 야코비안을 그대로 재사용하므로 **조상 조인트 제한(Phase 10에서 잡은 버그의 수정)이 자동으로 상속**된다. C는 M의 Christoffel 기호에서 얻는다.

**검증 (전부 독립 대조)**
| 항목 | 대조 대상 | 최악 오차 |
|---|---|---|
| M(q) | 2링크 평면 팔 닫힌형식 | **3.5e-18** |
| C(q,q̇) | 동일 닫힌형식 | **3.4e-12** (∂M/∂q 중앙차분 한계) |
| G(q) | 동일 닫힌형식 | **5.6e-17** |
| 정역학 브리지 | q̇=q̈=0에서 Phase 10 holding torque | **0** (정확) |
| passivity | Ṁ − 2C 반대칭 | **0** |
구현은 야코비안·Christoffel 기반 일반식이라 평면 팔을 모른다. 따라서 2링크 닫힌형식은 외부 검증이다. G가 Phase 10 statics를 **재사용**하므로 정역학 극한은 근사가 아니라 구조적으로 정확하고, 두 단계가 어긋날 수 없다.
passivity는 **토크만 맞고 C가 틀린 구현을 걸러내는** 성질이라 따로 검사했다.

**하중 케이스와 peak ≠ continuous**
LC1 nominal / LC2 max payload / LC3 max acceleration / LC4 worst gravity / LC6 holding / LC7 combined worst. 모터는 짧게 낼 수 있는 **peak** 정격과 계속 낼 수 있는 **continuous** 정격이 따로 있고 보통 2~3배 차이가 난다. peak만 보고 고르면 과사양, continuous만 보고 고르면 가속할 때마다 과열이다. 둘 다 낸다.
continuous는 **duty 가중 RMS 토크**다(발열이 전류 제곱, 전류가 토크에 비례하므로). duty 0인 순간적 최악 조합은 peak를 지배하되 **열 정격은 올리지 않는다**(테스트로 확인).

실측(2링크 팔): shoulder peak 27.0429 / continuous 11.0800 N·m → **2.44배**, elbow 12.2135 / 4.8823 → **2.50배**.

**정직하게 읽어야 할 결과 하나**: "dynamic share"가 **음수**로 나오는 케이스가 있다(LC7에서 −2.7%). 오류가 아니라, 최악 중력 자세에서 지령 가속이 중력을 일부 **상쇄**해 총 토크가 유지 토크보다 작아지는 것이다. 가속이 항상 더해지지는 않는다.

**모델하지 않는 것**: 마찰·백래시·관절 컴플라이언스 **전부 0**. 항은 인터페이스가 나중에 바뀌지 않도록 자리만 두었다. 실제 값에는 관절별 breakaway 토크·점성계수·감속기 효율이 필요한데 이 프로젝트엔 없고, **지어내면 모터를 고르는 근거 토크에 조작된 숫자가 들어간다.** 관절 유연성·제어 동역학도 없다. 강체와 이상 관절이다.
**이 단계는 필요 토크와 파워를 낼 뿐 모터·감속기를 선정하지 않는다.** 그건 효율·열·관성 정합 데이터가 필요하며 Phase 12다. 여전히 SIMULATED.

## Phase 12: 모터·감속기 선정 (구현 완료)
Phase 11의 하중케이스 토크·파워를 받아 관절별 구동계를 고른다.

**카탈로그는 실제 부품이 아니다.** 전 항목이 `illustrative` 태그의 일반 아키타입이고 source는 "representative archetype, replace with vendor datasheet"다. **벤더 부품번호를 지어내지 않았다.** 가짜 카탈로그는 나중에 출처가 있는 것처럼 읽히며, 이는 재료 DB가 전부 `reference_typical`로 태그해 피하는 것과 같은 실패다. **선정 로직이 deliverable이고 DB가 아니다.**
아키타입의 내부 일관성은 테스트로 강제한다: 연속토크×정격속도가 각 등급의 공칭 출력을 재현하고, peak는 연속의 약 3배이며, planetary 효율은 ratio가 오를수록(단수가 늘수록) 떨어진다.

**관계식**
```
출력토크   = 모터토크 × ratio × 효율
모터속도   = 관절속도 × ratio
반사부하관성 = J_load / ratio²        (모터축 기준)
출력축 총관성 = (J_rotor + J_gearbox) × ratio² + J_load
```
**높은 ratio는 토크를 사고 속도를 판다.** 이 트레이드가 선택을 자명하지 않게 만들며, 토크는 여유롭게 만족하는 조합이 속도 검사에서 떨어지는 이유다(테스트로 확인: 관절 20 rad/s에 ratio 160이면 모터 3200 rad/s 필요).

**연속과 피크를 둘 다 본다**
peak를 만족해도 연속 부하를 유지하면 과열하고, 연속으로 고르면 첫 가속에서 멈춘다. **정확히 그 두 케이스를 구성해 각각 거부되는지 확인**했다. 하나만 봤다면 통과시켰을 조합들이다. 감속기는 모터와 무관한 자체 정격이 있어서 **큰 모터가 작은 감속기를 구제하지 못한다**는 것도 테스트로 고정했다.

**불가능하면 불가능하다고 한다**: 충족 조합이 없으면 선정 결과로 None을 반환하고, 어떤 검사가 몇 개 조합에서 몇 배 부족했는지 보고한다. **가장 덜 나쁜 것을 선정인 척 돌려주지 않는다.**

**대안 비교**: 관절마다 상위 3개를 질량·백래시·효율·반사관성 트레이드와 함께 근거를 붙여 제시한다. 순위 기준은 총질량(가벼운 순)인데, 직렬 팔에서는 한 관절의 1 kg을 그보다 안쪽 모든 관절이 짊어지므로 질량이 누적되는 비용이기 때문이다.

**2링크 팔 실측 선정**
| 관절 | 필요(연속/피크) | 선정 | ratio | 질량 | 제한 검사 | 여유 |
|---|---|---|---|---|---|---|
| shoulder | 11.080 / 27.043 N·m | bldc_100w + planetary_50 | 50 | 1.10 kg | 연속토크 | 1.23x |
| elbow | 4.882 / 12.214 N·m | bldc_50w + planetary_50 | 50 | 0.90 kg | 연속토크 | 1.39x |
반사관성비(부하/로터)는 shoulder 0.38, elbow 0.07로 정합 양호. 대안으로 harmonic 조합(백래시 1' vs 15', +0.10 kg)이 제시되며 정밀도가 필요하면 그쪽을 고를 근거가 표에 남는다. 백래시 2' 요구를 걸면 planetary 계열이 전부 배제되고 harmonic만 남는 것도 테스트로 확인.

**하지 않는 것**: 열 검사는 **연속토크 프록시**다(실제로는 duty 프로파일·주위온도·열저항 필요). 그래서 상태를 "PASS subject to thermal validation"으로 표기한다. 효율·백래시는 속도·부하·온도에 대한 곡선이 아니라 대표값 하나다. 마찰·관절 컴플라이언스는 Phase 11에서 이어받아 여전히 0. 비용·납기·장착 인터페이스·제어기 정합도 없다.
**1차 스크리닝이지 최종 부품 결정이 아니다.** 여전히 SIMULATED.

## Phase 13: 토폴로지 최적화 (구현 완료)
단면 파라미터가 아니라 **밀도장** 위에서 자유 형상을 최적화한다. Phase 7 구조격자와 matrix-free GPU FEM을 그대로 재사용하고, 커널에 요소별 강성 배율을 추가했다(강성이 E에 선형이라 공유 Ke 하나로 충분해 matrix-free 구조가 유지된다).

**정식화**: E_e = E_min + x_e³(E0 − E_min), 컴플라이언스 최소화, 체적분율 제약, OC 업데이트(체적 라그랑주 승수 이분탐색 + move limit), 민감도 필터.
E_min은 장식이 아니다. 진짜 0이면 K가 특이해져 해가 **실패**하지 물렁한 영역이 되지 않는다. p=3은 회색을 손해로 만든다(밀도 절반이 강성은 8분의 1).

**🔑 민감도 검증(관문)**: 자기수반 공식 dc/dx_e = −p·x^(p−1)·(E0−E_min)/E0·u_e^T Ke0 u_e 를 **실제 목적함수의 유한차분**과 요소별로 대조. **최악 상대오차 2.6e-05.** 옵티마이저가 하는 모든 일이 이 미분에서 나오므로, 틀리면 아무것도 최적화하지 않은 그럴듯한 형상이 나온다.
부수 검증으로 컴플라이언스를 두 경로(F·U vs Σ scale·u^T Ke0 u)로 계산해 **1.9e-14** 일치.

**솔리드 회귀 브리지**: x=1 전체에서 스케일 경로가 Phase 7 순수 FEM과 **1.4e-14** 일치. 밀도 경로가 검증된 경로에서 이탈하지 않았음.

**체적 제약**: 매 이터레이션 정확히 목표(0.400000) 유지.

**필터 효과를 측정했다**: 면 이웃 간 평균 밀도차를 체커보드 지표로 정의해, 필터 없음 **0.365** → 필터 있음 **0.144**. 주장이 아니라 수치.

**하중경로 재발견**: 단면 높이 방향 밀도가 하단 0.64·0.67, 중앙 0.24~0.26, 상단 0.41~0.53으로, **굽힘이 요구하는 대로 중립축에서 먼 곳에 재료가 몰린다.** 길이 방향으로는 모멘트가 큰 근부에 집중. 아무도 알려주지 않은 물리를 옵티마이저가 스스로 찾아낸 것이다.

**⚠️ 수렴 판정의 함정을 발견해 고쳤다**: 교과서 기준인 "최대 밀도 변화 < tol"만 쓰면 오도된다. damping을 키우면 매 스텝이 작아져 기준이 일찍 충족되고, **명백히 나쁜 설계에서 수렴했다고 보고한다.** 실측: damping 0.3은 14 이터레이션에 컴플라이언스 1.78e-2에서 멈췄고, 더 부드러운 스텝은 1.01e-2까지 갔다. **설계가 수렴한 게 아니라 스텝 크기가 수렴한 것.** 그래서 밀도 변화와 **목적함수 정체**(최근 5회 상대변화 < 5e-4)를 **둘 다** 요구하도록 바꿨다. 임계값은 특정 실행이 성공으로 보이게 맞춘 게 아니며, 그보다 빠르게 개선 중인 실행은 정직하게 converged=False로 보고한다.

최종 실행: 131 이터레이션 수렴, 컴플라이언스 **6.05e-2 → 1.02e-2 (0.169배)**, 체적 0.400000.

**출력의 한계를 명시한다**
- **SIMP는 회색을 남긴다.** 40% 존재하는 재료는 없으므로 임계처리가 필요하고, 임계처리한 형상은 최적화한 밀도장과 다르다. 그래서 grey fraction(0.1~0.9 구간 비율)을 함께 보고한다.
- **STL이지 깨끗한 STEP이 아니다.** Phase 9 경계와 일관되게, 밀도장에서 해석적 면을 복원하는 것은 표면 재구성이다. 출력은 복셀 표면이고 구조적으로 각졌으며, 부피는 복셀 수에서 정확히 나온다.
- **watertight가 아닌 경우가 많고 그건 버그가 아니다.** 모서리로만 맞닿은 복셀은 비다양체 모서리를 남긴다. 그런 식으로만 붙은 재료는 하중을 못 나르므로 **면 연결 성분 필터로 제거**하지만, 연결된 본체 내부의 대각 접촉은 여전히 남는다. 그 경우 부피는 복셀 수 기준으로 정확히 보고한다.
- **컴플라이언스 최소화이지 응력 제약이 아니다.** 응력에 대해 아무 말도 하지 않는다. 응력 기반 토폴로지는 훨씬 어렵고 후속이다.
토폴로지 결과는 **설계 개념이지 검증된 부품이 아니다.** 3D FEM 관문을 통과하고 제조 피처를 얻어야 한다. 여전히 SIMULATED.

## Phase 14a: 응력제약 토폴로지 최적화 (구현 완료)
Phase 13은 응력에 대해 아무 말도 하지 않았다. 여기서 그 구멍을 메운다. 벤치마크는 **L-브래킷**이다. 정사각형에서 우상단 사분면을 도려내 **재진입 모서리**를 만들면 선형탄성이 그 자리에 응력집중을 놓는다. 컴플라이언스 목적함수가 신경 쓸 이유가 없는 특징이고, Phase 7이 외팔보 고정단에서 찾아낸 것과 같은 물리다.

**정식화**: qp 완화(강성 지수 p=3, 응력 지수 q=2.5)로 요소 응력을 σ_e = x_e^q · σ_solid(u)로 정의하고, P-norm으로 집계해 제약으로 건다. 민감도는 수반법으로 구한다. 명시항(x^q 인자를 통한 경로)과 암시항(변위를 통한 경로, 수반해가 필요)을 나눠 계산한다.

**🔑 수반 민감도 검증(관문)**: d(σ_PN)/dx를 유한차분과 요소별로 대조. **최악 오차 3.9e-08**(최대 민감도로 스케일). 옵티마이저가 하는 모든 일이 이 미분에서 나온다.
처음에는 상대오차가 최대 60%로 나와 틀린 줄 알았다. 원인은 기울기가 아니라 **차분이 솔버 잡음에 잠긴 것**이었다. CG 잔차 1e-8에서는 0 근처 민감도 성분의 차분몫이 수렴 정밀도에 지배된다. 1e-12로 조이자 3.9e-08이 나왔다. 그래서 판정 기준을 각 성분의 상대오차가 아니라 **최대 민감도로 스케일한 오차**로 잡는다. 0에 가까운 성분을 자기 크기로 재면 옳은 기울기가 탈락한다.

**⚠️ P-norm의 방향을 실측했고, 예상과 반대였다**: 여기서 쓰는 **합 형태** P-norm은 참 최댓값을 **과소평가하지 않고 과대평가**한다. 모든 요소에 대한 합이므로 최대항보다 클 수밖에 없다. L-브래킷 실측 비율은 P=2에서 **3.92**, P=32에서 **1.009**로 **위에서** 참값에 접근한다.
이 방향이 중요한 이유는 **안전한 쪽**이기 때문이다. 집계 제약을 만족하면 요소별 실제 한계도 만족한다. 더 익숙한 평균 형태 P-norm은 반대로 최댓값을 과소평가하고, 그건 과응력 부품을 내보내는 방향이다. 어느 쪽인지 테스트로 못박았다.

**완화의 효과를 대조로 확인했다**: 최고응력 요소 하나를 밀도 1.0에서 1e-3까지 비워가며 두 지표를 같이 측정했다.
- **완화 없는(미시) 응력은 사라지지 않는다.** 10.871 MPa에서 오히려 **올라가 25.655 MPa에 정착**한다. 강성이 떨어지는 만큼 변형률이 커지기 때문이다. 이 양에 제약을 걸면 **재료를 빼야 할 바로 그 자리에서 빼는 것이 금지된다.** 이것이 특이점이다.
- **완화된 응력은 0으로 간다.** 10.871 MPa에서 1e-6 MPa 이하로 단조 감소한다. 제거가 허용된다.

**L-브래킷 대비(같은 체적 0.4, 20x20x2, 한계 16 MPa)**
| 설계 | 컴플라이언스 | 최대응력 | p-norm | 모서리 밀도 | grey |
|---|---|---|---|---|---|
| 컴플라이언스 최소화 | 5.17e-02 | 17.92 MPa (위반) | 1.354 | 0.799 | 0.590 |
| 응력제약 | 7.51e-02 | 12.62 MPa | 0.997 | 0.711 | 0.797 |

응력제약은 최고응력을 내리고 재진입 모서리에서 재료를 걷어내며, 그 대가를 **컴플라이언스 +46%**로 치른다. 세 가지가 동시에 성립해야 제약이 제 일을 한 것이다.

**❌ 기대했던 "모서리가 필렛처럼 둥글어진다"는 결과는 얻지 못했다.** 정직하게 적는다. 응력제약 설계는 최고응력을 30% 낮추지만 형상은 깨끗한 필렛이 아니라 **더 회색**이다(grey 0.590에서 0.797로). 밀도장 렌더링에서 컴플라이언스 설계는 뚜렷한 구멍을 갖는 반면 응력제약 설계는 하부 팔에 공동이 거의 없다. 여기서 제조 가능한 형상을 읽어내려면 투영(projection) 기법이 필요하다. **Phase 14.5-A에서 구현했고, 이 회색 결과가 부분적으로 완화항을 이용한 수치적 탈출구였음이 드러났다**(흑백으로 강제하면 모서리 거동이 반대로 뒤집힌다). 아래 Phase 14.5-A 참조.

**❌ 체적최소화 정식화는 시도했다가 버렸다.** "체적 최소화 + 응력제약 벌점"은 여기서 **퇴화한다**. 체적 기울기가 상수 1/n이라 정규화한 스텝이 모든 요소를 같은 양만큼 움직이고, 밀도장은 형상을 만들지 못한 채 균일하게 줄어든다. 50 이터레이션 실행이 **모든 요소가 정확히 0.0500인 장**을 돌려줬다. 그건 구조가 아니다. 되살리려면 MMA 같은 진짜 제약 스텝이 필요하다. 그래서 채택한 정식화는 **고정 체적에서 컴플라이언스를 최소화하며 응력을 제약**하는 쪽이다. 컴플라이언스 목적이 흑백을 만들어 주고, 응력항은 제약이 실제로 무는 곳(재진입 모서리)에서만 설계를 바꾼다.

**벌점 방식의 한계를 보고한다**
- **벌점법은 제약 경계에서 진동하므로 마지막 반복이 살짝 밖에 있을 수 있다.** 17 MPa 실행은 p-norm 1.0036으로 끝났다. 그래서 `best_feasible_density`(허용역 안에서 컴플라이언스가 가장 낮은 반복)를 따로 들고 있고, 지어야 할 설계는 그쪽이다. `feasible`은 마지막 반복이 어디서 멈췄는지만 말한다.
- **벌점 스케줄은 설계 품질과 수렴 속도를 맞바꾼다.** 센 스케줄은 빨리 허용역에 들어가지만 사실상 응력 기울기만 보고 움직여 회색 설계를 남긴다(16 MPa 한계에서 가중치 10·성장률 1.3은 컴플라이언스 +212%에 grey 0.996, 기본값인 부드러운 스케줄은 +46%에 grey 0.797). 기본값은 품질을 택했고, 빠른 허용점만 필요한 호출자는 센 스케줄을 명시적으로 넘긴다.
- **한계가 너무 빡세면 도달하지 못한다.** 20x20x2에서 15 MPa는 컴플라이언스를 +227%로 밀어올리고 설계를 사실상 전부 회색으로 만든다. 10x10x1에서는 20% 감소가 아예 도달 불가이고, 벌점을 아무리 키워도 p-norm이 1.10 근처에서 정체한다. 그건 제약이 아니라 격자 해상도를 시험하는 것이다.

CLI는 `dae topology --stress`로 이 대비를 그대로 재현한다. 결과는 여전히 **SIMULATED**이고 설계 개념이지 검증된 부품이 아니다.

## Phase 14b: Method Registry와 전략 선택 (구현 완료)
전략 선택을 세 이름에 대한 하드코딩 switch로 짓지 않았다. **방법을 데이터로 등록하고 그 메타데이터 위에서 라우팅한다.** 이유는 단순하다. reasoner의 지능은 방법 라이브러리의 넓이와 정확성에 상한이 걸린다. 방법을 추가하는 일이 selector를 고치는 일이 되면 라이브러리는 넓어지지 않는다.

**등록 항목이 선언하는 것**: name, category(design_generation / analysis / optimization / selection), inputs/outputs, **유효범위(applicability)**, fidelity, cost, 그리고 **implementation**(실제 코드 경로).

**🔑 유효범위가 이 설계의 존재이유다.** Phase 7이 그 실증이다. 값싼 Euler-Bernoulli 모델을 세장비 약 6인 링크에 썼고, 전단을 빼먹은 그 모델의 맹점 위에 최적화기가 정확히 올라앉았으며, 3D FEM 관문이 그 결과를 탈락시켰다. **코드에 버그는 없었다.** 모델이 유효범위 밖에서 쓰였을 뿐이고, 시스템의 어느 부분도 그걸 지적할 위치에 있지 않았다.
이제는 registry가 자동으로 막는다. 세장비 6짜리 문제로 해석 방법을 조회하면 `beam_eb`는 후보에서 **빠지고**, 빠진 이유가 함께 나온다.

```
$ dae methods --geometry prismatic_beam --slenderness 6 --category analysis
applicable: fem3d, beam_timoshenko, surrogate_screen
excluded:
  beam_eb   slenderness L/h is at least 12
            (shear is omitted; measured error is 2.5% at L/h 6 and 6.2% at L/h 4)
```

**임계값은 교과서 통설이 아니라 이 프로젝트의 실측이다.** Phase 7.5의 3D FEM 대조표에서 EB/FEM은 L/h=4에서 0.9382, 6에서 0.9750, 12에서 0.9975다. 즉 L/h=6에서 2.5%, 4에서 6.2%를 잃는다. 그래서 EB의 문턱을 12로 둔다. Timoshenko는 측정 구간 4~20 전체에서 0.5% 안에 들므로 문턱이 4이고, **4 미만은 측정한 적이 없으므로 유효하다고 주장하지 않는다.**

**말하지 않은 것은 허락이 아니다.** 조건이 요구하는 문제 특성을 호출자가 명시하지 않으면 조건은 통과가 아니라 **실패**한다. 세장비를 아무도 적지 않은 문제야말로 보 이론을 유효하다고 가정하면 안 되는 경우다. 이때의 배제 사유는 "범위 밖"이 아니라 "명시되지 않음"으로 따로 보고된다. 고칠 방법이 다르기 때문이다(하나는 문제를 특성화해야 하고, 하나는 다른 방법을 써야 한다).

**등록된 것은 실제로 구현된 것뿐이다.** registry 항목은 "이 방법이 존재하고 쓸 수 있다"는 주장이고 selector는 찾은 것으로 라우팅한다. 스텁을 등록하는 것은 **가짜 부품번호와 같은 종류의 실패**다. 기록처럼 읽히는데 기록이 아니다. 이 트리에서 `physics/thermal.py`, `physics/collision.py`, `physics/rigid_body.py`, `optimization/bayesian.py`는 한 줄짜리 스텁이라 **의도적으로 빠져 있다.** MMA도 마찬가지다. 응력제약 문제에 가장 어울리는 최적화기지만 여기 구현되어 있지 않다. 테스트가 모든 등록 항목의 `implementation`을 임포트해 공개 정의가 있는지 구조적으로 확인한다.

**전략 선택 루프**: selector가 registry에서 design_generation 후보를 받아 **싼 것부터, 같으면 충실도 높은 순, 같으면 이름 순**으로 사다리를 만든다. 정체(기본 3회 비개선)하면 한 칸 올라간다. 마지막 칸에서 정체하면 순환하지 않고 **소진되었다고 보고한다**. 이름 타이브레이크는 장식이 아니다. 그게 없으면 사다리가 dict 순서에 의존해 실행이 재현되지 않는다.
selector는 `parametric_section`이나 `topology_stress`를 **언급하지 않는다.** 문제에 적용 가능한 design_generation 방법이 무엇인지 물을 뿐이다. 방법을 하나 등록하면 이 파일을 고치지 않고도 사다리에 들어온다.

**여전히 휴리스틱이다. LLM이 아니다.** 확장점은 명확하다. LLM reasoner를 꽂으면 정렬 규칙이 바뀔 뿐 **같은 registry를 라우팅**한다.

**⚠️ 라우팅한 것과 실행한 것을 구분해 기록한다.** Phase 4 루프 엔진은 파라메트릭 단면 설계를 평가한다. 토폴로지 방법은 아직 실행하지 못한다. selector가 `topology_compliance`로 올라갔을 때 에피소드의 `strategy_used`에 그 이름을 찍으면 **그 설계는 그 방법이 만든 게 아니므로 로그가 거짓 기록이 된다.** 그래서 실행 가능한 방법만 찍고, 권고는 hypothesis에 적고 `unmet_recommendations`로 센다. registry가 라우팅할 수 있는 범위와 루프가 실행할 수 있는 범위의 간극이 **침묵하는 한계가 아니라 보이는 숫자**가 된다.

**Brain 전략기억**: 어떤 문제류에서 어떤 registry 방법이 이겼는지 일반화한다. **런 단위로** 승자를 센다(에피소드 단위로 세면 단지 더 많이 시도된 전략이 더 나아 보인다). 일관된 승자가 임계치(0.7) 미만이면 **아무것도 승격하지 않는다.** 추천할 일관된 수가 없을 때 지어내는 것이 이 사다리가 막으려는 실패 그 자체다.
증거수준은 주장이 아니라 기존 사다리에서 **유도**된다. 단일 런이면 SIMULATED, 독립 런이 쌓이면 REPEATED까지 오른다. **EXPERIMENTALLY_VALIDATED에는 영원히 도달하지 못한다.** 그 관문은 물리시험 증거로만 열리고 이 경로는 그런 증거를 만들 수 없다. "방법 A가 B보다 낫다"는 진술은 모델에 대한 것이지 세계에 대한 것이 아니다.

## Phase 14.5-A: 흑백 토폴로지 (Heaviside 투영) (구현 완료)
Phase 13은 grey fraction 0.59, Phase 14a는 0.80을 남겼다. 밀도 0.4인 요소는 아무도 만들 수 없는 재료이고, 사후 임계처리로 얻은 형상은 옵티마이저가 평가한 적 없는 형상이다. **3-필드 정식화**로 옵티마이저 자신이 흑백 설계를 내게 한다.

    설계변수 x  --밀도필터-->  x_tilde  --투영-->  x_bar  -->  강성

투영은 tanh 형태의 매끄러운 Heaviside(임계 eta=0.5, 선명도 beta)다. **beta는 반드시 낮게 시작해 올려야 한다.** 처음부터 날카로우면 거의 모든 곳에서 미분이 0이라 옵티마이저가 움직이지 못한다. 그게 연속화의 존재이유다.

**🔑 민감도 연쇄 재검증(관문)**: Phase 13·14a에서 검증한 수반해는 이제 **x_bar에 대한** 미분이지 설계변수에 대한 미분이 아니다. 설계변수까지 되돌리는 연쇄 `H^T (dx_bar/dx_tilde * dc/dx_bar)`를 유한차분으로 다시 검증했다. **최악 스케일 오차: beta=1에서 2.8e-07, beta=4에서 1.2e-07, beta=16에서 5.0e-08.** 검증하지 않았다면 다른 문제의 기울기를 내려가면서도 그럴듯한 형상을 냈을 것이다.
필터는 성긴 행렬로 짓는다. 연쇄가 **정확한 전치**를 필요로 하는데, 전치를 두 번째 루프로 손으로 쓰면 순전파와 역전파가 어긋난다. 내적 항등식 `<Hx, y> == <x, H^T y>`를 테스트로 박았다.

**beta가 바뀌면 체적을 복원한다.** beta를 올리면 설계는 그대로인데 사상이 바뀌어 물리 체적이 움직인다. 그냥 두면 move limit 때문에 여러 이터레이션에 걸쳐 돌아오고, 그 사이의 모든 반복이 **잘못된 체적에서 잰 컴플라이언스**를 보고한다. 서로 비교할 수 없는 값이다. 그래서 beta가 바뀐 직후 설계를 균일 이동시켜 물리 체적을 목표로 되돌린다. 설계를 개선하는 스텝이 아니라 새 사상으로 다시 표현하는 것이므로 move limit 대상이 아니다. 시작 설계도 이분탐색으로 목표 체적 위에 올려놓는다.

**결과: 더 검고 동시에 더 뻣뻣하다** (L-bracket 512요소, 체적 0.4, 필터 반경 2요소, 200 이터레이션)
| 설계 | 컴플라이언스 | 최대응력 | grey | connected | 모서리밀도 |
|---|---|---|---|---|---|
| plain SIMP | 5.53e-02 | 17.12 MPa | 0.6172 | 0.989 | 0.7296 |
| 3-필드 | **3.33e-02** | 17.59 MPa | **0.0625** | 1.000 | 0.6509 |
| 3-필드 + 응력제약 | 5.41e-02 | **12.35 MPa** | 0.2969 | 0.982 | 0.8576 |

grey가 **0.617에서 0.0625로 약 10배** 줄었고, **컴플라이언스는 40% 좋아졌다.** 후자는 우연이 아니다. SIMP는 회색을 x^3로 벌하므로, 같은 체적을 흑백으로 쓰면 실제로 더 뻣뻣해진다. 캔틸레버에서도 같은 방향이다(grey 0.4115 -> 0.1094).

**⚠️ grey fraction만 보면 속는다. 설계가 조각날 수 있다.** 이걸 거의 놓칠 뻔했다. 필터 반경 1.5요소에서 투영 설계는 grey 0.109로 훌륭해 보이지만 **고형 재료의 76%만 최대 연결성분에 속한다.** 나머지는 하중경로에 붙어 있지 않아 아무것도 나르지 않는다. 그건 회색 설계보다 나은 설계가 아니라 **부서진 설계**다. 그래서 `connected_fraction`을 도입해 grey와 **함께** 보고한다.
반경을 넓히면 붙는다. 24x8x2 캔틸레버 200 이터레이션 실측:
| 필터 반경 | 컴플라이언스 | grey | connected |
|---|---|---|---|
| plain 1.5 | 1.084e-02 | 0.4115 | 100% |
| 3-필드 1.5 | 9.805e-03 | 0.1094 | **76.1%** |
| 3-필드 2.0 | 1.117e-02 | 0.0833 | 91.6% |
| 3-필드 3.0 | 1.771e-02 | 0.0573 | 100% |
연결성은 반경으로 사고 컴플라이언스로 지불한다. 양쪽 다 테스트로 고정했다.

**⚠️ checkerboard 지표는 흑백 설계에서 올라가고 그게 정상이다.** 이웃 간 밀도차를 재는 지표인데 날카로운 고체-공동 경계에서는 그 차이가 크다. 회색 장 위의 교대 아티팩트를 잡으라고 만든 것이라 진짜 모서리와 구별하지 못한다. **여기서 의미 있는 지표는 연결성이다.**

**❗ 응력제약의 모서리 거동이 Phase 14a와 반대로 뒤집혔고, 이쪽이 물리적인 답이다.**
Phase 14a(회색 허용)에서 응력제약은 모서리 밀도를 **낮췄다**(0.799 -> 0.711). 3-필드(흑백 강제)에서는 오히려 **높인다**(0.651 -> 0.858). 해석: 회색이 허용되면 옵티마이저가 밀도를 낮춰 완화항 x^q로 응력을 줄이는 **수치적 탈출구**를 쓸 수 있다. 흑백으로 강제하면 그 길이 막히므로 **재진입 모서리에 재료를 더 넣어** 물리적으로 해결해야 한다. 재진입 모서리를 두껍게 만드는 것은 필렛이 하는 일 그대로다. 최대응력이 17.59에서 **12.35 MPa로 30% 떨어졌다.**
**그래도 "필렛을 얻었다"고는 쓰지 않는다.** 20x20 격자는 필렛 반경을 표현하기에 거칠다. 정직한 진술은 이것이다. 흑백 응력제약 설계는 **모서리 영역에 재료를 추가하는 방향으로** 응력을 줄이며, 이는 필렛이 작동하는 방향과 같지만 기하학적 반경을 분해한 것은 아니다. 진짜 반경을 보려면 더 고운 메시가 필요하고, 그 크기대에서는 GPU가 이미 유리하다(14.5-C 참조).

**여전히 남는 한계**: 흑백이어도 유기적 형상의 깨끗한 STEP은 표면 재구성 문제다. 출력은 복셀 표면이고 결과는 **SIMULATED**이며 설계 개념이지 검증된 부품이 아니다.

## Phase 14.5-B: 루프가 토폴로지를 실제로 실행한다 (구현 완료)
14b가 남긴 "라우팅 != 실행" 간극을 닫는다. 이전에는 selector가 `topology_compliance`를 골라도 루프는 파라메트릭 솔버를 돌렸고, 권고는 `unmet_recommendations`에 쌓일 뿐이었다. 이제 루프의 설계 단계가 **선택된 method의 실제 구현으로 디스패치**한다.

**설계 결과에 출처를 강제로 붙였다**: `DesignOutcome`은 **정확히 하나**의 표현을 들고 있어야 한다. 파라메트릭이면 설계벡터, 토폴로지면 밀도장. 둘 다 없으면 아무것도 만들지 않은 것이고, 둘 다 있으면 출처가 모호하다. 이 불변식 덕분에 에피소드의 `strategy_used`가 **주장이 아니라 검사 가능한 기록**이 된다. 토폴로지라고 적힌 에피소드는 밀도장을 들고 있어야 하고, 토폴로지 최적화가 실제로 돌지 않으면 밀도장은 존재할 수 없다. 테스트가 이 대조를 확인한다.
에피소드의 `design_genome`도 방법에 맞춰 쓴다. 토폴로지 설계에 `wall_thickness_m`을 적지 않는다. 다른 설계에서 나온 벡터로 그 칸을 채우면 로그가 만들지도 않은 형상을 만들었다고 말하게 된다.

**질량 최소화는 체적분율 탐색으로 한다.** SIMP는 고정 체적에서 컴플라이언스를 최소화할 뿐 질량 목적함수가 없다. 그래서 토폴로지 실행기는 체적분율에서 SIMP를 돌리고, 결과 밀도장을 **문제의 진짜 제약(3D FEM 처짐)** 으로 평가하고, 제약을 만족하는 가장 가벼운 분율을 이분탐색한다. 토폴로지 이터레이션이 비싼 이유가 이것이고, registry가 이 방법들을 HEAVY로 매긴 근거다.

**실행 결과(MVP 링크, 6 에피소드)**
```
 #                        strategy_used           repr   mass kg  feas  best
 0   parametric_section:initial-exploit  design_vector    0.2518  True  True
 1  parametric_section:exploit-jittered  design_vector    0.2518  True False
 2  parametric_section:exploit-jittered  design_vector    0.2518  True False
 3 topology_compliance:explore-on-stall  density_field    2.6344  True False
 4     topology_stress:explore-on-stall  density_field    2.6344  True False
unmet recommendations: []
```
**실행 가능한 전략의 unmet이 0으로 떨어졌다.** 전략 선택이 권고에서 실제 실행으로 승격했다. 메커니즘 자체는 남겨 둔다. registry는 실행기보다 빨리 자라도록 만든 것이고, 다음에 추가되는 방법은 누군가 배선하기 전까지 다시 unmet이 된다.

**⚠️ 토폴로지가 파라메트릭에 크게 졌고, 그게 정직한 결과다.** 0.252 kg 대 2.634 kg, 10배 차이. 원인은 튜닝 부족이 아니다. 파라메트릭 설계는 **얇은 벽 중공 박스**이고 캔틸레버에 대해 대단히 질량효율적인데, 16x6x2 복셀 격자는 높이 방향으로 6요소뿐이라 **그 얇은 벽을 표현할 수 없다.** 루프는 파라메트릭을 best로 올바르게 유지한다.
`topology_compliance`와 `topology_stress`가 **같은 질량**(2.6344 kg)을 낸 것도 확인했다. 밀도장은 실제로 다르고(최대 차이 0.215) 두 최적화가 각각 돌았지만, 질량은 체적분율만으로 정해지고 두 이분탐색이 같은 분율(0.1875)에 도달했다. 이 문제에서 응력제약은 두 자릿수만큼 헐겁다(최대 응력 0.943 MPa 대 허용 120 MPa). 제약이 물지 않으면 벌점항이 0이라 응력 경로가 컴플라이언스 경로로 환원되는 것이 정상 동작이다.

## Phase 14.5-C: dof 기준 디바이스 선택 (구현 완료)
"연산이 느리다"의 원인을 실측했다. **CPU 폴백이 아니었다.** Warp는 `cuda:0`을 쓰고 있었다. 문제는 **우리 메시가 GPU 손익분기점보다 작다**는 것이었다.

워밍업 후, 두 디바이스에서 CG 반복수 동일:
| dofs | GPU ms | CPU ms | GPU/CPU |
|---|---|---|---|
| 1,071 | 43.5 | 23.2 | **1.88 (GPU 느림)** |
| 3,267 | 86.6 | 65.4 | **1.32 (GPU 느림)** |
| 9,996 | 142.9 | 246.9 | 0.58 |
| 24,375 | 396.5 | 1410.3 | 0.28 |
| 45,198 | 715.5 | 3435.7 | **0.21 (GPU 4.8배)** |

손익분기점은 약 10,000 dofs다. 이유는 둘이다. 작은 solve는 **커널 런치 오버헤드**가 지배하고(토폴로지 실행 중 SM 점유율 40%, 14W/35W), 이 소비자용 부품은 **fp64를 fp32의 약 1/64 속도**로 돈다. 솔버는 float64를 쓰는데, 이건 Phase 7에서 세장 보의 조건수 때문에 float32 CG가 정체해서 택한 것이라 낮출 수 없다.

그래서 `solve_linear_elasticity`가 dof 수로 디바이스를 고른다. 10,000 미만이면 CPU, 이상이면 cuda:0. **명시적으로 넘긴 device는 절대 덮어쓰지 않는다.**
**관문은 정확도 중립이다.** 속도를 위해 디바이스를 고르는 것은 답을 바꿀 수 없을 때만 허용된다. 같은 문제를 두 디바이스에서 풀어 변위와 응력이 **상대 1e-6 이내**로 일치함을 테스트로 고정했다. 같은 커널, 같은 float64, 같은 알고리즘, 같은 반복수이고 부동소수 축약 순서만 다르다.
실측 효과: L-bracket SIMP 60 이터레이션이 **14.6초에서 11.0초로**(이터레이션당 244 ms에서 183 ms), 예측한 1.32배와 일치.

**성능 로드맵(순서만 기록, 지금 구현하지 않음)**
1. (완료) dof 기준 디바이스 선택.
2. **독립 solve 배칭**: 체적분율 이분탐색의 solve들은 서로 독립이므로 한 번에 묶어 런치 오버헤드를 상각한다. 현재 병목이 산술이 아니라 런치이므로 여기가 다음 이득이다.
3. **혼합정밀도(fp32 CG + fp64 잔차보정)**: 메시가 커진 뒤에 한다. 지금 크기에서는 SM 점유율이 40%라 **산술 바운드가 아니어서 효과가 작다.** 고운 메시로 진짜 필렛을 볼 때는 GPU가 이미 4.8배 빠르고, 그 영역에서 제 값을 한다. 정확도는 깎지 않고 잔차보정으로 fp64를 유지하는 형태여야 한다.

## Phase 15: MCP 노드 아키텍처와 분산 capability registry (구현 완료)
지금까지 노드 간 통신은 Claude 세션이 수동으로 중계했다. 이걸 정식 프로토콜 경계로 바꾼다. **새 프로토콜을 발명하지 않고 MCP를 쓴다.** 퓨전이 이미 MCP로 붙는다는 사실과 정합하고, 표준을 하나 더 만드는 것은 그 자체가 부채다.

**14b registry를 대체하지 않고 확장한다.** capability = 등록된 method + 그것을 제공하는 노드. 라우터의 규칙은 하나다: **method가 문제에 적용 가능하고 AND 제공 노드가 사용 가능하면** 후보다. 이 프로세스 안이든, stdio로 붙은 자식 프로세스든, HTTP 너머 다른 기계든 판단에 들어오지 않는다.
이 통일성이 핵심이다. 경로가 둘로 갈리면 로컬을 정직하게 지켜 주는 유효범위 선언이 원격을 덮지 못한다. 세장비 6짜리 문제에서 `beam_eb`가 배제되는 것은 그것이 노드 capability가 된 뒤에도 그대로다.
배제 사유는 **둘 다** 보고한다. 범위 밖이면서 노드도 못 쓰는 capability는 이유를 두 개 단다. 노드를 살렸는데 여전히 무효인 후보가 조용히 살아나면 안 된다.

**사용 불가는 발견하는 게 아니라 선언한다.** 유료 권한이 없는 노드는 미리 그렇게 말하고, 라우터는 보 이론을 세장비로 배제하듯 **사유를 붙여** 배제한다. 호출해 보고 에러로 알게 되는 것은 그보다 나쁘고, **그럴듯한 숫자를 받아서 알게 되는 것은 최악이다.** 사용 불가 노드는 명세에서 빠지지 않고 사유와 함께 남는다. 안 만든 capability와 막힌 capability는 대응이 다르기 때문이다.

**노드 로스터**
- **Daedalus 엔진 노드** (in-process, MCP 서버로 노출 가능): `list_capabilities`, `route`, `evaluate_beam_section`.
- **추론 노드**: 계약만. 지금 추론은 CLI를 모는 운영자 세션이 담당하며, 이는 구조적으로는 노드지만 프로토콜상으로는 아니다. 세션을 감싸는 서버를 쓰는 것은 **반대편에 아무것도 없는 경계를 발명하는 일**이라 하지 않았다. 로컬 모델로 바꾸는 것이 라우팅 재설계가 아니라 이 capability 제공이 되도록 계약만 선언해 둔다.
- **퓨전 검증 노드**: 계약·워크플로우 완비, 구현 비움(아래).

**🔑 MCP 왕복 실증**: 엔진의 보 해석을 MCP 클라이언트에서 MCP 서버로 실제 호출하고, 결과가 in-process 호출과 **상대 1e-12로 일치**함을 테스트로 고정했다. 래퍼가 아니라 진짜 프로토콜 경계다. 전송이 결과를 조용히 바꾸면 전송이 없는 편이 낫다. `route`도 왕복시켜 **배제 사유까지 함께 건너오는지** 확인한다. 무엇을 쓸 수 있는지만 받은 호출자는 "없는 것"과 "배제된 것"을 구별할 수 없다.

**퓨전 검증 노드: 워크플로우만, 구현은 비움**
퓨전은 유료이고 권한을 아직 사지 않았다(두 달 뒤쯤 결제 예정). 호출 자체를 뺀 **전부**가 여기 있다. capability 선언, 요청·보고 계약, registry 등록 자리, 설계가 이 노드로 흘러가 결과가 되돌아오는 경로. 나중에 함수 본문 하나를 MCP 호출로 채우면 되고 주변은 움직이지 않는다.
**🔑 stub은 절대 숫자를 내지 않는다. 이것이 이 단계의 정직성 관문이다.** 0도, 빈 보고서도, 부분 결과도 아니다. 셋 다 파이프라인에서는 **실측과 구별되지 않는다.** `CapabilityUnavailable`을 던지고, **어떤 인자로도 결과가 나오지 않음**을 테스트로 고정했다. 반환 타입 주석은 계약이고, 노드가 사용 불가인 동안 의도적으로 한 번도 지켜지지 않는다.
`FusionVerificationReport`는 정의만 되어 있고 **이 모듈은 그것을 한 번도 생성하지 않는다.** 소비 코드를 고정된 계약에 대고 미리 쓰고 검토할 수 있게 하기 위한 것이다.

**검증 없는 설계는 그렇게 표시된다.** `VerificationStatus`가 불변식을 강제한다. 보고서 없이 외부검증을 주장할 수 없고, 외부검증이 아니라면서 보고서를 들고 있을 수도 없다(미검증 숫자가 검증된 것처럼 흘러다니게 된다). 검증이 없으면 **이유를 반드시 적어야 한다.** 지금 모든 설계는 `self_fem_only`, 즉 **우리 자체 FEM으로만 검증됨**이고 그 사실이 에피소드와 Brain에 사유와 함께 기록된다.

`mcp`는 선택적 의존성이다(`requirements-nodes.txt`). 해석 스택은 전부 in-process로 돌아 이것을 필요로 하지 않는다. CLI는 `dae nodes`로 로스터와 capability, 그리고 배제 사유를 보여준다.

**앞으로 채울 자리**: 권한 결제 후 `nodes/fusion_node.py`의 `verify` 본문에 실제 MCP 호출을 넣고, 노드를 `available=True`로 선언하면 라우터가 자동으로 후보에 넣는다. 14.5-A가 남긴 회색·복셀 표면 문제의 계획된 해법이 이 노드의 **클린 B-rep 지오메트리**다.

## Phase 16: 피로(S-N, Goodman)와 좌굴(오일러) (구현 완료)
method 라이브러리 확장의 첫 걸음. 이 둘을 고른 이유는 **PASS의 정직성을 직접 바꾸기 때문**이다. 지금까지 설계는 최대응력이 항복 아래이고 처짐이 한계 아래면 통과했다. 그건 **하중을 한 번 걸었을 때**에 대한 진술이다. 백만 번 걸었다 뺐다 하는 부품이나, 세장한 부재에 압축이 걸리는 부품은 그 두 검사가 볼 수 없는 파괴모드를 갖는다.

### 16-A: 고주기 피로
**🔑 Goodman 손검증**: Se=159, Su=572 MPa, σ_a=50, σ_m=70 MPa에서
`50/159 + 70/572 = 0.314465 + 0.122378 = 0.436843`, `n = 1/0.436843 = 2.28914`.
코드가 상대 1e-12로 일치한다. Soderberg(평균을 항복에 대고 계산)는 항상 더 보수적임도 확인했다.

**압축 평균응력은 크레딧을 주지 않는다.** Goodman·Soderberg 선은 인장 평균응력에 대해 유도된 것이고, 음의 σ_m으로 연장하면 **완전교번보다 높은 안전율**을 뱉는다. 그건 틀려도 되는 방향이 아니다. σ_m ≤ 0이면 평균항을 버리고 `Se/σ_a`로 간다. 어느 쪽이 일어났는지 `mean_stress_charged`로 기록한다.

**⚠️ 알루미늄·마그네슘에 무한수명을 주지 않는다.** 진짜 내구한도는 S-N 곡선이 평평해지는 재료(탄소강·저합금강)에만 있다. 알루미늄 곡선은 계속 내려가므로 DB의 `fatigue_strength_pa`는 **약 5e8 사이클에서의 응력**이지 무한수명 보장이 아니다(이건 `data/materials.yaml`이 이미 적어 둔 사실이고 여기서 코드로 강제한다).
내구한도 보유 목록은 **[ASSUMED]** 이고 `steel_s45c`, `steel_scm440`만 넣었다. **오스테나이트 스테인리스(ss_316)와 티타늄(ti_6al_4v)은 문헌이 갈려서 일부러 뺐다.** 목록에 없는 id는 내구한도 없음으로 처리한다. **모르는 재료가 조용히 무한수명을 주장하는 일이 없도록** 하는 보수적 기본값이다.

**압축-압축 사이클도 검사한다.** 테스트를 쓰다가 발견한 것이다. 0에서 -50 MPa를 오가는 하중은 인장 피크가 없지만 **교번응력이 25 MPa로 실재한다.** "인장이 없으면 피로 없음"으로 처리하면 이걸 건너뛰는데, 압축-압축 반복도 균열을 키운다. 진짜로 검사가 없는 경우는 **변동이 아예 없는** 경우뿐이다.

### 16-B: 좌굴
**🔑 오일러 손검증**: E=200 GPa, I=1e-8 m⁴, L=2 m, K=1에서
`P_cr = π²·200e9·1e-8/4 = 4934.8022 N`, 코드가 상대 1e-15로 일치.
유효길이계수는 **이론값**을 쓴다(고정-자유 2.0, 핀-핀 1.0, 고정-핀 0.699, 고정-고정 0.5). P_cr이 1/K²로 스케일하는 것도 확인했다(고정-자유는 핀-핀의 1/4). 설계기준은 실제 단부 구속이 이상적이지 않다는 이유로 더 높은 값을 권고하며(AISC는 고정-고정 0.65, 고정-핀 0.80), 그 값이 필요한 호출자는 `k_factor`를 직접 넘긴다.

**약축이 결정한다.** 기둥은 단면 2차모멘트가 작은 축으로 좌굴하므로 `min(I_x, I_y)`를 쓴다. 강축 값을 쓰면 임계하중을 두 값의 비만큼 과대평가한다.

**임계세장비로 오일러 유효성을 판정한다.** `C_c = sqrt(2π²E/Sy)`, 오일러 응력이 항복의 절반이 되는 지점이다. 7075-T6에서 **C_c = 53.0**. 세장비가 그 아래면 탄성 임계하중에 도달하기 전에 항복이 시작되므로 **오일러는 과대예측이고 `euler_valid=False`로 보고한다.**

**⚠️ 무효한 오일러 값이 지배 마진이 되지 못하게 막았다.** 이걸 통합하다가 잡았다. 짧은 기둥(L=0.2 m, λ=25.7 < 53.0)에서 오일러 SF가 46.4로 나오는데, 그게 후보에 끼면 **과대예측이 구속 마진 행세를 한다.** 유효성 플래그를 만든 이유가 바로 그건데 무의미해진다. 유효한 오일러 결과만 지배 후보가 되고, 무효한 값은 경고와 함께 결과에 남기만 한다.

**⚠️ 축방향 항복 검사가 빠져 있던 것도 같이 잡았다.** 횡하중이 없는 부재는 정적 SF가 무한대로 나오는데, 그 부재가 압축을 나르고 있었다. 압축응력을 항복에 대고 재는 검사를 따로 추가했다.

### 통합: 이제 설계가 피로나 좌굴로 탈락한다
`physics/failure_modes.py`가 duty가 가능하게 만드는 검사만 돌리고 **가장 작은 안전율**을 지배모드로 보고한다. **적용 불가한 검사는 큰 숫자가 아니라 `None`이다.** 적용되지 않은 검사가 큰 안전율을 보고하면 잘 설계된 부품처럼 읽히는데, 실제로는 아무것도 안 본 것이다.

**실증 1: 정적 통과, 피로 탈락.** 7075-T6, 40x40x2 mm, L=0.5 m, 완전교번 179.9 MPa.
정적 SF **2.796**(항복 503 MPa 대비 여유), 피로 SF **0.884** → **FAIL**. 항복만 봤으면 통과시켰을 설계다.

**실증 2: 정적 통과, 좌굴 탈락.** 같은 단면, L=1.5 m, 압축 7000 N, 고정-자유.
축방향 항복 SF **21.8**(항복 근처에도 못 감), 좌굴 SF **0.824** → **FAIL**. λ=193.1로 오일러 유효.

**실증 3: 세장비가 지배모드를 바꾼다.** 같은 단면·같은 하중에서 **길이만** 바꾼다.
L=1.5 m → **좌굴**이 지배(SF 0.824). L=0.2 m → **축방향 항복**이 지배(SF 21.8), 오일러는 무효.

### registry 등록과 정직성
`fatigue_sn`과 `buckling_euler`를 capability registry에 등록했다(applicability·fidelity·cost). 적용범위가 이 둘의 핵심이다. **정적 단일하중에는 피로가 없고, 인장 부재는 좌굴하지 않는다.** 반복하중이 없으면 `fatigue_sn`이, 압축이 없으면 `buckling_euler`가 사유와 함께 후보에서 빠진다.
**3D FEM 선형좌굴은 등록하지 않았다.** 이 형상이 못 보는 국부좌굴(박벽 웹·플랜지)을 잡아 주겠지만 **구현되어 있지 않다.** 가짜 카탈로그 금지 규율 그대로다.

**남는 한계를 적어 둔다**
- S-N은 **고주기 전용**이다. 소성변형이 지배하는 저주기 피로는 변형률-수명 모델이 필요하고 없다. **모듈은 자기가 범위 밖이라고 말해 주지 않으므로** 호출자가 알아야 한다.
- **노치·표면조도·치수·온도 계수가 전부 없다.** 실제 내구한도는 핸드북 값에 이것들(각각 1 미만)을 곱한 값이다. 따라서 여기 숫자는 실물 대비 **낙관적**이다.
- 피로 데이터는 `reference_typical`이고 **산포가 크다.**
- 오일러는 **이상 기둥**이다. 초기 굽음·하중 편심·잔류응력이 전부 실제 붕괴하중을 낮추며 **knock-down 계수를 적용하지 않았다.** 안전율 1 근처는 안전한 설계가 아니다.
- **굽힘과 축압축을 따로 검사하고 합치지 않는다.** 둘 다 걸리는 부재는 상관식이 필요하고, 여기 개별 안전율은 그 경우에 낙관적이다. 모듈은 그런 duty를 알아채지 못하므로 호출자가 알아야 한다.
- 전부 **SIMULATED**다.

CLI는 `dae failure-modes`로 셋을 한 번에 본다.

## Phase 17: 샤프트 설계와 베어링 L10 수명 (구현 완료)
Phase 11이 관절 토크를 내고 Phase 12가 그걸 낼 모터·감속기를 골랐다. **그 토크는 거기서 멈추지 않는다.** 샤프트로 나가고, 샤프트는 베어링이 잡는다. 이 단계가 그 구간을 메워 duty cycle에서 실제로 하중을 받는 부품까지 경로를 잇는다.

### 17-A: 샤프트 (복합하중, DE-Goodman)
**비틀림만으로 지름을 고르면 틀린다.** 기어나 풀리를 단 샤프트는 전달하는 힘에 의해 **비틀리는 동시에 굽는다.** 더 중요한 것은, **회전축이 정상 횡하중을 받으면 그 굽힘이 완전교번**이라는 점이다. 매 회전마다 한 섬유가 최대 인장에서 최대 압축까지 간다. duty에 변하는 것이 하나도 없어 보여도 샤프트는 피로 문제이고, 정적 비틀림 검사는 그걸 못 본다.

**🔑 DE-Goodman 손검증**: 일반형
`1/n = (16/πd³)·[ sqrt(4(Kf·Ma)²+3(Kfs·Ta)²)/Se + sqrt(4(Kf·Mm)²+3(Kfs·Tm)²)/Sut ]`
가 회전축·정상토크에서 손계산 가능한 축약형
`1/n = (16/πd³)·[ 2·Kf·M/Se + √3·Kfs·T/Sut ]`
과 **상대 1e-15로 일치**한다. 지름식은 같은 관계를 d에 대해 푼 것이고, 두 식이 **상대 1e-12로 서로를 역산**한다(사이징과 검사가 같은 관계여야 한다. 아니면 둘 중 하나가 틀린 것이다).

**독립 교차검증 두 개**로 교과서 대조를 대신했다. 축약형이 두 개의 **독립적으로 알려진 결과**로 환원되는지 확인한 것이다.
- **순수 교번굽힘**(토크 0): `n = Se/(Kf·σ_a)`로 환원. 이건 Phase 16 완전교번 피로검사에 응력집중을 붙인 것과 같다. **다른 유도가 같은 수에 도달**하는 것이 요점이다.
- **순수 정상비틀림**(굽힘 0): `n = Sut/(Kfs·√3·τ)`로 환원. 순수전단의 von Mises 등가를 극한강도에 대고 잰 정적 검사다.

**응력집중 기본값을 1.0으로 두지 않았다.** `Kf=1.7`, `Kfs=1.5`이고 **[ASSUMED]** 다. 무언가를 잡아 주는 샤프트에는 그 자리에 어깨 필렛이나 키홈이나 스냅링 홈이 있고 전부 국부응력을 올린다. 1.0으로 두면 **아무 특징 없는 봉**을 모델링하는 것이고 실제로 기어를 물릴 수 없는 지름이 나온다. 실제 설계는 자기 형상을 재야 한다.

**⚠️ 회전/정지의 배분이 이 모듈에서 가장 결과를 크게 바꾸는 사실이다.** `ShaftLoads.rotating`은 굽힘을 **전부 교번**, 토크를 **전부 평균**에 넣는다. 이걸 뒤집으면 교번응력이 내구한도가 아니라 극한강도에 대고 계산되어 **안전율이 대략 두 배로 부풀어 오른다.** 테스트로 배분과 그 결과(회전 케이스가 더 가혹함)를 둘 다 고정했다.

**위험속도는 의도적 단순화이고 답을 좋게 만든다**: `ω₁ = (π/L)²·sqrt(EI/(ρA))`, 균일 단면·단순지지·**부착질량 없음**. 기어·풀리·로터가 바로 그 무거운 것들이고 위험속도를 크게 낮춘다. 운전속도가 공진 근처인지 보는 용도이지 안전하게 아래라고 결론 내리는 용도가 아니다.

### 17-B: 베어링 L10
**🔑 L10 손검증**: C=20300 N, P=2000 N, p=3에서 `(20300/2000)³×10⁶ = 1.045678375e9` 회전, **상대 1e-15 일치**. 시간 환산 `L10h = L10/(60n)`도 확인. 볼(p=3)과 롤러(p=10/3) 지수 차이로 **하중을 반으로 줄일 때 롤러가 더 많이 얻는 것**까지 확인했다.

**등가동하중** `P = X·Fr + Y·Fa`. 깊은홈 볼베어링의 X·Y는 Fa/C0에 따른 **표준 표**(ISO 281)에서 보간한다. 이건 발행된 표준이지 제조사별 카탈로그가 아니라서 부품번호와 달리 여기 들어올 수 있다. Fa/Fr이 e 이하면 추력이 접촉기하를 바꾸지 않으므로 등가하중은 Fr 그대로다.
**원통 롤러 베어링은 추력을 거부한다.** 플랜지가 없어 축하중을 받지 못하므로, 추력이 들어오면 등가하중에 흡수하지 않고 **예외를 던진다.** 조용히 무시하면 베어링이 실제로 받지 않는 하중조합에 대한 수명을 돌려주게 된다.

**베어링 DB: 치수는 표준, 정격은 카탈로그가 아니다.** 608·6000·6002·6004·6206·NU206은 **ISO 경계치수 코드**이지 벤더 부품번호가 아니다. 6206은 어느 제조사에서든 내경 30·외경 62·폭 16 mm이고, 그걸 적는 것은 표준을 진술하는 것이지 부품을 지어내는 것이 아니다. **C와 C0는 다른 문제라서** 제조사·등급마다 10% 넘게 다르고, 따라서 여기 값은 크기등급의 대표값이며 `illustrative` 태그가 붙는다. 모터·감속기 아키타입, 재료 DB와 같은 규율이다.

**⚠️ 검증기를 하나 고쳤다.** 처음에 `C > C0`를 강제했는데, 이건 여기 있는 소·중형 볼베어링에서만 성립하고 **일반 규칙이 아니다.** 대형 롤러 베어링은 C0가 더 큰 경우가 흔하다(예: NU 2320). 그대로 뒀으면 **정당한 실물 베어링을 거부**했을 것이다. 비율이 [0.1, 20] 범위에 드는지로 바꿔서, kN을 N 자리에 넣은 단위 오류나 두 필드가 뒤바뀐 경우는 여전히 잡으면서 실물을 막지 않게 했다.

### 하중경로 연결
`drivetrain/loadpath.py`가 선정된 구동계를 샤프트·베어링 하중으로 잇는다. 모델은 흔한 배치, **두 베어링에 오버행 하중**이다. 로봇 관절이 대체로 그 모양이고(감속기 출력이 지지되고 링크가 지지점 밖으로 뻗는다), 주어진 하중에 대해 **최대 굽힘모멘트를 만드는 배치**라서 모델링할 가치가 있다.
반력 `R_B = P(L+a)/L`, `R_A = P·a/L`. 손계산·평형 둘 다 확인했다. **가까운 베어링은 가해진 하중의 일부가 아니라 그보다 많이 받는다.** 오버행 하중은 먼 베어링을 **들어올린다.**
`use_peak_torque`는 기본값이 아니라 **결정**이어야 한다. 피로와 베어링 수명은 duty에 걸쳐 누적되므로 연속토크가 맞고, 피크토크로 수명을 계산하면 비의 세제곱만큼 수명을 과소평가한다.

### 검사가 실제로 변별한다
**샤프트: 정적 통과, 피로 탈락.** 굽힘 20 N m, 토크 25.6 N m, SCM440.
d=10 mm에서 정적 SF **2.15**(어느 검토라도 통과시킬 값), 피로 SF **0.784** → **FAIL**. 정적 응력으로 사이징했으면 그대로 출하됐을 샤프트다. 11 mm면 통과한다.
그리고 이건 한 지름의 우연이 아니다. **두 안전율이 모두 d³로 스케일해서 비가 고정**이므로, 어떤 지름에서도 정적 검사가 구속조건이 되지 않는다. 정적만 보는 설계 절차는 **한계 근처가 아니라 모든 크기에서** 틀린다.

**베어링: 같은 부품, 같은 하중, 감속기 반대편에서 반대 판정.** 608에 650 N.
관절측(3 rad/s, 29 rpm): L10 **79,644 h**, 여유 3.98 → PASS.
모터측(300 rad/s, 2865 rpm): L10 **796 h**, 여유 0.04 → **FAIL**.
**회전수 기준 수명은 동일**하다(같은 하중이므로). 감속기가 토크를 속도와 맞바꾸므로 모터측은 그 회전을 100배 빨리 소모하고, 시간 기준 수명이 100분의 1이 된다. 테스트가 `L10_회전수`가 같고 `L10_시간`이 정확히 비만큼 다른 것을 고정한다.

### registry와 정직성
`shaft_combined`(ANALYSIS), `bearing_l10`(SELECTION)을 등록했다. **인장 봉은 샤프트가 아니고, 회전 지지가 없으면 베어링 수명이 없다.** 각각 사유와 함께 배제된다.
**미구현은 등록하지 않았다**: 3D 샤프트 FEA, 상세 접촉피로, 기어 AGMA, 볼트 이음. 전부 등록되어 있지 않다.

**남는 한계**
- 샤프트: **중실 원형 단면만**. Kf·Kfs는 **[ASSUMED]** 이고 실물 대비 낙관적일 수 있다. 위험속도는 **부착질량 무시**로 낙관적이다. 피로는 고주기 전용(Phase 16 상속).
- 베어링: **L10은 통계다.** 열 중 하나는 그 전에 고장 나며, 특정 베어링에 약속된 수명은 없다. **ISO 281의 신뢰도·윤활·오염·온도 계수를 적용하지 않았고**, 각각이 답을 한 자릿수 넘게 움직일 수 있다. 카탈로그는 `illustrative`.
- 전부 **SIMULATED**.

CLI는 `dae loadpath`로 선정부터 샤프트 지름·베어링 수명까지 한 번에 본다.

## Phase 18: 모터 정상상태 열과 구조 열응력 (구현 완료)
Phase 12가 "subject to thermal validation"으로 남긴 프록시를 진짜 열 모델로 대체한다. 연속토크 정격 자체가 **열에 대한 진술**이고, 하나의 주위온도와 하나의 장착조건에서 매긴 것이라, 그와 다른 duty는 그 정격이 보증하지 않는다.

### 18-A: 모터 정상상태 열
**🔑 손검증**: `P_cu = k·T_rms²`, `P_fe = k·ω`, `T_rise = P_loss·R_th`. 전부 상대 1e-15 일치.

**⚠️ 평균토크가 아니라 RMS 토크여야 한다.** 동손이 토크의 **제곱**으로 가기 때문이다. 절반은 1 N m, 절반은 정지인 duty는 평균 0.5, RMS 0.7071이고, 평균을 쓰면 발열을 **두 배 과소평가**한다. duty가 뾰족할수록 오차가 커지므로, **오차가 가장 큰 곳이 정확히 문제가 되는 곳**이다.

**⚠️ 아키타입 열계수를 재보정했다.** 처음에 손실계수와 열저항을 서로 독립으로 골랐더니 연속토크에서 **온도상승이 6 K**로 나왔다. 그러면 어떤 열검사도 통과하고 계산 전체가 장식이 된다. **모터의 연속토크 정격은 곧 열 정격**이므로, 연속토크·정격속도에서 절연등급 한계 근처에 떨어지도록 세 계수를 맞췄다. 결과가 물리적으로 정합한다: 상승 약 95 K, 40 °C 주위에서 권선 135 °C(F등급 155 °C 대비), 효율 70~77%. 테스트가 이 자기정합성을 고정한다.

**실증 1: 프록시가 통과시키는 duty가 권선을 태운다.** bldc_100w(연속 0.32 N m).
duty: 절반 0.60 N m, 절반 정지. **평균 0.30 N m → 연속토크 프록시 통과.** RMS는 **0.4243 N m**, 권선 **178 °C**(한계 155) → **FAIL**.
프록시는 duty의 **평균만 보고 모양을 못 본다.** 이것이 Phase 12가 미뤄 둔 검증 그 자체다.

**실증 2: 주위온도가 마진을 1:1로 먹는다.** 같은 duty(0.30 N m)에서 25 °C 주위 → 권선 111 °C PASS, 70 °C 주위 → **156 °C FAIL**. duty는 하나도 안 바뀌었다. 벤치에서 25 °C로 사이징한 드라이브가 자기 드라이버 옆에 설치되면 45 K의 마진을 잃는다. 그래서 기본 주위온도를 상온이 아니라 **40 °C**로 뒀다.

### 18-B: 구조 열응력
**🔑 손검증**: `σ = -E·α·ΔT`. 7075-T6, 71.7 GPa, 23.6e-6/K, 60 K → **-101.527 MPa**, 상대 1e-15 일치.

**부호가 장식이 아니다.** 구속된 부재를 **가열하면 압축**, **냉각하면 인장**이다. 이게 기계적 인장에 더해지는지 덜어지는지, 좌굴을 유발할 수 있는지를 결정한다. 크기만 돌려주면 **답의 쓸모 있는 절반을 버리는 것**이다.

**실증 3: 냉각이 위험한 방향이고, 그게 직관과 반대다.** 7075-T6 브래킷, 기계적 인장 350 MPa(항복 503, SF 1.44로 통과).
`ΔT = +100 K` → 열응력 **-169 MPa**, 합성 181 MPa, **SF 2.78** (완화된다).
`ΔT = -100 K` → 열응력 **+169 MPa**, 합성 **519 MPa**, **SF 0.97 → FAIL**.
기계적 하중은 하나도 안 바뀌었는데 **식으면 항복을 넘는다.** 크기를 더하는 모델이었다면 이 두 경우를 구별하지 못했을 것이다.

**⚠️ 강이 알루미늄보다 켈빈당 응력이 크다.** 직관과 반대이고 계수만으로 재료를 줄 세우면 틀리는 지점이다. 강은 팽창이 절반이지만 강성이 세 배라 **구속 비용이 더 크다**: 2.3985 MPa/K 대 1.6921 MPa/K. 탄성계수를 빼놓을 수 없는 이유다.

**구속계수는 모델의 필수 입력**이다(0=자유, 1=완전구속). 같은 온도변화가 장착방식에 따라 응력 0에서 항복 수준까지 나온다. **자유팽창은 변형만 있고 응력이 없다.** 열응력을 만드는 것은 온도가 아니라 구속이다.

**이종재료 접합**: `(α_a - α_b)·ΔT`. 외부 구속이 없어도 **서로가 서로를 구속**한다. 알루미늄-강 100 K에서 **1190 마이크로스트레인**, 알루미늄 자체 팽창의 절반이다.

### 재료 DB에 열팽창계수 추가
15종 전부에 `thermal_expansion_1_k`를 넣었다(reference_typical, 1/K). **기존 값은 하나도 바꾸지 않았다**(diff로 확인).

**⚠️ CFRP 때문에 필드를 양수로 제약할 수 없었다.** 탄소섬유는 가열하면 길이방향으로 **수축**한다. α₁ = **-0.5e-6**(음수), α₂ = +25e-6. **50배 차이에 부호가 반대**다. `gt=0.0` 제약을 걸었다면 물리적으로 옳은 값을 거부했을 것이고, 단일 값을 쓰면 어느 값을 고르든 한 방향에서 틀린다. 그래서 방향별 필드를 두고, 직교이방 재료가 단일 α만 선언하면 검증기가 거부한다(단일 항복강도가 무의미한 것과 같은 이유이고, 부호까지 다르므로 더 나쁘다).
**고분자 값은 산포가 크다**(±20%, 등급·충전재에 따라). 헤더에 명시했고 자릿수 수준으로 보라고 적었다. **CTE 자체가 온도의존**이며 여기 값은 상온 기준이라는 것도 적었다.

### registry와 정직성
`motor_thermal`, `thermal_stress` 등록. **duty cycle이 없으면 모터를 데울 것이 없고, 온도변화가 없으면 열응력이 없다.** 각각 사유와 함께 배제된다.
**미구현 미등록**: 과도 열해석, 상세 열저항망, CFD, 접촉 열저항.

**남는 한계**
- **정상상태 전용.** 짧은 과부하는 여기서 걸려도 실제로는 괜찮을 수 있고(열용량이 못 따라감), 평균으로 통과한 긴 과부하가 권선을 태울 수도 있다. 과도 해석은 없다.
- **열저항이 단일 집중 상수**다. 실제로는 권선-케이스와 케이스-주위가 직렬이고 **장착이 후자를 지배**한다. 큰 알루미늄 브래킷에 볼트로 조이면 절반이 되고 공중에 세워두면 두 배가 된다. 한 숫자로 표현할 수 없으며 **어느 방향으로든 두 배 틀릴 수 있다.**
- **철손이 속도에 선형**이다. 실제로는 히스테리시스(대략 선형)와 와전류(대략 제곱)로 갈리므로 **고속에서 과소평가**한다.
- 열응력은 **균일 온도변화·균일 구속**이다. 실제 부품에는 **온도 구배**가 있고, 구배는 **구속이 전혀 없어도 응력을 만든다.** 이 모델은 그걸 표현하지 못한다.
- 계수는 `reference_typical`(CTE)·`illustrative`(R_th, 손실계수). 전부 **SIMULATED**.

CLI는 `dae thermal`로 둘을 한 번에 본다.

## Method Registry와 향후 방법 라이브러리 확장
registry에 등록된 방법이 시스템의 능력 상한이다. 지금 **없는** 것을 적어 둔다. 이후 단계는 이것들을 하나씩 **검증하고 등록하는** 일이며, 검증 없이 등록하는 것은 금지된다.

**구조 해석**
- **좌굴**: (Phase 16에서 **해석적 오일러 완료**) 남은 것은 **3D FEM 선형좌굴 고유치**(기하강성 Kσ)와 **국부 좌굴**(박벽 웹·플랜지). 해석적 오일러는 전체 기둥 좌굴만 보고 얇은 벽이 국부적으로 접히는 것은 못 본다.
- **피로**: (Phase 16에서 **S-N + Goodman/Soderberg 완료**) 남은 것은 **Miner 누적손상**(가변진폭 스펙트럼), **저주기/변형률-수명**, **노치·표면·치수 계수**.
- **열**: (Phase 18에서 **완료**, 모터 정상상태 + 구속 열응력 + CTE 15종) 남은 것은 **과도 열해석**(열용량·시정수), **분포 열저항망**, **온도장/구배로 인한 열응력**, CFD, 접촉 열저항, **온도의존 CTE·물성**.
- **접촉**: 볼트 이음, 압입, 베어링 좌면. `physics/collision.py`는 스텁이다.
- **비선형**: 대변형, 소성, 좌굴 후 거동. 현재 FEM은 선형탄성·미소변형 전용이다.
- **모달·다중모드**: 고유진동수, 모드형상, 공진 회피.

**재료**
- **완전 이방성**: 지금은 등방과 직교이방까지다. 21개 독립상수의 일반 이방성은 없다.
- **온도의존 물성**: E(T), 항복(T).
- **CLT(고전 적층판 이론)**: 복합재 적층 설계.

**형상·구조 생성**
- **형상 최적화**: 경계를 직접 움직이는 방식. 지금은 밀도장(SIMP)뿐이고, 그래서 Phase 14a가 깨끗한 필렛을 못 만든다.
- **Lattice·주기적 미시구조**: 균질화 기반.
- **Implicit·레벨셋 표현**: 밀도 임계처리 없이 경계를 직접 다룬다.

**최적화**
- **MMA**: 응력제약 토폴로지의 표준 최적화기. 지금은 벌점 투영법이고 제약 경계에서 진동한다.
- **베이지안 최적화**: `optimization/bayesian.py`는 스텁이다. 비싼 평가에서 필요하다.
- (Pareto 비지배 필터는 구현되어 있고 등록되어 있다.)

**기계요소 (전부 없음)**
- **베어링 L10 수명**: (Phase 17에서 **완료**) 남은 것은 **ISO 281 보정계수**(신뢰도 a1, 윤활·오염 a_ISO), 상세 접촉피로, 앵귤러·테이퍼 형식.
- **샤프트 설계**: (Phase 17에서 **완료**, DE-Goodman 복합하중) 남은 것은 **중공·단차 단면**, 측정된 Kf(현재 [ASSUMED]), **부착질량 포함 위험속도**, 3D 샤프트 FEA.
- **볼트 이음**(예압, 이음강성, 풀림), **기어**(AGMA 굽힘·접촉 응력, 치형): 미착수.

## Post-Phase-7 업그레이드 로드맵 (Phase 8~10)
확정된 방향의 기록이다. 지금 착수하지 않는다.

### Phase 8: 재료 전면 확장 + 이방성
- 재료 DB를 전 물질군으로 확장: 금속에 더해 비금속(고분자, 세라믹, 복합재, 엘라스토머).
- **물성 추론층**: DB에 없는 재료의 값을 **불확실성과 출처를 함께** 추정한다. 기존 evidence-level 규율을 그대로 따른다. 추정값을 데이터시트 값처럼 표기하는 것은 금지다. 비금속은 온도·변형률·습도 의존이고 복합재는 공정 의존이므로 단일값이 아니라 조건과 범위로 다룬다.
- **물리 확장**: 등방성 E/ν에서 직교이방성·이방성 강성텐서로. 재료 스키마에 방향별 물성(E1/E2/E3, G12/G13/G23, ν12/ν13/ν23)과 복합재 적층·배향을 추가한다. FEM 요소는 직교이방성 벤치마크로 재검증한다.
- 기존 "재료 확장 로드맵"의 3단계(이방성)를 구체화한 것이 이 Phase다.

### Phase 9: CAD 출력 (STEP)
- OpenCascade 계열(pythonocc, CadQuery, build123d)로 B-rep을 만들고 STEP으로 내보낸다.
- **난이도를 정직하게 구분한다**: 파라메트릭 솔리드에서 STEP 출력은 간단하고 보장할 수 있다. 반면 토폴로지 최적화 결과나 유기적 형상은 깨끗한 STEP이 어렵다. 그 경우 메시·STL 경로나 표면 재구성이 필요하다. "항상 STEP"은 파라메트릭 경로에서만 보장되며, 유기 형상은 별도 경로임을 명시한다.

### Phase 10: 어셈블리와 기구학
- 단일 부품에서 다물체 조립으로: 링크와 조인트(회전, 직동 등), 구속조건 데이터 모델, 운동학 표현(DH 또는 URDF 유사), 다물체 하중 전달.
- 최종 비전(링크 → 조인트 → 감속기 → 다리 → 휴머노이드)의 축이며 기구학·동역학 원칙을 도입한다.

**순서와 의존성**: Phase 8과 9는 서로 비교적 독립이라 병행 가능하다. Phase 10이 가장 크고 앞의 둘 위에 얹힌다(이방성 재료로 만든 부품을, CAD로 내보낼 수 있어야 조립이 의미를 갖는다).

## 7. 현재 상태
**Phase 1 완료.** Phase 0(venv+스켈레톤+프로파일+Warp/torch 새너티) 위에 Engineering IR(문제 표현), 재료 DB, Design Genome(중공 사각단면 MVP)을 구현. 역할 분리: IR=문제(고정: 길이·하중·재료·제약·목표), Genome=설계변수(가변: 단면 치수).
- 테스트 100개 통과. 단면2차모멘트 공식은 해석식과 별개의 수치적분(격자 리만합)으로 독립 검증(최악 상대오차 ~1e-5, 허용 1e-4).
- 재료는 MVP 기준 등방성 Al 2종(al_7075_t6, al_6061_t6)만. 확장은 위 "재료 확장 로드맵" 순서.
- Warp 1.16.0 + torch 2.13.0+cu130, RTX 3050(sm_86) 인식·커널 검증. 자동감지 → laptop_4gb.

**다음 단계는 Phase 2, Warp GPU 물리 코어** (보/링크 응력·처짐·질량, 미분가능, 해석해 대조 검증).
